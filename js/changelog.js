// ======================================================================
// Changelog — the source of truth for both the "What's new" overlay shown
// after sign-in and the full changelog page.
//
// Same discipline as ENCA's js/changelog.js (read its header for the long
// version):
//
// HOUSEKEEPING: whenever a tool is added or changed, add a NEW release
// object here for that build, in the same commit as the code — together
// with the home-tile NEW/BETA/UPDATED tag in index.html, the TOOL_VERSIONS
// bump, APP_BUILD.build + the ?v= cache-busting numbers, and js/promote.js
// when the change lands on the beta channel. One release object per build,
// holding ONLY what changed in that build; never bump an existing release's
// number to cover new work.
//
// kind: "new"      — a whole tool or capability that did not exist
//        "improved" — an existing tool got better
//        "fixed"    — something was wrong and now is not
//
// TEXT IS PLAIN TEXT. The renderer escapes it, so an HTML tag written here
// comes out as angle brackets mid-sentence (ENCA carried 228 broken tags
// before noticing). Quote a literal token — "bad" — rather than reaching
// for code, and carry emphasis in the words.
//
// Newest release first.
// ======================================================================
const CHANGELOG = [
  {
    build: 10517, date: "2026-08-31", title: "A partly-enforced protection now says what happens to everyone else",
    items: [
      { kind: "fixed", tool: "T20", text: "THE IMPACT BRIEF WENT SILENT ABOUT THE REST OF THE FLEET. A statement headed \"partly enforced on ~38 of 9947 devices \u00b7 9909 not yet targeted\" then said nothing at all about those 9909, because the at-rollout line was only written when a policy was staged behind the statement. A rollout communication that reports a hole and not its plan is worse than one that reports neither." },
      { kind: "improved", tool: "T20", text: "Every partly-enforced statement now carries an at-rollout line, in place under Already enforced today. Where the remainder can be counted it is quoted against the fleet: nothing is staged for the remaining 9852 of 9947, so this reaches the same machines after rollout as it does today." },
      { kind: "improved", tool: "T20", text: "Where an assignment FILTER is what narrows the statement, the line says so — the filter is the cause, not the assignment, and nothing is staged to widen it — and deliberately quotes no remainder, because a filter rule that could not be evaluated leaves out a set nobody can count from a browser." },
      { kind: "fixed", tool: "T20", text: "Coverage AFTER rollout is now a range rather than the wrong number. A partly-enforced statement whose staged policy is itself partial was being given the staged policy\u2019s own target, which answers a different question — the union of today\u2019s devices and the destination is a set whose membership nobody read, so the line states at least the larger and at most the sum, and refuses to state a range at all when either side is a filter ceiling or an unreadable count." },
      { kind: "improved", tool: "T20", text: "The heading word and the new line now read one predicate, so they cannot disagree about what partly means. A side effect worth naming: a filter-capped sum that happened to land at or above the fleet size used to be headed enforced now, and no longer is." },
    ],
  },
  {
    build: 10516, date: "2026-08-31", title: "A default field look that can actually be overridden",
    items: [
      { kind: "fixed", tool: "T01", text: "THE VERSION TOKEN BOX WAS THE WIDTH OF THE PAGE. In the Get it into Intune step it is an inline field in the middle of a sentence — the profile name, then the version, then the grouping — and it had been stretching to the full width and wrapping that sentence into three lines around itself." },
      { kind: "fixed", tool: "TUNO", text: "THE CAUSE WAS THE APP-WIDE FIELD LOOK, not the tool. Build 10466 made every text field look like a text field, which was right, and wrote the rule with five exclusions for tick boxes and sliders. Five exclusions weigh as much as five class names in the cascade, so the \"default\" outranked every field rule in the app and its full-width setting could not be argued with by anything. A default that cannot be overridden is not a default." },
      { kind: "improved", tool: "TUNO", text: "The exclusions are now written so they carry no weight of their own. They still mean exactly what they meant — tick boxes, radios, file, colour and range pickers keep their own treatment — but a field that asks for its own width now gets it. Measured against the deployed beta before and after: the version box goes from 981 pixels to 72." },
      { kind: "improved", tool: "T01", text: "Two smaller shifts come with it, both restoring what those rules already asked for: the AppLocker form fields and the deploy row fields get back the padding they declare, and an input styled as a button gets button padding rather than field padding." },
    ],
  },
  {
    build: 10515, date: "2026-08-31", title: "The examine popout leaves the tab bar reachable",
    items: [
      { kind: "fixed", tool: "T22", text: "THE POPOUT COVERED THE TAB BAR, so reaching another tool meant closing it first — the opposite of what moving the detail into a popout was for. It is a working pane you leave open while you think, not a dialogue that has to be answered. It now starts below the tab bar and sits underneath it, so the tabs, the ＋ menu and the home button all stay live while it is up. It still covers the work it is acting on, and still closes by its button, the backdrop or Escape." },
      { kind: "improved", tool: "T23", text: "Deliberately not applied to the create, edit and delete dialogues: those ask a question that has to be answered before anything else happens, and blocking is what they are for." },
    ],
  },
  {
    build: 10514, date: "2026-08-31", title: "Policy overview takes the posture tool's layout",
    items: [
      { kind: "improved", tool: "T19", text: "THE THIRTEEN SURFACES MOVE INTO A STICKY LEFT RAIL, the shape the endpoint posture tool already uses — chosen from a two-option mockup. They arrived as a horizontal grid of cards above the list, which reads well and costs the top third of every screen; in a rail they have a permanent home and the whole width of the pane becomes the answer." },
      { kind: "improved", tool: "T19", text: "THE COUNTS SURVIVED THE MOVE rather than being traded for the space: every row carries its pair — how many of that surface's objects reach somebody, over how many exist — with a legend under the All surfaces row saying which number is which." },
      { kind: "improved", tool: "T19", text: "A surface that could not be read keeps BOTH of its homes: a red row in the rail and the note above the list. It is still not a filter, because filtering to a surface nobody read would be an empty grid pretending to be an answer — and one red line is easier to skim past than the dashed card it replaced, which is why it kept the second home." },
      { kind: "improved", tool: "T19", text: "The rail uses the posture tool's own classes rather than a second rail that drifts away from it. Two tools showing the same kind of thing should not slowly stop looking alike." },
      { kind: "improved", tool: "T19", text: "Nothing else changed: the cards and list faces, the verdict chips, the search and the shared policy popout all behave exactly as before. The other layout option — a strip of count chips above the list, with the rail carrying names only — is recorded in the code rather than discarded, in case the numbers read too quietly in their new home." },
    ],
  },
  {
    build: 10513, date: "2026-08-31", title: "The reach line belongs to the statement it measures",
    items: [
      { kind: "fixed", tool: "T20", text: "IN THE IMPACT BRIEF, \"partly enforced\" FLOATED BETWEEN TWO STATEMENTS as though it were a third protection, when it is a qualification of the one above it. The brief has always written it as a child of that statement; the report viewer was flattening it, because its list matcher swallowed the indentation and turned every child bullet into a sibling. The relationship was in the text all along and the renderer was losing it." },
      { kind: "improved", tool: "TUNO", text: "THE REPORT VIEWER NOW RENDERS NESTED LISTS, to any depth, for every tool — one viewer, so one fix. Ordered lists nest inside unordered ones and the other way round; a heading or a table closes whatever is open." },
      { kind: "fixed", tool: "TUNO", text: "A table written straight after a list, with no blank line between them, was being emitted INSIDE the list. Every other kind of block closed the list first and this one did not. Found by the new tests rather than by a reader, because the result looked merely cramped." },
    ],
  },
  {
    build: 10512, date: "2026-08-31", title: "T23 Restricted AUs — the vaults, and who may open them",
    items: [
      { kind: "new", tool: "T23", text: "RESTRICTED AUs, ported from the sister tool's own version. A restricted management administrative unit is the only place in Entra where membership can be put out of reach of every tenant-wide role: inside one, only a principal holding a role scoped to that unit may change the members, and Global Administrator and Privileged Role Administrator can manage the unit but not what is in it. This is where Group migration files a migrated group, and this is where those units are now created and managed — list, create, rename, delete, members, scoped administrators." },
      { kind: "new", tool: "T23", text: "THE FLAG IS IMMUTABLE AND THE EDITOR SAYS SO. Restricted management is set when a unit is created and can never be added or removed afterwards, so there is no convert: the editor changes the name and the description, and a unit under the right name without the flag has to be renamed aside and replaced." },
      { kind: "new", tool: "T23", text: "A UNIT WITH NO SCOPED ADMINISTRATOR IS A VAULT NOBODY CAN OPEN, so creating a restricted one without naming a keyholder is refused rather than warned about — tenant-wide roles are blocked by design and the members would be unmanageable by everyone. The field arrives prefilled with the signed-in account: whoever creates the unit is its administrator unless they say otherwise." },
      { kind: "new", tool: "T23", text: "FROZEN MEMBERS ARE FLAGGED. A role-assignable group inside a restricted unit admits only Global Administrator or Privileged Role Administrator while the unit blocks exactly those two — so nobody at all can change its members. The tag says so and names Group migration as the way out." },
      { kind: "new", tool: "T23", text: "Deleting a unit does not delete its members; they lose the shield and become manageable tenant-wide again, all of them at once. That is why the confirmation is typed rather than clicked." },
      { kind: "improved", tool: "T23", text: "Directory writes are not read-your-writes: a re-read straight after adding a member faithfully reports the old state, so an added row would vanish in front of you and reappear a minute later. Changes are held optimistically and re-checked with a backoff, and the tool says plainly when the directory has not caught up rather than pretending the change failed." },
      { kind: "improved", tool: "T23", text: "None of the sister tool's Conditional Access baseline came across — no persona vaults, no policy-number routing, no group-to-persona map. That machinery answers a question TUNO does not ask, and importing it would have meant a vocabulary this tool cannot honour." },
    ],
  },
  {
    build: 10511, date: "2026-08-31", title: "Examine opens over the list, and the chips filter it",
    items: [
      { kind: "improved", tool: "T22", text: "EXAMINE IS A POPUP NOW, NOT A SCREEN SWAP. Deciding a migration means comparing one group against the others, and replacing the list with the detail made that a matter of memory. The detail opens in the same modal the Group Analyzer and the RBAC tool use, with the list still underneath it. It opens before the read finishes and holds its own progress, because nine surfaces take a while and a blank screen for the duration was the reason this moved. Backdrop and Escape close it." },
      { kind: "new", tool: "T22", text: "THE CHIPS ARE FILTERS — all, carries a membership rule, no destination worked out. Clicking the one in force clears it, so a filter is never a state you cannot leave, and a filter that would empty the list stays hidden until it is the one in force." },
      { kind: "improved", tool: "T22", text: "Only the chips that CAN filter became buttons. The restricted-unit count and the detected naming prefix are facts about the tenant rather than properties of a group; dressing them as filters would promise a narrowing that cannot happen." },
      { kind: "new", tool: "T22", text: "WHOEVER CREATES THE UNIT IS ITS ADMINISTRATOR BY DEFAULT. The scoped-administrator field arrives prefilled with the signed-in account — a restricted unit blocks every tenant-wide role, so someone standing one up without naming a keyholder has locked themselves out of the vault they just built. Prefilled, not forced: it is a text field and it takes anyone." },
    ],
  },
  {
    build: 10510, date: "2026-08-31", title: "The permissions card is a button",
    items: [
      { kind: "improved", tool: "T22", text: "THE PERMISSION LIST COMES OFF THE PAGE AND ONTO THE BUTTON. Yesterday's build put the whole inventory on screen — three tables of scopes, taller than the tool they were serving — and a permission inventory is something you consult once, not something that sits above the work. It is now a single Grant permissions button in the same row as Read, carrying the full list on its tooltip: every scope, what each one buys, and the warning that three of them write to the directory rather than to Intune. Microsoft's own consent screen lists them again at the moment it matters, so the shrink hides nothing." },
      { kind: "improved", tool: "T22", text: "The per-scope ticks went with the card rather than being squeezed into the tooltip. They reported what the current session had acquired, which is not the same as what the tenant has consented — and two states that look alike but mean different things are worse in a tooltip than in a table. The button says what it will do instead." },
    ],
  },
  {
    build: 10509, date: "2026-08-31", title: "Grant it once, find it faster, and finish the cleanup the report asks for",
    items: [
      { kind: "new", tool: "T22", text: "ONE BUTTON FOR EVERY PERMISSION THE TOOL NEEDS. Asking at each step is the app's model and it stays — reading costs read permissions, the write permissions are requested at the migrate click — but for this tool that is up to four Microsoft prompts in a single run. The permissions card lists every scope with what it buys, marks the three that write to the DIRECTORY rather than to Intune, and grants the lot in one go for anyone who would rather agree once with the whole list in front of them. Pressing Read without it behaves exactly as before." },
      { kind: "improved", tool: "T22", text: "The permission ticks say THIS SESSION HOLDS IT, in those words, rather than implying the tenant has consented. A page can see the tokens it holds and cannot see a tenant's grants, and a chip that blurred the two would be the one place in the app where a permission screen guessed." },
      { kind: "new", tool: "T22", text: "A SEARCH BOX over the role-assignable groups, matching the name or the object id — an id is what a report hands you. It filters the list already read rather than querying the tenant on every keystroke: a directory typeahead here would re-read groups this screen has just read, and would suggest groups that are not role-assignable, which is every group the tool cannot act on." },
      { kind: "new", tool: "T22", text: "THE SCOPED ADMINISTRATOR FIELD AUTOFILLS FROM THE TENANT, through the app's existing suggestion component rather than a second one. It fills the user principal name, because two people can share a display name; typing an object id or a name the suggestions never offered keeps working, since the field is not a picker." },
      { kind: "new", tool: "T22", text: "ARCHIVED GROUPS CAN NOW BE SELECTED AND DELETED — the last step the migration report asks for, finished by the tool that wrote it. The guard is that report's own sentence made mechanical: the reference check runs first, a group anything still points at is REFUSED rather than warned about, changing the selection invalidates the check, and deleting stays locked until a check has run — an unchecked group is an unknown one, and unknown is not unused. The screen also says plainly that a deleted group is recoverable for thirty days." },
      { kind: "improved", tool: "T22", text: "The reference check for a whole selection is ONE read pass matched against every ticked group, not one pass per group. Asking thirteen surfaces the same question four times over is how a cleanup becomes something nobody runs." },
    ],
  },
  {
    build: 10508, date: "2026-08-31", title: "The permission the unit read actually needed, and a member type it stopped guessing",
    items: [
      { kind: "fixed", tool: "T22", text: "THE ADMINISTRATIVE-UNIT READ ASKED FOR THE WRONG PERMISSION. It was handed the app's shared \"directory\" scope constant, which is named for the directory but holds the user and group read permissions rather than the directory-wide one — so the tenant refused it with \"Insufficient privileges to complete the operation\". Administrative units now carry their own least-privileged read permission, listed alongside the write one because Entra consents by name and a token asked for the read is refused unless the read itself was granted." },
      { kind: "fixed", tool: "T22", text: "AND THE SCREEN BLAMED THE WRONG CALL. Both reads sat in one error handler, so a refusal on the administrative units printed \"Could not read the groups\" — a message about the call that had just succeeded, which sent the first real diagnosis to the wrong half of the tool. The two reads now report separately and each names the permission it wanted." },
      { kind: "improved", tool: "T22", text: "A failed unit read no longer takes the group list with it. The list is still correct and still worth having, so it renders under a warning card that says plainly that nothing can be migrated until the read works — without it the tool cannot tell whether a group is already inside a restricted unit, which is the frozen case where nobody at all can change the members." },
      { kind: "fixed", tool: "T22", text: "MEMBER TYPING ASKS THE DIRECTORY INSTEAD OF INFERRING IT. The member split read a type property off a response that Microsoft's own documented example returns without one, and treated its absence as \"this is a user\". A service principal is allowed in a role-assignable group, so one would have been counted as a user and quietly not copied — a clean-looking migration that lost a member. The tool now asks the directory which members are users and compares that against the total; any difference is refused, naming the object id to move by hand." },
      { kind: "new", tool: "TUNO", text: "FIVE DELEGATED PERMISSIONS ADDED TO THE APP REGISTRATION SCRIPT, in the open, with the tool that needs them — three of which write to the directory rather than to Intune, which is a wider blast radius than anything before them. Each buys exactly one step of the migration and the script says which. They need an administrator to consent once per tenant before the tool can run." },
    ],
  },
  {
    build: 10507, date: "2026-08-31", title: "Administrative units live under /directory on v1.0",
    items: [
      { kind: "fixed", tool: "T22", text: "THE TOOL COULD NOT READ ANYTHING. Every administrative-unit call came back \"Resource not found for the segment 'administrativeUnits'\" on the first real tenant it met — before it could list a single group. The sister tool it was ported from talks to the beta endpoint, where an administrative unit is a top-level collection; TUNO talks to v1.0, where the resource is nested under the directory. The port carried the path across along with the reasoning." },
      { kind: "fixed", tool: "T22", text: "FIXED BY USING THE RIGHT PATH, NOT BY MOVING TO BETA. Everything about the resource is generally available on v1.0 — including the restricted-management flag that is set at creation and can never be changed — so a directory WRITE had no business reaching for a preview endpoint. One constant now carries the path for the unit list, the unit create, the member reference and the scoped-role grant, so there is a single place to be wrong. The membership read also takes the cast form v1.0 documents, which is not the one that works elsewhere in the app." },
      { kind: "improved", tool: "T22", text: "The headless suite now REFUSES the old path the way the tenant did, and asserts every administrative-unit call the tool makes. A fake that answers a path which does not exist is how this shipped in the first place." },
    ],
  },
  {
    build: 10506, date: "2026-08-28", title: "T22 Group migration — off role-assignable, into a restricted unit",
    items: [
      { kind: "new", tool: "T22", text: "GROUP MIGRATION, ported from the sister tool ENCA's seventh CA-groups step. A role-assignable security group becomes a plain one inside a restricted management administrative unit. Role-assignable was only ever a way to keep membership away from tenant-wide group administrators — a restricted unit does that and lets you NAME who may manage the group, without the 500-per-tenant cap, the ban on dynamic membership, or the loss of nesting control." },
      { kind: "new", tool: "T22", text: "THE TWO CAN NEVER BE COMBINED, and that is the state this tool exists to get you out of. A role-assignable group admits only Global Administrator or Privileged Role Administrator; a restricted unit blocks exactly those two; and neither can be assigned at administrative-unit scope. A group that is both is a group whose members nobody can edit. The tool refuses to create that state and names it where it already exists." },
      { kind: "new", tool: "T22", text: "THE NEW GROUP HAS A NEW OBJECT ID, because isAssignableToRole is immutable and this is therefore a recreate rather than a patch. What TUNO may write it repoints: the four surfaces the Assignment editor already writes, using that tool's own engine, so the fresh-read drift check and the verify read-back are the ones that have been in production since build 8 rather than a second copy of them." },
      { kind: "new", tool: "T22", text: "WHAT IT CANNOT REPOINT, IT NAMES. Applications, scripts, app protection and configuration, enrolment restrictions, Autopilot profiles and update rings are read through the Group Analyzer's reader and listed as work to finish by hand — each would need a write scope this registration does not declare, and adding a write scope is a decision taken in the open. Everything outside Intune is declared invisible in the same breath: Conditional Access, group-based licensing, Azure RBAC and app role assignments all address a group by object id." },
      { kind: "new", tool: "T22", text: "THE ORDER IS THE SAFETY PROPERTY. Members move while the new group is still ordinary, because once it is inside a restricted unit only a unit-scoped role could add them; and every assignment gains the new group BEFORE it loses the old one, so nothing is ever pointed at neither. The original is renamed aside and never deleted — it keeps its members, stays role-assignable, and is the rollback until the references this tool could not move have been checked." },
      { kind: "new", tool: "T22", text: "FIVE REFUSALS, each with the reason printed. A group that holds a directory role (a plain group cannot carry one); a group with a non-user member (this tool copies users and would leave the rest behind in silence); an assignment read that did not complete (migrating then would repoint an unknown fraction and report the rest as the whole remainder); no destination unit (a group with nowhere to go is skipped rather than filed into whichever unit was nearest); and a NEW unit with no scoped administrator named, because a restricted unit nobody is scoped to is a vault nobody can open, including you." },
      { kind: "new", tool: "T22", text: "UNITS FOLLOW THE TENANT'S OWN INT- CONVENTION. The suggested name is built from the group's own segments with the tenant's dominant prefix detected and stripped, shown per group before anything is applied, and overridable — a naming rule is a default, never a decision made behind the operator. The unit is the boundary, so two groups in one unit share their administrators." },
    ],
  },
  {
    build: 10505, date: "2026-08-28", title: "The filter count reaches the arithmetic, and the brief stops overclaiming",
    items: [
      { kind: "new", tool: "T20", text: "A FILTERED TENANT-WIDE TARGET IS A COUNT, NOT A CEILING. \"All devices with an include filter\" is not \"the whole fleet, at most\" — it is exactly the devices the rule matches, and the filter-rule evaluator built for the Assignment filters tool can count them. Exclude mode is exactly the fleet minus that set. Both are measurements, and this house has been rendering them as \"at most all 9964\" since filters were understood at all. The fleet read now asks for the fields a rule reads, so one read answers both questions." },
      { kind: "improved", tool: "T20", text: "A GROUP plus a filter stays a bound, deliberately: the filter narrows the group, and the intersection needs the group's MEMBERSHIP, which nobody has read — only its size. Widening the new arithmetic to groups would mean guessing an overlap, which is the one thing this file never does. A filtered wide target alongside group targets is reported as a FLOOR rather than a measurement, for the same reason. And a rule the grammar cannot fully read falls back to the old ceiling and now NAMES what stopped it, instead of a bare \"at most\"." },
      { kind: "fixed", tool: "T20", text: "An unfiltered tenant-wide target was being capped by a filter on a DIFFERENT target of the same policy. All devices with no filter reaches everybody, and a second, filtered target cannot take that away — but any filter anywhere set the cap, so a policy that genuinely reached the whole fleet was reported as reaching at most the whole fleet. It now says the number." },
      { kind: "improved", tool: "T20", text: "ENFORCED NOW, OR PARTLY ENFORCED — the leading word carries it. A statement whose policies reach one 27-device group was headed \"enforced now\", with the 27 two lines below where a skim-reader never gets to. \"Enforced\" and \"enforced on 2.7% of the fleet\" are different facts about a rollout, and the first word is the only part of a brief statement everybody actually reads. The fraction is spelled out beside the number, and the at-rollout line says STILL PARTIAL when the destination is one too — a rollout that ends at 312 of 9964 devices is a pilot." },
      { kind: "new", tool: "T05", text: "EVERY GROUP CHIP CARRIES ITS MEMBER COUNT. The documenter pools the counts beside the group names — one read for the whole collection, the pattern the names already used — so the popout, the Markdown, the HTML report and the Word export all say \"SG-Pilot (Included) · 27 members\" from one implementation. The posture tool had been reading these itself since 10479 and was the only tool that could answer the question everybody asks next; the read moved to where the names are." },
      { kind: "improved", tool: "T05", text: "An EMPTY group says 0 members on purpose — that is the Assignment health tool's whole finding, and it belongs on the chip where somebody is looking at the target. A group whose count could not be read prints nothing at all rather than a zero: absent is unknown, never none." },
      { kind: "improved", tool: "T19", text: "Group member counts on every assignment chip, for free — the documenter reads them now, so this tool shows them without asking the tenant anything it was not already asking." },
      { kind: "improved", tool: "T20", text: "The LIST is the default face, reversing the choice made at 10477 on the evidence of a real tenant: Account protection came back with 33 policies, Attack surface reduction with 23, Edge-in-catalog with 23. A wall of thirty-three cards is a scroll where thirty-three rows are a screen. Cards are one click away and unchanged." },
      { kind: "improved", tool: "TUNO", text: "TOOL VERSIONS NOW SAY WHICH CHANNEL, NOT HOW FINISHED. A tool in production is 1.0.x and a beta-only tool is 0.x, so a 1.x on this channel is a bookkeeping bug rather than a compliment. Eighteen production tools were renumbered from 0.N to 1.0.N — the counter carries across so every note stays filed under the version it describes — and the posture tool was corrected DOWNWARDS from 1.3 to 0.13, having carried a 1.x since 10486 while still beta-only. Which tools are in production is read from what main actually contains, never remembered." },
    ],
  },
  {
    build: 10504, date: "2026-08-28", title: "One read per session, not one per screen",
    items: [
      { kind: "fixed", tool: "T20", text: "THE SECURE SCORE NODE ASKED FOR A READING IT ALREADY HAD. With the Secure Score visualizer run and its numbers on screen, opening the posture tool's Secure Score node still offered a button to go and read the tenant — a Graph round trip, and on most tenants a second consent prompt, for an answer sitting in memory. The reading is now cached for the session and both screens share it: the node adopts it on open and shows the matrix straight away." },
      { kind: "improved", tool: "T20", text: "10500 had refused to reuse it on purpose, and the reason was written into the code: T20 would be showing numbers whose age it could not state. That was the wrong conclusion drawn from a real constraint — the fix for an unstateable age is to state it. The node now says how long ago the reading was fetched and at what time, that it is shared with the Secure Score visualizer, and carries one button for a fresh one. A posture re-read still clears the correlation, because the checks it was computed against have changed; opening the node re-adopts." },
      { kind: "improved", tool: "T21", text: "The Secure Score visualizer's own Read button still always goes to the tenant. An explicit click means a fresh read — the cache exists so a second SCREEN does not cost a second read, not so a button can quietly do nothing." },
    ],
  },
  {
    build: 10503, date: "2026-08-28", title: "Every category bar is a filter",
    items: [
      { kind: "new", tool: "T21", text: "THE CATEGORY BARS ARE FILTERS. A bar answers \"how is Apps doing\" and the only next question anybody has is \"which Apps controls\" — a number you cannot click is a number you then have to go and find somewhere else on the page. Click one and the screen goes to that category's improvement actions with ITS OWN SCORE above them: points, gaps, and Microsoft's two comparison figures for that category rather than for the tenant. The rule is the Policy overview's, whose surface stat cards are its filters; this is the same idea on a bar." },
      { kind: "new", tool: "T21", text: "A chip row on the improvement-actions tab carries the same filter, so it is visible and clearable without going back to the Score tab for it — and the list's Category cell narrows too, checked before the row's own open handler so that clicking the category filters while clicking anywhere else on the row opens the control. One piece of state behind all three, because three controls for one filter is how a screen starts disagreeing with itself." },
      { kind: "improved", tool: "T21", text: "Clicking an active filter clears it. A filter you cannot undo where you set it is a trap, and the trap has a second door: a filter carried into a view that empties it — Data has gaps but nothing achieved — names the category that emptied the list and offers the way out inline, because by then its own chip is gone from the row. Chips exist only for categories actually present in the current view; \"Data (0)\" under the achieved list is a control that leads nowhere." },
      { kind: "improved", tool: "T21", text: "The exports stay whole-tenant, and the filtered card says so. A file called Secure Score that quietly held only the Apps category would be the kind of surprise that outlives the session it was downloaded in — so the filter narrows the screen and not the download." },
    ],
  },
  {
    build: 10502, date: "2026-08-27", title: "The catalogue text is HTML, and the cards said so out loud",
    items: [
      { kind: "fixed", tool: "T21", text: "MICROSOFT'S CONTROL TEXT IS HTML AND NOTHING DOCUMENTS IT. description, remediation and remediationImpact come back carrying line-break, bold, paragraph and list tags, anchor links, and curly-quote entities — frequently malformed, with an entity opening inside a tag it does not close. 10500 escaped the string and printed it, which put a run of literal angle brackets and entity codes on the card where a sentence should be. Rendering it as HTML was never the alternative: that hands a third party's markup an injection point in a page holding a tenant token." },
      { kind: "improved", tool: "T21", text: "So the markup is READ AND THROWN AWAY and the text inside it kept. Structure becomes line breaks and bullets, a link keeps its label AND its href — the URL is frequently the actual instruction — entities decode, and the plain text that comes out is escaped on render like every other string. No markup of Microsoft's ever reaches the DOM. It runs once, in the engine, so the screen, the Markdown, the CSV and T20's Secure Score node all read one clean shape rather than four places cleaning it slightly differently." },
      { kind: "fixed", tool: "T21", text: "THE PAGE GREW A HORIZONTAL SCROLLBAR and the left column slid under the sidebar. A grid item's default minimum width is its min-content, and Microsoft's remediation carries portal URLs eighty characters long with nothing to break on — so one card's longest word pushed its track past the 340px the grid asked for, the row past its container, and the page past the viewport. The cards now allow their track to shrink and their text to break; both are needed, either alone still overflows." },
      { kind: "new", tool: "T21", text: "🗂 Cards | ☰ List — the segment T19, T20 and T14 already wear, ported rather than reinvented. Cards lead because Microsoft's assessment is the substance of a control and no fixed table column can hold it; the list is for the tenant with fifty-six of them, where scanning beats reading. Both faces open THE SAME popout, carrying the full unclipped assessment, remediation, impact, threats and portal link — so the list is a denser view of the same information rather than a lossy one, and the card can clamp to five lines and say \"open for the full text\"." },
      { kind: "fixed", tool: "T21", text: "Microsoft's real impact vocabulary. The Graph reference documents low, moderate and high; live tenants answer Low, Medium, High and — on a great many controls — the literal string \"Unknown\". Only three of those were mapped, so Medium and Unknown both fell through to a default nobody had written down. All of them are mapped now, an unknown level weighs as MODERATE by decision rather than by accident, and the card prints Microsoft's own word with what this tool does with it beside it: an unstated impact is not a low one." },
    ],
  },
  {
    build: 10501, date: "2026-08-27", title: "T21 rendered into another tool's screen",
    items: [
      { kind: "fixed", tool: "T21", text: "THE SCREEN WAS BLANK AFTER A SUCCESSFUL READ. T21 shipped owning the element ids ssBody, ssMd and ssProg — ids T10 settings search already owned, and T10's screen comes first in the document. getElementById answers with the FIRST match, so every render wrote into the settings-search screen while T21's own stayed empty. The read worked, the consent worked, the exports worked; the page just showed nothing, which is the worst shape a failure can take because there is no error to follow. The ids are now scBody, scMd and scProg, and the whole tool's prefix moves from ss- to sc- in the markup and the stylesheet together." },
      { kind: "fixed", tool: "T21", text: "The missing 👁 Report button was the same bug wearing a different face: T21's own scMd stayed hidden while T10's export button — invisible on a screen nobody was looking at — was the one being shown and wired up." },
      { kind: "improved", tool: "TUNO", text: "THE HEADLESS SUITE HAD NOT CAUGHT IT BECAUSE IT ASKED THE SAME WRONG QUESTION. The render tests called getElementById(\"ssBody\") exactly as the tool did, both got T10's div, and every assertion passed on content sitting in another tool's screen. A test that shares the bug's assumption is not a test. Two checks close it: a document-wide duplicate-id assertion over index.html — run against 10500's markup it names all three ids — and a render test that reaches through T21's own section element rather than trusting an id lookup. The first one guards every tool, not just this one; a new tool is exactly when an id gets claimed twice." },
    ],
  },
  {
    build: 10500, date: "2026-08-27", title: "The Secure Score, and the gap between policy and the estate (R02)",
    items: [
      { kind: "new", tool: "T21", text: "📊 Secure Score visualizer — R02, the oldest card on the roadmap. The tenant's Microsoft Secure Score over Graph, made explorable: the gauge, the comparison against tenants of a similar size and against all tenants, the per-category breakdown with Microsoft's two comparison figures as TICKS ON EACH BAR rather than numbers in a footnote, the timeline of every reading Graph still holds, and what improved and what regressed across it. Shaped after GCIT's Export-SecureScoreReports by Elliot Munro — that report's anatomy, rebuilt in a browser tab against one signed-in tenant instead of a partner-side PowerShell sweep across GDAP customers." },
      { kind: "new", tool: "T21", text: "THE IMPROVEMENT ACTIONS, CHEAPEST POINTS FIRST — and the ordering is OURS, NOT MICROSOFT'S, which it says wherever it renders. Points still available, weighted down by the userImpact and implementationCost Microsoft publishes on each control. A control that declares neither is treated as MODERATE: an unstated impact is not a low one, and calling it low is how a quick win becomes the change that breaks sign-in on Monday. Microsoft's own stack ranking is beside it as its own sort, unchanged." },
      { kind: "new", tool: "T21", text: "GRAPH KEEPS ABOUT NINETY DAYS, so \"what changed since last month\" runs out at a quarter. The tool exports a versioned JSON snapshot and uploads them back, and the timeline then reaches as far as somebody kept files. The snapshot carries the readings AND the catalogue they were read under, because a control's title and max score change under you and an old point re-labelled with today's profile is a quiet lie. Uploaded points are drawn as their own marks; a same-day clash resolves to the LIVE reading and says how many it dropped; a snapshot from a different tenant is REFUSED BY TENANT ID rather than merged with a caveat — two tenants in one timeline is not a comparison, it is a wrong graph." },
      { kind: "new", tool: "T21", text: "Three numbers that decline to be guessed. Microsoft returns the comparison figures with no stated unit — in practice a percentage — so a value above 100 is rendered as POINTS rather than as an impossible percentage. A control with no readable score or no ceiling in the catalogue is counted as NEITHER achieved nor a gap, and is listed by name. And a tenant with no Secure Score readings at all gets \"the read succeeded and returned nothing\", not a failure — reporting an empty collection as an error sends somebody hunting a permission problem that is not there." },
      { kind: "new", tool: "T20", text: "📊 SECURE SCORE GAPS — a fourth node on the rail, and the one finding neither tool produces alone. These checks read POLICY: what is configured, and whether it reaches anybody. Secure Score reads THE ESTATE: what devices actually report back. They disagree constantly, and the disagreement is the answer — a check that passes while Microsoft still withholds the points is almost never the policy: it is machines that never onboarded to Defender for Endpoint, a licence the control needs, or devices that have not reported since the policy was written. That bucket leads the screen, because the work is already done and is not being counted." },
      { kind: "new", tool: "T20", text: "Four buckets, and the two least comfortable ones are kept rather than hidden. Both readings agree it is open. Configured here and still unscored there. MICROSOFT SCORES IT AND NO CHECK COVERS IT — which is a gap in the check set, listed under Microsoft's own title and remediation instead of quietly falling off the screen. And findings Microsoft does not score at all, which are still real: App Control running in audit mode blocks nothing whatever the score says." },
      { kind: "new", tool: "T20", text: "THE PAIRINGS ARE FALSIFIABLE ON SIGHT. A check is tied to a Secure Score control by an explicit match on Microsoft's own control id or its published title, and every correlated row PRINTS BOTH NAMES — the check's and the control's — so a wrong match is visible to a reader rather than load-bearing under one. Control ids on this surface are provider strings Microsoft renames, so a miss is designed to be harmless: an unmatched control is listed under Microsoft's title and an unmatched finding under ours. Nothing is dropped by failing to match; the only thing that must never happen is a wrong pair." },
      { kind: "improved", tool: "T20", text: "The Secure Score node is a BUTTON, not part of the run. It costs SecurityEvents.Read.All and the rest of the tool costs no new permission — folding it into the read would have quietly made that sentence false on the tile, the screen header and the roadmap card at once. The read is T21's own SecureScore.collect(), so the two tools cannot disagree about one tenant (the T05 rule on a second surface), and it is asked fresh at this click rather than borrowed from a T21 screen whose result age this node could not state. A new posture read clears the correlation instead of pairing new checks with an old score." },
      { kind: "improved", tool: "T20", text: "The impact brief keeps its contract. It is END-USER language — a communications draft — and Secure Score gaps are an administrator's document that would be noise in a rollout mail, so they ride at the very end as APPENDIX B, behind a line telling you to cut it before sending. Markdown, Word and the on-screen report all carry it, and a brief written before anybody read the score carries no such section at all rather than an empty one implying nothing was found." },
    ],
  },
  {
    build: 10499, date: "2026-08-27", title: "The working session opens the repo instead of mailing patches to it",
    items: [
      { kind: "improved", tool: "TUNO", text: "No product behaviour changes in this build \u2014 it is a working-rules change, recorded here because every commit on this branch is a build. CLAUDE.md now says a session that can reach the repo COMMITS IN IT, and asks for the folder before writing a line rather than after, when the work is already stranded in a throwaway clone. The whole patch protocol existed to survive not having the repo, and every failure it documents \u2014 build numbers taken twice, a stale rebase-apply, an mbox re-cut from a base that had moved \u2014 is a failure it caused. It stays as the fallback, with the new rule at the top: a handover is cut from the APPLIED TIP, never re-cut from the base the last one used." },
      { kind: "improved", tool: "TUNO", text: "And a note that commands handed over are typed into PowerShell, where ^ { } are live syntax: git rev-parse HEAD^{tree} does not fail there, it answers the PARENT COMMIT'S SHA and reads exactly like the tree hash that was asked for. Quote every rev-spec. A verification command that silently answers a different question is worse than one that errors." },
    ],
  },
  {
    build: 10498, date: "2026-08-27", title: "How many devices a filter actually matches (R32)",
    items: [
      { kind: "new", tool: "T14", text: "\ud83d\udcdf Count devices \u2014 new js/filterrules.js parses the Intune filter rule grammar and evaluates it against the enrolled device inventory, so a filter stops being an unknown and starts being a number: \"312 of 9969\". One read, trimmed by $select to the fields the grammar can reach. Every tool in the house has been saying \"at most\", \"may reach it\", \"\u2691 filter \u2014 may\" since filters were understood at all; this is the first thing that answers." },
      { kind: "new", tool: "T14", text: "A COUNT IS OFFERED ONLY WHERE THE WHOLE RULE IS UNDERSTOOD. Not best-effort with a confidence marker \u2014 a wrong count renders exactly as confidently as a right one, which is the failure mode 10483 through 10496 were spent removing. A rule outside the grammar says \"not evaluated\" and NAMES what stopped it: deviceOwnership because Personal/Corporate and personal/company would need a guessed translation, operatingSystemSKU because Graph does not return it, app.* because it is not about devices. Seven device properties are evaluated; the rest declare themselves." },
      { kind: "improved", tool: "T14", text: "The grammar is taken from learn.microsoft.com's own reference rather than from what one tenant happened to contain \u2014 and, or, not, parentheses, -eq -ne -startsWith -contains -notContains -in -notIn, plus -gt -lt -ge -le on version properties compared segment by segment so 10.0.19045 sorts below 10.0.22631 rather than lexically. Matching is case-insensitive; a device missing the property does not match a positive test and does match a negative one." },
      { kind: "improved", tool: "T14", text: "Three states, because they are three different things: not counted, counted, and not evaluated. A device read that fails leaves the column unknown rather than zero, and the note says the number is a reading of today \u2014 the service evaluates the rule at assignment time against inventory that moves." },
    ],
  },
  {
    build: 10497, date: "2026-08-27", title: "Assignment filters get a face, and a search box",
    items: [
      { kind: "new", tool: "T14", text: "\ud83d\uddc2 Cards | \u2630 List \u2014 Option B of the mockup round, Mihai's pick. Cards lead because a filter is a bounded object whose RULE is the thing a fixed table column could never hold: it was cut at 160 characters with the only way to read the rest being to open the edit form. On a card the whole rule just sits there. The list face keeps the dense view, and the reference chips are one implementation shared by both so they cannot drift." },
      { kind: "new", tool: "T14", text: "A search box, over the name, the description, the platform AND THE RULE \u2014 the one tool without one, while T05, T19 and T20 all had it. \"Which filter mentions weuavd\" is the question a tenant with fourteen of these actually gets asked, and the answer was scrolling." },
      { kind: "new", tool: "T14", text: "A Devices cell on both faces, saying \"not evaluated\". Counting the devices a rule matches is R32 and is not read yet \u2014 the cell exists and says which it is, because \"we did not look\" and \"none\" are different answers and only one of them is dangerous." },
    ],
  },
  {
    build: 10496, date: "2026-08-27", title: "The refusal names the filter, and the claims match the code",
    items: [
      { kind: "fixed", tool: "T11", text: "10494's refusal \u2014 whose entire job is telling an operator WHICH two filters differ \u2014 printed an eight-character id prefix, while the display names sat loaded in the dropdown on the same screen. They are handed to the planner now, so the message names both." },
      { kind: "fixed", tool: "T08", text: "The on-screen Lost table said only \"no remaining assignment reaches the subject\" where the Markdown already said the loss was conditional and named the filter \u2014 the screen and the export disagreeing about the same row. A filter name containing a pipe also broke the comparison table: it was the one cell on that line not passed through mdCell. And the CSV column headed Conditional now says Filter, which is what it holds." },
      { kind: "improved", tool: "T13", text: "\"A filter narrows an EXCLUSION\" gets its own chip in compliance coverage and \ud83d\udee1 Endpoint security. 10490 computed the fact and showed it nowhere, which is only half of not folding it into the wrong caveat \u2014 fewer devices are kept out than the exclusion suggests, and that is worth saying rather than merely worth not mis-saying." },
      { kind: "improved", tool: "T05", text: "assignmentOf() reads its filter through Docs.filterOfTarget like everything else, so the claim that there is one parse of that field is true of the file making it." },
    ],
  },
  {
    build: 10495, date: "2026-08-27", title: "The payload read, and the counts that hang off it",
    items: [
      { kind: "fixed", tool: "T14", text: "10491's used-by read was broken in the one way demo mode cannot show. payloads is a STRUCTURAL property of an assignment filter, not a relationship \u2014 the resource lists Relationships: None \u2014 so $select=<fields>&$expand=payloads projects the property away and the expand has nothing to attach. On a live tenant every filter would have reported a confident 0 references, on the screen whose delete gate exists precisely because a wrong zero there widens live assignments; and \ud83c\udf9b Assignment editor, which calls the same list inside a bare catch to fill its filter dropdown, would have silently lost the ability to attach a filter at all. The demo router discards the query string and every fixture carries payloads by hand, so the mock looked perfect. payloads now rides in the $select, which is the shape the delete gate's own read had all along." },
      { kind: "fixed", tool: "T14", text: "The Markdown export stated two different totals for one filter \u2014 the table counted payloads, the section heading below it counted sweep rows \u2014 directly under a note saying the counts were unaffected by unread surfaces. The heading counts payloads too, and says how many of them the scan managed to name." },
      { kind: "fixed", tool: "T14", text: "A newly created filter references nothing, so the only-used view hid the row whose success message was still on screen. The view drops back after a write." },
      { kind: "fixed", tool: "T02", text: "Filter ids are matched case-insensitively on both sides of the name lookup. 10490 normalised ids to lowercase in the sweep and left the resolver keyed on the raw id, so a tenant returning mixed-case filter ids would have shown \u201cfilter 3f2a1b9c\u2026\u201d everywhere instead of a name." },
    ],
  },
  {
    build: 10494, date: "2026-08-27", title: "A filter is part of an assignment's identity",
    items: [
      { kind: "fixed", tool: "T11", text: "The Assignment editor's noop check compared the target type and the group id and nothing else, so a filter was invisible to the one check standing between an operator and a write. \"Add an include for SG-Pilot\" reported ALREADY ASSIGNED against an existing include for SG-Pilot narrowed by a filter \u2014 two assignments that reach different machines, called identical \u2014 and adding a filter to a group already targeted without one was equally a noop, so the filter never landed. A write path is the worst place in the tool for a silent nothing." },
      { kind: "improved", tool: "T11", text: "Same group, different filter is now REFUSED and says which is which: Graph holds one include per group, so replacing the filter is an edit rather than an add, and the tool reports it instead of performing it. That is the same rule the file already keeps for removals \u2014 \"remove just the filter\" would be an edit wearing a removal's name." },
    ],
  },
  {
    build: 10493, date: "2026-08-27", title: "Two tools stop paying for an answer and throwing it away",
    items: [
      { kind: "fixed", tool: "T08", text: "\ud83d\udd2e What-if resolved filter names on every run and then discarded them \u2014 only a boolean reached the screen, so the answer was \"may apply\" with no way to find out which filter decides. The names now travel with the effective state: \"may apply \u2014 \u2691 PVM-DG-CORP-FILTER-AVD-ALL (include) decides\", on the gained and lost tables, the Markdown and a real CSV column in place of the word \"filtered\"." },
      { kind: "fixed", tool: "T09", text: "\ud83e\ude7a Assignment health did the same: resolveFilters() over every row, then nothing read filterName, filterMode or filterId in the findings, the Markdown or the CSV. An empty-group finding on a FILTERED assignment describes reach the filter may already have removed and said nothing about it. Findings now name the filter, the Markdown gains a Filter column and the CSV gains Filter and FilterMode." },
    ],
  },
  {
    build: 10492, date: "2026-08-27", title: "The documenter's list says who a policy reaches",
    items: [
      { kind: "fixed", tool: "T05", text: "A browse row said \"2 assignments\" \u2014 a number, on the screen whose whole job is writing down what a tenant is configured to do, while collect() had already resolved every group name and every filter name onto the object. Rows now name them: SG-Pilot (Included), All devices \u2014 \u2691 PVM-DG-CORP-FILTER-AVD-ALL (include). Two chips then a +N, so a policy on eleven groups does not eat the row, with the rest in the tooltip and all of them in the popout." },
      { kind: "improved", tool: "T05", text: "The search reaches the assignment. \"Which policies hit SG-Pilot\" and \"what does this filter touch\" are the two questions the box was asked and could not answer \u2014 T19's search had reached group names since it shipped and T05's, over the same collection, had not." },
      { kind: "fixed", tool: "T05", text: "The search read as MISSING rather than waiting. It is disabled until a read finishes, with the reason in a paragraph below it \u2014 which is not where the eye is. The control now says it itself: \"Read the configuration first \u2014 then search here\", becoming \"e.g. BitLocker, SG-Pilot, or a filter name\" once there is something to search." },
    ],
  },
  {
    build: 10491, date: "2026-08-27", title: "Used by is Graph's own answer, not a scan away",
    items: [
      { kind: "fixed", tool: "T14", text: "The used-by column sat empty behind an opt-in nine-surface sweep when the answer had been one $expand away the whole time. A filter arrives with its PAYLOADS \u2014 Graph's own words for the property are \"associated assignments for a specific filter\" \u2014 carrying the policy id, the surface type, the targeted group and the filter mode for every reference. The count is now filled by the first read, always, and a 0 means Graph said none rather than nobody having looked." },
      { kind: "improved", tool: "T14", text: "A reference row names the GROUP it targets. It had shown the raw sweep tokens \u2014 policy \u00b7 surface \u00b7 all-devices \u00b7 include \u2014 with no group at all; the payload carries the group id and it is resolved once for the whole list, with the id left visible where the directory read did not answer." },
      { kind: "improved", tool: "T14", text: "The usage scan's job shrinks to what only it can do: turning payload ids into policy NAMES. Its failure note moved with it \u2014 unread surfaces no longer make the counts a floor, because the counts no longer come from the sweep. And after a write the reference counts stay, since they are re-read with the filters; only the names go absent." },
      { kind: "fixed", tool: "T14", text: "The scan's progress bar was indeterminate while the sweep knew exactly where it was \u2014 prog() took a message and dropped the n and of that GroupUse's onStatus supplies. And a rule longer than 160 characters was cut silently in the Markdown export; it now says it was cut, as the screen already did." },
    ],
  },
  {
    build: 10490, date: "2026-08-27", title: "One reader for the assignment filter, and the right permission for it",
    items: [
      { kind: "fixed", tool: "T05", text: "The assignment-filters surface had asked for the WRONG PERMISSION since it was written \u2014 DeviceManagementRBAC.Read.All, where learn.microsoft.com's List deviceAndAppManagementAssignmentFilters names DeviceManagementConfiguration.Read.All. 10482 built the filter-naming read on top of that mistake and 10488 added it to two more scope unions, so on a tenant granting config and not RBAC the names 403'd and the changelog stated the wrong fix. \ud83e\udde9 Assignment filters had it right all along, which is precisely how the two came to disagree." },
      { kind: "fixed", tool: "T02", text: "The Group Analyzer keyed filter detection off the filter TYPE rather than its ID, so an assignment carrying a filter id with a missing or \"none\" type had no filter at all in T02, T06, T08, T09 and T14 \u2014 and a filter in T05, T12, T19 and T20. Five tools on one side of the split, four on the other, describing the same assignment differently. Docs.filterOfTarget() is now the one reader, keyed on the id; a mode that is absent is defaulted to include and SAYS it was assumed." },
      { kind: "fixed", tool: "T13", text: "Compliance coverage, \ud83d\udee1 Endpoint security and the \u2694 Conflict finder all flagged a policy as filtered when the filter sat on an EXCLUSION \u2014 where it narrows what is kept out, not what is reached. The \"may reach, not does\" caveat those tools print does not describe that case. Filtered now means reach is capped; a filter on an exclusion is carried as its own fact rather than folded into the wrong caveat." },
      { kind: "fixed", tool: "T02", text: "The filter-name splice hunted for \"filter: <word>\" in a detail string and silently did nothing when it was not there \u2014 which was exactly the case a missing mode produced. Built from the parts instead, so a resolved name always lands." },
    ],
  },
  {
    build: 10489, date: "2026-08-27", title: "The documenter writes an assignment one way, and the filter is in it",
    items: [
      { kind: "fixed", tool: "T05", text: "A filtered assignment lost its filter on EVERY surface the documenter writes \u2014 the popout, the Markdown, the HTML report and the Word export all printed name (kind) and stopped. So a policy targeted at All devices through PVM-DG-CORP-FILTER-AVD-ALL circulated as a claim of whole-fleet reach in the document an auditor reads. The filter had been resolved onto the assignment since 10482 and not one writer read it. All four now route through Docs.assignmentText(), so they cannot drift apart again." },
      { kind: "fixed", tool: "T05", text: "\"All devices \u00b7 All devices\" \u2014 the tenant-wide chip said its name and its kind, which for a tenant-wide target are the same word. assignmentOf sets name === kind there and all four writers concatenated both; T19 and T20 had dodged it with name || kind and the documenter never got the fix. The kind is now dropped when it only repeats the name." },
      { kind: "improved", tool: "T19", text: "The shared popout is T19's and T20's too, so both gained the named filter and lost the duplicate in the same change \u2014 which is the whole argument for the template existing once." },
    ],
  },
  {
    build: 10488, date: "2026-08-27", title: "The review round \u2014 five ways the new numbers could still lie",
    items: [
      { kind: "fixed", tool: "T20", text: "The count unwrap had 10479's bug hiding in a second shape. The pool worker catches its OWN failures and returns null, so a 403'd group arrived as { value: null } rather than { error } \u2014 and Number(null) is 0. A refused group would have contributed a confident zero to the sum with no \"the sum is a floor\" caveat raised anywhere. Absent is now unknown BEFORE it is a number, and both failure shapes are counted." },
      { kind: "fixed", tool: "T20", text: "A policy both FILTERED and carrying an unreadable group count claimed \"at most N\" in the brief \u2014 an unsound ceiling the real reach can exceed, because the missing groups push the number up while the filter pushes it down. The findings line had already refused this case; the brief's two lines now refuse it the same way instead of picking the bound that reads better." },
      { kind: "fixed", tool: "T20", text: "A staged policy assigned ONLY through exclusions was briefed as \"carries no assignment yet\" \u2014 reporting a configured contradiction as an empty field waiting to be filled in. Excluded-only is its own verdict here as everywhere else (T09's distinction), and the line says what it is." },
      { kind: "fixed", tool: "T05", text: "Naming assignment filters (10482) is an RBAC-scoped read, and the documenter and \u2694 Conflict finder never asked for that scope \u2014 so a tenant with one filtered assignment reached for a token mid-read with no user gesture behind it: a consent popup the browser blocks, blamed on filter naming, after the tool had already reported permissions fine. Both unions now include it." },
      { kind: "fixed", tool: "T20", text: "A reach line whose sentence says there is no sum no longer appends \"the sum is a floor\" after it; a sum larger than the fleet says why it can be (overlapping groups, or groups of users rather than devices); and the card's not-computable cell no longer renders \"of null\" when the fleet count itself could not be read." },
      { kind: "improved", tool: "T20", text: "The brief's at-rollout line leads with the number. \"Where is this going\" is the question a rollout communication exists to answer, and burying the fleet total behind its caveat left the brief with no destination figure at all: it now reads \"at rollout: all 9969 enrolled Windows devices in total \u2014 the staged policy carries NO assignment yet, so the fleet total is the intention, not a reading\"." },
    ],
  },
  {
    build: 10487, date: "2026-08-27", title: "The count unwrap becomes something a test can reach",
    items: [
      { kind: "improved", tool: "T20", text: "The group-count unwrap moves out of run() and into EndpointPosture.countsFrom(), a named seam in the DOM-free half. The 10483 bug is the argument: it threw nothing, logged nothing and rendered a confident \"~0 of 9969\" for four builds, because the one line that could be wrong lived where no headless test could call it. A wrong number that renders is worse than an exception \u2014 the defence is a test, and a test needs a seam." },
      { kind: "improved", tool: "T19", text: "_setForTest() resets the view the way a fresh read does, so the headless suite exercises the same reset the tool performs rather than a seam that quietly differs from it." },
    ],
  },
  {
    build: 10486, date: "2026-08-27", title: "The brief says what is enforced now AND where it is going",
    items: [
      { kind: "new", tool: "T20", text: "Every brief statement now carries two device lines instead of one: \ud83d\udcdf enforced now on ~5000 of 9969 enrolled Windows devices, and \ud83c\udfaf at rollout: targets All devices \u2014 all 9969 enrolled Windows devices in total. Today's coverage and the destination are the two numbers a rollout communication is actually about, and the brief had only ever printed the first." },
      { kind: "fixed", tool: "T20", text: "The at-rollout number is READ, not assumed. It said \"at rollout: all N enrolled Windows devices\" about every staged policy, which is an assumption wearing a figure \u2014 a staged policy targeting two pilot groups does not end up fleet-wide. The line is now the same arithmetic run over the statement's own not-yet-assigned policies, and a staged policy with no assignment at all says exactly that rather than borrowing the fleet total." },
      { kind: "improved", tool: "T20", text: "The wording moved with it \u2014 \"applies to\" became \"enforced now on\", so the two lines cannot be misread as one repeated. Pane, Markdown and Word exports all speak both." },
    ],
  },
  {
    build: 10485, date: "2026-08-27", title: "Policy overview gets its list face",
    items: [
      { kind: "new", tool: "T19", text: "\ud83d\uddc2 Cards | \u2630 List \u2014 T20's own segment, ported rather than reinvented, because two tools showing the same policies must offer the same two faces or the eye has to relearn the screen. Cards stay the default (a tenant you do not know is a scan, not a lookup); the table is the house .cg-table with Policy, Surface, Included, Reach, Platform, Settings and Verdict, and a row click opens the same documenter popout a card click does. The choice survives filtering and typing, and resets on a fresh read." },
      { kind: "improved", tool: "T19", text: "The seg sits in the static toolbar beside the search box, for the reason the search box is there: a control that gets re-rendered under a click is a control that loses clicks. The grid class comes off the host in list mode \u2014 a table dropped into a card grid renders as one very narrow grid item." },
    ],
  },
  {
    build: 10484, date: "2026-08-27", title: "A filtered assignment states a ceiling, not a count",
    items: [
      { kind: "fixed", tool: "T20", text: "A policy targeted at All devices WITH an assignment filter was reported as reaching the whole fleet \u2014 \"applies to all 9969 enrolled Windows devices\" \u2014 when the filter is the entire point of that assignment and narrows it to a fraction. A filter only ever narrows: include mode keeps what the rule matches, exclude mode drops it, and either way the number a browser can compute is a CEILING. Filtered reach now says \"at most\" and names the filter that caps it, in the reach cells, the findings and the brief alike." },
      { kind: "improved", tool: "T20", text: "The missing side flips with it. At most N reached means AT LEAST the remainder missed \u2014 the old line put a firm number on the side that could only grow. And a policy that is both filtered and carrying an unreadable group count is bounded on neither side; it says so rather than picking whichever bound reads better." },
      { kind: "improved", tool: "T20", text: "The brief's \u2691 chip names the filter and its mode instead of saying \"filtered \u2014 some devices\", which told a communications reader that some machines are left out and gave them no way to find out which." },
    ],
  },
  {
    build: 10483, date: "2026-08-27", title: "The group member counts actually land",
    items: [
      { kind: "fixed", tool: "T20", text: "Every device number on the screen was wrong, and wrong in the direction that hides a problem: \"~0 of 9969 devices \u00b7 9969 still missing (floor)\" about policies whose groups Graph had answered for perfectly well. Graph.pool hands back { item, value }, and the 10479 code read the WRAPPER as the count \u2014 so Number.isFinite was false every single time, every group landed as unknown, and the sum of nothing rendered as a measured zero wearing a footnote. Caught on the first tenant big enough to notice. The value is now unwrapped; the cards, the list rows, the findings and the brief all count again." },
      { kind: "fixed", tool: "T20", text: "A sum with NO readable count in it is no longer a number. It said \"~0\" with a (floor) note beside it, and a reader takes ~0 as a measurement and the note as a footnote \u2014 the exact shape of an unmeasured claim. It now says what it is: \"4 groups targeted \u2014 not one member count could be read, so how many devices this reaches is UNKNOWN, not zero (the fleet is 9969)\"." },
      { kind: "improved", tool: "T20", text: "A PARTIAL floor counts up rather than down: \"at least 5000 of 9969 targeted \u00b7 at most 4969 not targeted\". The old wording put ~ on a number that could only grow and a firm figure on the missing side that could only shrink \u2014 both leaning the wrong way." },
    ],
  },
  {
    build: 10482, date: "2026-08-27", title: "An assignment filter says its name",
    items: [
      { kind: "fixed", tool: "T19", text: "A policy assigned through a filter showed \"\u2691 filter \u2014 may\" and nothing else \u2014 the tool knew a filter narrowed the assignment and refused to say which one, while the portal shows the name right beside the group. The documenter now names assignment filters in the same pass it names groups (one read of /deviceManagement/assignmentFilters for the whole collection), and the reach cell wears the name with its mode: \u2691 PVM-DG-CORP-FILTER-AVD-ALL (include) \u2014 may. The mode travels with the name on purpose: a reader who sees only the name cannot tell which way the filter cut." },
      { kind: "fixed", tool: "T20", text: "The same naming, on the cards, the list rows and everywhere reach is stated. T20's scope union gains the assignment-filter read (DeviceManagementRBAC.Read.All, already asked for by the documenter's own filters surface) \u2014 without it every filtered assignment would have printed a GUID." },
      { kind: "improved", tool: "T05", text: "Docs.filterLabel() and Docs.filtersOf() are the one way a filter is written down, so the two tools cannot drift into two spellings. A filter whose name could not be read says so and keeps its id \u2014 a blank would be a claim that there is no filter, and there is one." },
    ],
  },
  {
    build: 10481, date: "2026-08-27", title: "Audit mode is read from the policy, and the reach cells count devices",
    items: [
      { kind: "fixed", tool: "T20", text: "The brief claimed 'only approved software runs — installing arbitrary software no longer possible' for App Control policies running in AUDIT mode — the wrong-direction error, caught on the first live tenant: the OIB WDAC policies say Enabled:Audit Mode in their XML, and an audit policy blocks nothing. The mode is now read from the policy content — the documenter stamps an audit flag from the RAW setting value, because the display row loses the word to tail-shortening and the 300-character cap — and App Control speaks three ways: an enforcing policy claims blocking, an auditing one says 'inventorying, not blocking yet — nothing changes for you today', and unreadable content says unknown, verify in the portal. The best-practice check moves the same way: audit-only coverage is a finding that names itself, never a silent green." },
      { kind: "improved", tool: "T20", text: "The reach cell on every policy card and list row carries its device numbers: '4 groups · ~5000 of 9969 devices · 4969 still missing' — the target groups' member total against the fleet, the same arithmetic as the findings, per policy. Tenant-wide says 'all N devices'; an unreadable group count marks the sum a floor." },
      { kind: "improved", tool: "T20", text: "The brief's at-rollout statements state the destination: 'at rollout: all N enrolled Windows devices' — today's interim coverage and the rollout's full fleet, side by side. And a retired interim policy — (TO-BE-REMOVED) but no longer assigned — is dropped from the brief entirely: not today, not the plan, mentioned nowhere." },
    ],
  },
  {
    build: 10480, date: "2026-08-27", title: "The interim policies say they are interim, and the brief counts its devices",
    items: [
      { kind: "new", tool: "T20", text: "(TO-BE-REMOVED) is understood as what it is in this tenant: in place now, phased out at rollout. The impact brief wears it — ⏳ interim chips on the affected statements, 'staged replacement takes over' when a permanent policy stands ready, and a new WHAT STOPS AT ROLLOUT section when nothing is staged, because a protection quietly disappearing is exactly what a rollout communication exists to say out loud. The appendix states 'enforced now — interim, retired at rollout' per policy, in the pane, the Markdown and the Word export alike." },
      { kind: "new", tool: "T20", text: "A best-practice PASS whose every reaching policy is interim is now PASS — INTERIM ONLY, counted as a finding: it is true today and expires at rollout. The verdict names the staged replacement to assign, or says plainly that the gap is coming. One override in one place, so all eighteen checks speak it identically." },
      { kind: "improved", tool: "T20", text: "The brief's 'Already enforced today' statements carry their 📟 device count — the target group's members against the whole Windows fleet, with the not-yet-targeted remainder. The findings' arithmetic, spoken shorter, same honesty: targets, not check-ins." },
    ],
  },
  {
    build: 10479, date: "2026-08-27", title: "Every finding says how many devices it is about",
    items: [
      { kind: "new", tool: "T20", text: "📟 Devices on every best-practice check, on screen and in the export: how many enrolled Windows devices the check's policies actually target, and how many the tenant leaves out. A GAP now reads '0 of N — all N are missing this control' — T16's denominator idea carried into every check, because a gap is not an empty row, it is that many machines on local defaults. The arithmetic is assignment reach and says so on the line: tenant-wide is the Windows fleet, groups are summed by member count, overlaps are not deduplicated, exclusions are not subtracted, a filter caps reach at may, and an unreadable group count makes the sum a floor — targets, not check-ins." },
      { kind: "fixed", tool: "T20", text: "The rail pane's cards rendered their text flush against the card border — the 10395 disease in a new container shape: the shared padding rule pads direct children of *Body containers, and the rail pane sits one level deeper. The pane joins the convention's named exceptions, and stacked cards get the standard gap." },
    ],
  },
  {
    build: 10478, date: "2026-08-27", title: "The impact brief is readable before it is a file",
    items: [
      { kind: "new", tool: "T20", text: "👁 Read the full brief — the finished communication renders on screen: intro, every statement, the what-if-you-are-blocked section and the appendix, exactly as the Markdown export writes it, with Copy Markdown and Download in the viewer. A document you hand to a communications team should be readable before it is a file in your Downloads folder." },
      { kind: "new", tool: "All tools", text: "The Markdown report viewer arrives — the sister tool ENCA's, ported as one shared implementation (js/report.js): a deliberately small renderer for the subset the reports emit, everything escaped before inline markup so a policy name containing angle brackets can never become markup, and in-app tool links resolved through the tile's own click handler. The .md-view styles have sat in the stylesheet since the scaffold waiting for exactly this; any tool with a Markdown export can now show it first." },
    ],
  },
  {
    build: 10477, date: "2026-08-27", title: "The posture toolbar becomes a card, and the policies learn to be a table",
    items: [
      { kind: "improved", tool: "T20", text: "The filter bar on a discipline node is a card like everything else on the page — the bare input floating on the background read as unfinished, and was. It carries the count and a new 🗂 Cards | ☰ List toggle: cards stay the default because they read better, and the list is the same policies as one table row each — Policy, Template, Reach, Platform, Settings, Verdict — for the node where seventeen cards scroll and one table scans. A row click opens the same popout a card click does, the choice sticks while you move between nodes, and a re-read resets it like every other filter." },
    ],
  },
  {
    build: 10476, date: "2026-08-27", title: "The Endpoint security blade gets a mirror, a translator and an examiner",
    items: [
      { kind: "new", tool: "T20", text: "🧭 Endpoint security posture (R31, Option B of a two-option mockup round): the portal's Endpoint security blade as a rail — every discipline's policies as the Policy overview's clickable cards, opening the documenter's own popout — plus two nodes the portal does not have: plain settings-catalog policies that configure Defender (MDE) or Microsoft Edge, found by their setting definition ids. Legacy intents are classified through their template and say only assigned-or-not, because that is all their surface says. Reads only, no new permission — the read is the documenter's own." },
      { kind: "new", tool: "T20", text: "🗣 The impact brief — the sister tool ENCA's T32, translated from sign-ins to devices: what a person will notice, in end-user language meant for the rollout mail, every statement derived from a policy actually present and naming it. Enforced-today and at-rollout are never mixed, because the split is reach by construction — a policy reaching nobody describes the plan, not today. Export as Markdown or Word." },
      { kind: "new", tool: "T20", text: "🎓 Best practice — the tenant's actual setting values measured against learn.microsoft.com: tamper protection, real-time and cloud protection, PUA, network protection, the three standard-protection ASR rules, silent BitLocker's exact trio, firewall profiles and inbound defaults, EDR onboarding and sample sharing, Edge SmartScreen with its override prevention. Each check carries severity, what Microsoft recommends, what this tenant has, the remediation and the Learn page it stands on. NOT REACHING is its own verdict — the right setting in a policy reaching nobody is configured, not enforced — and a value the check set does not recognise is said so, never guessed into a verdict." },
      { kind: "improved", tool: "T05", text: "The documenter's doc shape carries templateFamily and templateName — read from data the list request already returned, so no read changed. It is how the new tool tells the disciplines apart, and the popout subtitle can now name the template." },
    ],
  },
  {
    build: 10475, date: "2026-08-27", title: "The Device analyzer's settings popout reads the right Graph version",
    items: [
      { kind: "fixed", tool: "T06", text: "Opening a Settings catalog or ADMX policy from the table failed with Resource not found for the segment — those two surfaces exist only on the beta Graph endpoint, and the on-click settings read was going to v1.0 while the list it came from was read on beta all along. Device configuration and compliance opens worked, which is what made the failure look selective. The read now uses the same beta-and-retry options as the Documenter, whose readers it already shared for redaction — and the collection kinds page, so a catalog policy with hundreds of settings arrives whole instead of one truncated response." },
    ],
  },
  {
    build: 10474, date: "2026-08-27", title: "The Device analyzer says each policy once",
    items: [
      { kind: "fixed", tool: "T06", text: "A policy included by All Devices and excluded through a group appeared TWICE, each row telling half the story and both stamped with the whole verdict — which read as a duplicate and was worse than one: the row wearing Excluded next to All Devices, including this one was the story backwards. The verdict was always computed per policy; the table now is too. One row, every relationship stacked in the Why column with the exclusion leading when it decided, and the rule said once: an exclusion beats an inclusion, the include above does not land." },
      { kind: "improved", tool: "T06", text: "Screen, Markdown and the HTML report share one implementation of the Why lines, so the three cannot drift; conflicts carry their own sentence about device-against-user targeting. The CSV deliberately keeps one line per assignment — it is the evidence export, and its Assignment and Via columns are per-row by design." },
    ],
  },
  {
    build: 10473, date: "2026-08-27", title: "The tiles stop saying NEW and BETA about shipped work",
    items: [
      { kind: "fixed", tool: "All tools", text: "Eighteen home tiles wore NEW and BETA while their tools ran in production — the roadmap had this exact disease and build 10467 cured it there; the tiles kept it. Once a tool is in production it is live, whichever channel you read the tiles from. NEW and BETA now belong only to a tool that is not in production yet — one tile today, the Policy overview." },
      { kind: "improved", tool: "All tools", text: "A tool a pending promotion-queue item changed wears UPDATED — the Device analyzer, Intune RBAC and the Configuration documenter today — so the label answers the question the tile stamp raises: this version number moved, what happened. Cleared as items leave the queue at promotion. The writes-to-the-tenant chip is a property, not a status, and stays put. This build exists because the Device analyzer changed twice in one day and its tile said nothing but a stale NEW." },
    ],
  },
  {
    build: 10472, date: "2026-08-27", title: "The Device analyzer suggests as you type",
    items: [
      { kind: "improved", tool: "T06", text: "The search box type-aheads from the tenant: devices and primary users together, four of each, every row saying which it is. Typing mihai now offers the person and their machines instead of silence. Picking a user fills the UPN, since two people can share a display name and a UPN is still readable; picking a device fills its name. Built on the shared Suggest component every other name box already uses, so the consent rule holds: nothing is read on a keystroke until the enable row is clicked, and that row now names both scopes the suggestions will use." },
    ],
  },
  {
    build: 10471, date: "2026-08-27", title: "Intune RBAC: the role opens to what it allows",
    items: [
      { kind: "improved", tool: "T07", text: "Every role head carries a permissions button: the actions the role allows, grouped the way the portal groups them, on one page instead of a blade per category. Read on the click from the role definition itself — the run deliberately leaves the permission sets unread, since they are large and most readers never ask — cached per role until the next run, with the raw Graph action name on hover. The rare denied actions are shown as denials rather than silently subtracted, and the sentence that matters is stated: Intune RBAC is an allow list, so anything not named is not granted." },
    ],
  },
  {
    build: 10470, date: "2026-08-27", title: "Intune RBAC: a group member answers who is in it",
    items: [
      { kind: "improved", tool: "T07", text: "A role member that is a group now carries a members button: who is IN it, read live on the click and shown in the same modal the other tools use. Transitive, so nested groups are flattened to the people who effectively hold the role; users only, since a device or service principal in the group holds no Intune RBAC; capped at 500 with the true total shown, disabled accounts tagged. The report itself keeps showing the assignment as written — group as group — because whoever can change that group's membership can grant the role without holding it, and that fact should not be flattened away." },
      { kind: "improved", tool: "T07", text: "No new permission, and nothing extra during the run: the read happens only for the group you ask about, at the moment you ask, under the directory scopes the tool already brings in. The answer is cached per group until the next run, so a second look is instant. ENCA's per-group member scan, ported — the read, the cap and the modal." },
    ],
  },
  {
    build: 10469, date: "2026-08-27", title: "Every tool header wears its number and version",
    items: [
      { kind: "improved", tool: "All tools", text: "The T-number and version stamp that rides every home tile now sits on the tool's own header card too — where somebody actually is when they wonder what changed. ENCA's implementation, ported: the same pill, hover for the tool's full release note, and the class had been waiting unused in the stylesheet since the scaffold. Stamped on all nineteen tool screens, and re-stamped automatically if a tool ever re-renders its header." },
    ],
  },
  {
    build: 10468, date: "2026-08-27", title: "The Device analyzer finds the machine from its user",
    items: [
      { kind: "improved", tool: "T06", text: "The search box also takes the PRIMARY USER, because the ticket names the person far more often than the serial: a term with an @ is tried as a UPN before the device filters, an exact display name matches in the inventory scan, and a GUID gets a third try as the user's object id after the two device ids. No new permission — the Intune enrolment record already names its user, so nothing reads the directory to answer this." },
      { kind: "improved", tool: "T06", text: "More than one match is now an answer instead of an error. A user with a laptop and a phone is the normal case, so every multi-match — by user, name, serial or a shared Entra device id — renders as clickable device cards carrying the primary user, compliance and last check-in, and clicking one runs the analysis on that machine. The old dead end, matching three devices and being told to go find the Intune device id, is gone from every route." },
    ],
  },
  {
    build: 10467, date: "2026-08-27", title: "In production it is live — the roadmap stops saying BETA about shipped work",
    items: [
      { kind: "fixed", tool: "All tools", text: "Twenty-three roadmap cards wore a BETA chip while also naming the production build they shipped in. Both cannot be true: once something is in production it is live, whichever channel you happen to be reading the roadmap from. The chip now belongs only to work that is genuinely still only here — one card today." },
      { kind: "improved", tool: "All tools", text: "The eras are reordered: what is on this channel and not yet in production comes FIRST, then what is live, then Next and Later. The beta era is the news on a beta channel — it was sitting below twenty-three shipped cards, which is the one place nobody scrolls to look for it." },
    ],
  },
  {
    build: 10466, date: "2026-08-27", title: "Every text field looks like a text field",
    items: [
      { kind: "fixed", tool: "T15", text: "The device search in Defender status rendered as a raw browser input — square corners, the wrong border, the wrong height — next to controls that did not. It was not alone: nine controls across four tools had the same problem, because the field styling was only reachable through a wrapper class and any control written without it got the browser's default." },
      { kind: "improved", tool: "All tools", text: "The field look is now the DEFAULT for text inputs, selects and text areas inside the app, rather than something each control opts into. That is what stops the tenth one — the pattern here was a control being written correctly in every way except remembering a class, which is not a mistake worth catching one screenshot at a time." },
      { kind: "improved", tool: "All tools", text: "Tick boxes, radios, sliders, file and colour pickers opt OUT rather than every text field opting in, and are also explicitly reset — a 38px bordered checkbox is a bug this same rule already caused once when it lived under the wrapper. The sign-in card keeps its own treatment." },
    ],
  },
  {
    build: 10465, date: "2026-08-27", title: "The roadmap stops calling a beta-only tool shipped",
    items: [
      { kind: "fixed", tool: "All tools", text: "R30, the Policy overview, was sitting under “Now — shipped & shipping” while it runs only on this channel. It was the one card in that era naming no production build, and it was there while “In beta today” sat empty saying the two channels match — so the roadmap claimed the opposite of the truth in two places at once. Moved to the era it belongs in." },
      { kind: "improved", tool: "All tools", text: "The beta era no longer has an empty-state sentence claiming the channels match. It describes what the era is for, so it reads correctly whether it holds one card or none." },
    ],
  },
  {
    build: 10464, date: "2026-08-27", title: "Sixth promotion — items 97, 98 and 100-102 ship as build 11",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves to build 11 and takes five queue items with it — beta 10451-10457 and 10460-10463: the AppLocker generator's three layout cards and its gap report carrying coverage, the flood of gaps collapsed into one finding, Apply never a dead button, an applied fix changing the finding rather than only the rules, the deployed profile updatable in place, loop marks belonging to one tenant, the tenant's own name read at sign-in, the sidebar icons made legible and consistent between the folded and open rail, and the branding gear working on production as a preview that stays in your own browser." },
      { kind: "improved", tool: "All tools", text: "A PARTIAL promotion: item 99, T19 Policy overview, stays on this channel and stays in the queue on both. Its builds sit between the promoted ones, so index.html, the stylesheet and the shell each held promoted and unpromoted work in the same file — the promotion had to be assembled commit by commit rather than copied across, and one stray reference to the held-back tool's script did make it into the production tree before being caught." },
    ],
  },
  {
    build: 10463, date: "2026-08-27", title: "The sidebar icons stop changing size when you fold the rail",
    items: [
      { kind: "fixed", tool: "All tools", text: "Folding the sidebar resized every icon. The collapsed rail set its own size and the expanded one quietly inherited the size of the row text beside it, so the two states disagreed and every glyph jumped as the rail opened. Both now come from ONE rule, so they are the same size in both states and nothing moves." },
      { kind: "improved", tool: "All tools", text: "The size is declared in a single place rather than in each state. Two declarations for one requirement is how they came to disagree, and the headless suite now checks that the collapsed rule does not restate the size — not merely that the two numbers happen to match today." },
      { kind: "fixed", tool: "All tools", text: "The icon box widened with them. A 20px glyph in the old 22px box pushed the wider emoji into their labels, so the tool names no longer started at the same place down the list." },
    ],
  },
  {
    build: 10462, date: "2026-08-27", title: "The branding gear appears on production too — as a preview that stays in your browser",
    items: [
      { kind: "fixed", tool: "All tools", text: "The ⚙ branding gear was missing from tuno.limon-it.nl. It was not a bug — the whole self-host module returned early on the production host, and the gear went with it — but the effect was that the one deployment you most often want to show somebody a look on was the one you could not." },
      { kind: "improved", tool: "All tools", text: "The gate now splits by REACH rather than by feature. What you do to your own browser is allowed on production: the gear, Apply, Import, Download. What would change the site for everyone else is not — production still never reads selfhost-branding.json, so no served file can restyle it, and it never relabels its own ribbon as SELF-HOSTED." },
      { kind: "improved", tool: "All tools", text: "On production the dialog says what it is: that this is the production site, that its real identity is js/branding.js under review, that Apply reaches this browser and nowhere else, and that serving the downloaded file here does nothing. It previously promised that serving the file gives every visitor the look — true on a self-hosted copy, false here, and the kind of sentence somebody acts on." },
      { kind: "improved", tool: "All tools", text: "The one guarantee that always mattered is untouched: the canonical host is still not a field in that form, on any deployment. No amount of local branding can make a copy claim to be production, which is what the original gate was really protecting." },
    ],
  },
  {
    build: 10461, date: "2026-08-27", title: "Bigger icons in the collapsed sidebar",
    items: [
      { kind: "improved", tool: "All tools", text: "The icons in the collapsed rail are larger. Folded down to the icon rail there is no label beside them, so the icon is the only thing telling one tool from another — and it was doing that job at the same size as the row text it inherited, which is sized to sit NEXT to a word rather than to replace one." },
      { kind: "fixed", tool: "All tools", text: "The row height is unchanged. A taller glyph left to its own line height would have spread seventeen tools far enough apart to need scrolling, which would have cost more than the larger icon gained." },
    ],
  },
  {
    build: 10460, date: "2026-08-26", title: "Loop marks belong to one tenant",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "A fresh sign-in showed the audit loop's stations already ticked — old manual marks, persisted per browser by design, but a hand-mark is a claim about ONE tenant and it kept being shown against the next. Marks now carry the tenant they were made under (the organization read at sign-in) and clear themselves automatically when a different tenant signs in. Marks made before signing in are adopted by the first tenant that does; a browser that never signs in keeps the old behaviour unchanged; and 🧹 Start over still clears them by hand." },
    ],
  },
  {
    build: 10459, date: "2026-08-26", title: "The overview's read looks like every other read",
    items: [
      { kind: "fixed", tool: "🗂 Policy overview", text: "Reading the tenant now shows the same centred progress card — spinner, current step, the bar — that every other tool shows while it reads. The first build hand-rolled a small text line under the button instead, which is exactly the divergence the shared progress implementation exists to prevent: one way a read looks, everywhere. The step names are the documenter's own, because the read is." },
    ],
  },
  {
    build: 10458, date: "2026-08-26", title: "Policy overview — the tenant as cards, at last",
    items: [
      { kind: "new", tool: "🗂 Policy overview", text: "A new tool, in BETA: every configured object in the tenant as clickable cards — the sister tool ENCA's list-policies view, applied to Intune. The thirteen configuration surfaces sit on top as stat cards saying how many objects each holds and how many actually reach somebody, and clicking one filters the grid to it; clicking again brings everything back. The verdict chips work the same way: Assigned means reaching somebody by construction — at least one include or tenant-wide target — excluded-only is its own verdict because a policy whose every assignment says not-you is a different fault from one nobody assigned at all, and an assignment filter caps reach at may, worn on the card, because the filter rule is evaluated by the service against inventory a browser cannot see. The search box matches names, types, descriptions, platforms, surfaces and assignment group names, so which policies hit this group is one keystroke run. Clicking a card opens the full policy with its settings through the Configuration documenter's own popout — the read is the documenter's own thirteen-surface read and the popout template was extracted to be shared in this build, so the two tools cannot render one policy two ways, and every value has already passed the documenter's redaction. A surface that could not be read appears as its own warning card, named and deliberately not clickable as a filter, because filtering to a surface nobody read would be an empty grid pretending to be an answer. Reads only, no new permission, no exports on purpose: the document is the documenter's job, one click away in the same popout." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "The policy popout's head and body moved into the shared Docs module so the new Policy overview opens the identical popout. Nothing the documenter shows changed; the include-in-the-document tick in the popout's foot stays the documenter's own." },
    ],
  },
  {
    build: 10455, date: "2026-08-26", title: "TUNO knows whose tenant it is — the organization is read at sign-in",
    build: 10456, date: "2026-08-26", title: "TUNO knows whose tenant it is — the organization is read at sign-in",
    build: 10457, date: "2026-08-26", title: "TUNO knows whose tenant it is — the organization is read at sign-in",
    items: [
      { kind: "new", tool: "TUNO", text: "The header now shows the organization's display name — Contoso B.V. rather than contoso.onmicrosoft.com — because TUNO reads the organization at sign-in, ported from ENCA, which has always done this. The read costs nothing it did not already have: Microsoft Graph's own permission table lists User.Read, the one scope TUNO signs in with, as sufficient for reading exactly the organization's id, display name and verified domains — everything else comes back null and is not asked for. No new permission, no consent popup, and the security documentation's claim that sign-in is User.Read only stands untouched. The read is best-effort, ENCA's way: the header shows the UPN domain immediately and upgrades when the answer lands, and a read that fails — offline, a guest account — leaves everything as it was. Two things ride on it. The org-name half of the cfdev tenant detection, dormant since build 10373 because nothing read the org, is now live from the moment of sign-in. And every tool gets one place to ask who the tenant is — TunoTenant.org() — instead of paying its own organization read, which is one request less per tool that wants the tenant's name or id on a report." },
    ],
  },
  {
    build: 10456, date: "2026-08-26", title: "Update the deployed profile in place — and edit the version where the button is",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The same-grouping stop used to end with go-paste-the-OMA-URIs-into-the-portal — every loop, by hand. It now offers ✎ Update it in place: a confirm card names the deployed profile and the promise, then TUNO writes the adjusted rules and the new version name into that profile — same grouping, same object, so its assignments never move and there is nothing to unassign or carry over. Removals land too, because the settings are replaced whole. This is the one deliberate exception to TUNO-never-overwrites, it happens only on that confirmation, and it re-reads the tenant first. Also reachable from the 🔎 check button when the grouping matches — a draft with open findings has its deploy disabled, and that is exactly when someone is iterating. (A supersede flow — new profile, assignments moved over — was mocked up and declined: transition window, sequencing risk, no gain over the in-place edit.)" },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The version token in the create row is an input now: edit V4.0.1 right where the profile name is shown. It moves the version in the name only — the grouping never follows a version — and anything that is not a version number is refused." },
    ],
  },
  {
    build: 10455, date: "2026-08-26", title: "An applied fix changes the finding, not just the rules",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Applying the unsigned-executables hash fix left the finding on screen unchanged — same files, same recommendation, button gone — which read as the fix not working (the bug report, verbatim; the new rules sat two cards down and the ✔ Applied notice fades after seconds). The finding now changes state once its collections carry the TUNO hash rules: Info, covered by the hash rules in this draft, with the expiry duty written in and the way back named — delete the TUNO hash rules in the Rules card and the original finding reopens, fix and all. Undo does the same in one click." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "For the record after the where-did-it-go report: the ＋ Add default Windows rules buttons live in the right column of the Enforcement per collection table (step 2), one per collection plus ＋ Default rules everywhere — a collection that already has its defaults shows ✓ present instead of a button, which is the button doing its job, not the button missing." },
    ],
  },
  {
    build: 10454, date: "2026-08-26", title: "The grouping explains itself — and the deployed identity is one click away",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The question kept coming back, so the answer now lives where the deploy happens: the grouping is the policy's address on the device. Same policy, next iteration — same grouping, edit the deployed profile in place; a new GUID deploys a second policy beside the old one, the device merges both, and anything you removed keeps applying from the old address. That line sits at the create button now, with a Check-against-the-tenant button beside it that works even while findings block the deploy — which is exactly when someone is iterating." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The check lists any AppLocker profile deployed under a different grouping and offers to adopt its identity: the deployed grouping and the name at its next version land in the form, while the adjusted draft on the table stays exactly as edited — unlike the evidence card's full pull, which replaces the draft with the tenant's rules. This is the mid-loop move: scan bundle in, results in, gaps closed, adopt the deployed identity, export, edit in place. Re-checking confirms the match, and the collision stop now says plainly that a same-grouping stop while iterating is the system working — the export goes into the existing profile in the portal, not into a twin beside it." },
    ],
  },
  {
    build: 10453, date: "2026-08-26", title: "Apply is never a dead button",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The Add-hash-rules fix deduplicates by rule name, so applying it a second time added nothing — and said nothing, which reads as a broken button. Two corrections: a fix whose rules already exist is not offered at all any more, and any confirm-card Apply that ends up adding nothing now says why right in the card — the rules already exist from an earlier apply, and deleting them in the Rules card is the way to rebuild." },
    ],
  },
  {
    build: 10452, date: "2026-08-26", title: "A flood of gaps becomes one finding",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Adding five DLL hash allows put rules into the Dll collection — and one rule in a collection makes AppLocker enforce the whole collection, so every other DLL the fleet had loaded woke up as a would-block and the findings table became a scroll of two hundred rows. The flood was true, but a finding you cannot read is not a finding. More than eight gaps in one collection now collapse into a single High row: the count, the reason they all woke, and every file behind an expand-and-collapse — with a recommendation that says the honest thing: allowing DLLs one hash at a time is not a policy; decide whether the collection should be governed at all, or remove its rules and the events go back to set-aside. A handful of gaps keep their individual rows and their fix cards. Long unbroken file names also stopped crushing the neighbouring columns." },
    ],
  },
  {
    build: 10451, date: "2026-08-26", title: "Three cards, one per layout",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The merged card put three different table layouts under one roof — the findings table, the coverage table, the rule tables — and three layouts in one card read as a broken card. The split now follows the layouts: Findings lead as their own card, Microsoft app coverage has its own, and Rules carry their nested findings in a third. Everything the merges won stays won: one fix flow, findings under the rules they are about, and a sticky header on each card — with the Applied notice landing in whichever header is pinned above the fix you just clicked. The empty leftover cards from the merge are gone." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The gap report now always carries a Microsoft app coverage section. The report is the change-ticket document, and whether a standard user can still run OneDrive belongs in the same envelope as the gaps; a report generated before a policy is loaded says exactly that instead of dropping the section without a word." },
    ],
  },
  {
    build: 10450, date: "2026-08-26", title: "Production stops calling its tools beta",
    items: [
      { kind: "changed", tool: "TUNO", text: "Mihai's rule, recorded on both channels: BETA is channel language and never ships to production. Here it says a tool is still proving itself on this channel; on production it named a channel the visitor is not on, sixty times over after build 9. Production build 10 translated its chips — NEW on the nine tools new to that production line, UPDATED on the six that changed in it, nothing on the rest — and this build makes it a standing rule: the promotion ritual gains a fifth step (relabel on main), the production check now fails any BETA chip that slips through, and CLAUDE.md carries the rule for future sessions. Nothing changes on this channel's own chips: BETA still means what it always meant here." },
    ],
  },
  {
    build: 10449, date: "2026-08-26", title: "Fifth promotion — the whole queue ships as build 9",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves to build 9 and takes all thirty-five queue items with it — beta builds 10357 through 10448, the largest promotion yet: nine tools (what-if, health, settings search, conflicts, compliance, Defender, firewall and ASR, multi-admin approval, the LAPS audit with the registration's first new permission in ninety-nine builds), the widened self-verifying backup with assignment import, the timeline layout across five tools, demo mode, self-host branding, and the shell polish. The queue is empty again; the roadmap's In-beta-today era stands empty on both channels, saying truthfully that they match." },
      { kind: "improved", tool: "All tools", text: "This was the first promotion ordered by the exported promotion file — the tick boxes shipped in 10444, exported by Mihai two days later, handed to a working session, verified against what main actually contained, and promoted. The order file's own sentence held: it was the order, not the verification, and the verification found every difference between the channels accounted for before a single file moved." },
    ],
  },
  {
    build: 10448, date: "2026-08-26", title: "One family of fix buttons",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The coverage table's Add-allow-rule button wore the dark primary style while the findings' fix buttons were outline, and the two never read as one family. Mihai picked the quiet option from three: every fix and allow action is now the same outline button, and the wording carries the difference — a label that names an act (Set AuditOnly, Add allow rule, Add collection) happens on the click; a label trailing an ellipsis (Fix…) opens the editor. The suite holds the family together: a primary class creeping back onto either side fails it." },
    ],
  },
  {
    build: 10447, date: "2026-08-26", title: "The companion scripts fold away",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Step 1's eight download rows were most of a screen before the tool's own work began. The scanner — the act step 1 exists for — stays out; the seven companions and the paragraph explaining them fold under one line: cleanup pair, IT-TOOLS provisioning pair, events-collection set. A native fold, closed by default, no script behind it to break; find-in-page still opens it." },
    ],
  },
  {
    build: 10446, date: "2026-08-26", title: "The Defender report learns to find one machine",
    items: [
      { kind: "improved", tool: "🦠 Defender status", text: "A search box joins the toolbar: type a device name, a user, or an id, and the fleet list filters live under it while ranked suggestions unfold from what was just read — no call to the tenant per keystroke, because the fleet is already in memory. Picking a suggestion narrows the list to that machine and opens its fold in one act, since the point of finding a device is reading its protection state. Arrow keys and Enter work; Escape and clearing restore the whole fleet." },
      { kind: "improved", tool: "🦠 Defender status", text: "The box appears only after a fleet has been read — its suggestions come from the fleet, and an autocomplete with nothing behind it is a promise nothing keeps. An empty query suggests nothing, deliberately: an autocomplete that lists ten thousand machines unprompted is a dump, not a search. The search combines with the bucket cards, and the devices header says when a search is narrowing what you see." },
    ],
  },
  {
    build: 10445, date: "2026-08-26", title: "The sticky header actually sticks",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The merged card's sticky header shipped in 10443 and quietly never stuck: the card clips its corners with overflow hidden, and a sticky element inside a clipped ancestor never engages — no error, just nothing. The card now lets its overflow show; the padding keeps content off the rounded corners and the tables inside carry their own scrollers, so nothing else changes." },
    ],
  },
  {
    build: 10444, date: "2026-08-26", title: "The queue takes ticks, and hands back an order",
    items: [
      { kind: "new", tool: "All tools", text: "The promotion queue in Help grew a tick box per item and an export button. Tick the items you have verified on this channel, press export, and a small file comes down — the promotion order: the ticked numbers up front, each item's title, builds, risk and files below, and a machine-readable block at the end. Hand that file to the working session and it becomes the promotion instruction. Ticks persist across reloads, keyed on the item's stable number, and a tick whose item ships is pruned rather than left pointing at nothing." },
      { kind: "new", tool: "All tools", text: "The file says on its face what it is and is not: the ORDER, not the verification. The receiving session still checks every item against what main actually contains before building the production commit — the queue's own oldest rule, and a nicer file format does not bend it. An export with nothing ticked refuses; a stale tick for a shipped item refuses by name rather than silently dropping, because an order that quietly shrank is the same lie as a range that quietly shrank." },
    ],
  },
  {
    build: 10443, date: "2026-08-26", title: "One card, one fix flow, and a header that stays in sight",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The merged card's header is now sticky, so however deep you scroll you can see whose card you are in — and Microsoft app coverage folded in as a section of the same card, its verdicts and one-click allow rules unchanged, its separate card gone. The events finding that said review the audited list below while pointing at nothing now carries that list itself: expandable in place, aggregated per file with counts and affected users — the 1068 audited executions from a real bundle become a readable list of the files behind them." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "One fix flow everywhere, reported from real use. The fleet-gap fixes used to apply instantly while the rule fixes opened an editor — two behaviours on one screen read as one of them broken. Every fix now opens the same card first: the exact rule about to be added, publisher, hash or path, then Apply. And applying announces itself — ✔ Applied with Undo beside it lands in the sticky header and fades after a few seconds. The publisher editor also stopped accepting an unchanged Apply: leaving the version bound at * used to rewrite the rule identically and close, which read as a fix that did nothing; now it says what is actually missing. Finally the unsigned-in-writable-locations finding gained its own fix card — one hash rule per collection, every hash inside, the expiry warning written into the rule itself." },
    ],
  },
  {
    build: 10442, date: "2026-08-26", title: "Rules and findings become one card",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The rules list and the findings table told one story in two places: here is a rule, and here is what is wrong with it. Now they are one card. A finding that is about a rule sits nested under that rule, inside the per-collection rule tables — severity, reason, recommendation and the fix button right where the rule is, with the judgement editors opening in place. Findings about nothing in particular — a missing collection, the enforcement judgement, a fleet gap — keep the table shape at the top. The severity filters, the compact narrow-column summary and every fix behave exactly as before; the page just stopped asking you to scroll the same story twice. Chosen by mockup round over folding the rules away behind the findings." },
    ],
  },
  {
    build: 10441, date: "2026-08-26", title: "One table — the fleet gaps become findings",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The findings table and the fleet evidence were two tables saying the same kind of thing about the same files — a file needs a decision — and together they made the overview long. Now there is one. Every fleet gap is a finding: a 📡-marked row beside the audit's and the scan's, carrying the event count and the still-blocked reason, closed through the same one-click fix framework as everything else — publisher when signed, hash when the event carries one, exact path last, undo one click away. The evidence card shrank to what only it can say: the device facts, one counts line (gaps, blocked by design, covered, undecided, DLL set aside) that links into the findings, the gap report download, and the pull-from-tenant chooser. Covered and by-design files need no rows on a working screen — they are reassurance, and reassurance lives in the counts and the report. The filter chips retired with the second table they filtered." },
    ],
  },
  {
    build: 10440, date: "2026-08-26", title: "The profile version counts its iterations",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The version token at the end of the profile name now moves with the loop. Pull the deployed profile from the tenant and the name on the table is already the next iteration — V4.0 in the tenant becomes V4.0.1 to edit, V4.0.1 becomes V4.0.2 — so the portal's history reads as a sequence instead of one name overwritten in place. A +v button beside the name field increments by hand, typing in the field overrides everything, and a name without a version token is left alone: inventing a version is worse than not having one. The pull also learned to sync the form fields it changes — the screen was still showing the old name and grouping after adopting a profile, which this build's own test caught." },
    ],
  },
  {
    build: 10439, date: "2026-08-26", title: "Change audit narrows to one type of thing",
    items: [
      { kind: "new", tool: "T03", text: "Policy changes now has a row of type chips \u2014 Application, Device configuration, Compliance policy, and whatever else your tenant emitted \u2014 each with its count. Click one to see only those, click it again to clear. Until now the only type filter lived in the All events tab, so on the tab most people use there was no way to ask for just the app changes." },
      { kind: "improved", tool: "T03", text: "The counts are the answer before you click anything: 37 of 49 being application changes is the finding, and you should not have to filter to learn it. So picking a type does NOT rewrite the other chips \u2014 the row keeps describing the window rather than the selection, which is what lets you see what you are choosing between." },
      { kind: "improved", tool: "T03", text: "The row is only drawn when there is more than one type in the window. A filter with a single option is chrome pretending to be a choice, and it would say nothing that every card in the list does not already say." },
      { kind: "improved", tool: "T03", text: "A type with nothing under it says so, and says the others still have events \u2014 rather than showing an empty list that reads like a quiet tenant. Switching to All events drops the type, because that tab has its own category control and a filter its own screen cannot show you is a filter you will forget is on." },
    ],
  },
  {
    build: 10438, date: "2026-08-26", title: "Two uploads, two buttons",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The upload split into the two acts it always was: Upload scan bundle or policy XML for the draft, Upload events results for what the collector brought home. Both open the same picker and the import still recognises the file by its content — the second button exists for the reader, because three files crammed into one label read as none of them." },
    ],
  },
  {
    build: 10437, date: "2026-08-26", title: "DLL events step out of the way",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "A real fleet bundle drowned the evidence in DLL rows — 112 audits of one Windows library among them — all reading undecided, which was technically true and practically noise. Generated policies omit the DLL collection on purpose (absence is the only state that restricts nothing), so a DLL load in the log is the record of that decision, not a question waiting for an answer. Those events are now set aside: out of the default view and the undecided count, behind their own chip that brings them back with the reasoning written into each row, no fix button offered, and their own section in the gap report. The one case where they classify normally is a draft that actually carries DLL rules — because then policing DLL loads was a choice, and the evidence should be judged against it." },
    ],
  },
  {
    build: 10436, date: "2026-08-26", title: "Filter the evidence, clear the table",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The counts above the fleet evidence became filters: click 4 gaps to close and the table shows exactly those four rows — the fifty-row cap applies after the filter, so a handful of gaps among two hundred files never hides. The active chip reads as pressed, clicking it again shows everything, and the downloadable gap report deliberately ignores the filter, because a document for the change ticket covers everything or it is not evidence." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Two ways to take things off the table. Clear, on the evidence card, drops just the events bundle and keeps the policy — for loading the next device's results against the same draft. Start over, next to the upload button, empties everything: policy, scan, evidence, the loop's manual marks — behind a confirmation whose one promise is that nothing in the tenant is touched. And the upload button now says what it actually takes: scan bundle, policy XML, or the collector's events results." },
    ],
  },
  {
    build: 10435, date: "2026-08-26", title: "Events first — and the policy comes from the tenant",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Uploading the events bundle before a policy used to earn you an alert telling you to go find the scan or the XML — confusing, and wrong about where the policy actually is mid-loop: in the tenant, not on your disk. The fleet evidence card now stands on its own. It renders the moment the bundle lands, ahead of any policy, and leads with the real question — what to judge the evidence against — instead of hiding until a file is found." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "And the best answer to that question is now a button: pull the deployed AppLocker profile straight from the tenant. Sign in, click, and the tool lists the custom profiles that carry AppLocker OMA-URIs; pick one and the policy is rebuilt from the RuleCollection values the profile holds. Pulling adopts the profile name and grouping into the Intune form, so closing gaps and re-exporting edits the same profile in place — and the deploy panel keeps refusing to create a twin beside it, which is exactly the discipline the grouping rules demand. If Graph withholds a value, the pull refuses with a reason rather than rebuilding half a policy quietly." },
    ],
  },
  {
    build: 10434, date: "2026-08-25", title: "Click a group in the sweep and see everything it is connected to",
    items: [
      { kind: "new", tool: "T02", text: "A group in the sweep is now a link. Clicking it opens that group on its own \u2014 every assignment naming it, grouped by surface, with how it is targeted and any filter capping the reach. Until now the sweep gave you counts and no way to ask what they were counting." },
      { kind: "improved", tool: "T02", text: "It costs NO reads. The sweep already holds every assignment it found, so opening a group filters what is in memory rather than scanning again \u2014 instant on a tenant of any size. Re-running an analysis to show data already on the page is the thing this replaces, and the headless suite counts the calls rather than taking the claim on trust." },
      { kind: "new", tool: "T02", text: "\u2af7 Deep analyze, which was missing entirely. A sweep matches every group against itself, so a policy that reaches a group THROUGH A PARENT is not in its row \u2014 the popup says so instead of answering short and looking complete. Deep analyze is the opt-in that goes back and reads that one group with its parents expanded." },
      { kind: "new", tool: "T02", text: "Deep analyze no longer costs you the sweep. The finished sweep is parked and \u201c\u2190 Back to the sweep\u201d puts it straight back \u2014 same groups, same filters, no second scan. Charging a full re-scan for having been curious about one group is what stops people clicking." },
      { kind: "improved", tool: "T02", text: "A group the directory no longer has opens like any other, and says what it is: the assignments still naming it, and the fact that they reach nobody. It was already counted as dangling; now you can see which assignments to go and remove." },
    ],
  },
  {
    build: 10433, date: "2026-08-25", title: "The tool menu says which T number each tool is",
    items: [
      { kind: "improved", tool: "All tools", text: "The sidebar now shows each tool's permanent number beside its name \u2014 Group analyzer T02, Change audit T03, and so on. These numbers are how the tools get referred to out loud and in the promotion queue, and until now the only place to find one was the tile heading on the overview." },
      { kind: "improved", tool: "All tools", text: "The number is read from the tool register rather than typed into the menu, so the menu and the tile cannot drift apart \u2014 a wrong T number would be worse than none. Help, What's new and the roadmap stay unnumbered on purpose: the app describing itself is not a tool." },
      { kind: "fixed", tool: "All tools", text: "The number holds the right edge of the row and the NAME shortens when the rail is too narrow. Appended to the label it would have been the first thing cut off, on exactly the longer names it is most useful for. The collapsed rail drops it along with the name." },
    ],
  },
  {
    build: 10432, date: "2026-08-25", title: "The group box suggests groups again \u2014 by any word in the name, not just the first",
    items: [
      { kind: "fixed", tool: "T01 \u00b7 T08 \u00b7 T11", text: "Typing in a group box suggested nothing unless you happened to type the START of the group's name. It asked Graph for names BEGINNING with what you typed, so \u201cpilot\u201d found a group called \u201cPilot ring\u201d and did not find \u201cINT-DEV-Pilot\u201d \u2014 which is how tenants actually name groups: site, then function, then purpose. The word you remember is almost never the first one. It now matches a word anywhere in the name, so the box finds the group you were thinking of." },
      { kind: "improved", tool: "T01 \u00b7 T08 \u00b7 T11", text: "Both searches are asked and the answers merged, because neither alone is enough: word matching splits on separators, so a half-typed word finds nothing, while prefix matching still catches it. A tenant that refuses word search loses the extra matches rather than the box \u2014 the field keeps working on the prefix alone instead of going dead." },
      { kind: "fixed", tool: "T01 \u00b7 T08 \u00b7 T11", text: "The group name is now escaped the way the rest of the read layer escapes it. It was being encoded twice, which quietly mangled any search containing an apostrophe or an ampersand \u2014 the two characters most likely to appear in a real group name." },
    ],
  },
  {
    build: 10431, date: "2026-08-25", title: "The tick boxes stop sitting on the labels \u2014 in the two tools the first fix never reached",
    items: [
      { kind: "fixed", tool: "T08 · T09", text: "In the surface pickers, the tick box was printed on top of the end of the label \u2014 \u201cConfiguration profiles\u201d and \u201cEnrolment restrictions\u201d were the visible ones. This was reported before and fixed before, at build 10425, and the fix did not work: the space was reserved on the card's TITLE, and these two tools write their labels as plain text with no title element to reserve anything. The space is now reserved on the CARD, which every tool draws the same way, so it protects the label whatever shape the tool writes it in." },
      { kind: "improved", tool: "All tools", text: "One mechanism instead of two. Having the reservation on a class only four of the six tools emitted is exactly how the same bug survived being fixed \u2014 the four that opted in looked right, the two that did not kept the fault, and both looked like the same card on screen. The headless suite now checks the shape of the fix rather than the symptom: it counts the tools that draw these cards, notes which write the label bare and which wrap it, and fails if the reservation ever moves back onto a child that a tool has to remember to use." },
    ],
  },
  {
    build: 10430, date: "2026-08-25", title: "A whole demo tenant, and not one line of it inside a tool",
    items: [
      { kind: "new", tool: "All tools", text: "TUNO has a demo. Add ?demo=1, or take the link on the sign-in card, and every one of the seventeen tools runs against a fake tenant called Contoso \u2014 no sign-in, no consent prompt, nothing read from anybody's real tenant and nothing written to one." },
      { kind: "new", tool: "All tools", text: "The demo tenant is BROKEN ON PURPOSE. A demo of a healthy tenant is a demo of empty tables: every tool would load, report nothing, and showcase nothing. So Contoso carries the faults the tools exist to find \u2014 a policy still assigned to a group somebody deleted, firewall configured twice and reaching nobody either way, two policies setting the same thing to opposite values on the same group, a Mac fleet with no compliance policy in a tenant where that means they pass anyway, an approval gate whose approver group is empty, three laptops with no LAPS escrow, and one that last checked in nine days ago. Each fault is commented with the tool it is there for, so a later edit can tell a fixture from a mistake." },
      { kind: "improved", tool: "All tools", text: "It hooks in at ONE PLACE. Every read and every write in every tool already went through a single function in the Graph layer, so that is where the demo stands \u2014 no tool knows demo mode exists, no tool has a branch for it, and a tool written next year gets it for free the moment its endpoint is in the fixture. ENCA needed roughly eighty branches for the same feature because it had nowhere single to stand; this is the one place TUNO's plumbing is genuinely better, and it was worth spending." },
      { kind: "improved", tool: "All tools", text: "Standing in for Graph means keeping its promises, and the tools lean on more of them than you would guess: a single object must not arrive wrapped in a collection, $expand has to actually expand, a $count answers in text rather than JSON, $filter has to genuinely narrow or the group picker throws, and $batch is a protocol rather than a shortcut. All of that is honoured, and the suite checks each one \u2014 along with the assertion that matters most, which is that nothing in demo mode can reach the network at all." },
    ],
  },
  {
    build: 10429, date: "2026-08-25", title: "The Windows LAPS audit — R29 ships, on the first new permission in 99 builds",
    items: [
      { kind: "new", tool: "🔑 Windows LAPS audit", text: "T18, in Endpoint security. Which Windows devices have an escrowed local administrator password and how old it is — not escrowed and stale are the two shapes of one finding, a LAPS policy that is not applying, found before the day somebody needs the password that is not there. Escrow metadata cross-referenced against every Intune Windows device on the Entra device id; five buckets, not the original's four: healthy, stale (threshold yours, default 60 days), not escrowed, age unknown — escrowed with an unparseable backup time, neither healthy nor stale because either would be a guess — and unmatchable, a device with no Entra device id on its record, which the original counts as not escrowed and this refuses to accuse. Escrow records for retired machines are their own list, never silently ignored. Passwords are never retrieved and there is no switch that would: the permission is DeviceLocalCredential.ReadBasic.All — the first new one since the Device analyzer, taken in the open — through which Graph cannot return a password value. Graph also gates the endpoint on the signed-in user's directory role, and that refusal is explained on screen as who-you-are, never a bare 403. The registration script and the security documentation take the scope in this same build; existing tenants need a fresh admin consent before the first run. Reads only. Exports Markdown and CSV. After Ugur Koc's Get Windows LAPS Audit (MIT)." },
    ],
  },
  {
    build: 10428, date: "2026-08-25", title: "The compliance report learns what it cannot see — R28 ships",
    items: [
      { kind: "new", tool: "📋 Compliance report", text: "The coverage section: the platforms nothing evaluates. Every other number in this report comes from policies that exist; this one finds the platforms with enrolled devices and no compliance policy that reaches anybody — computed entirely from the two reads the report already does, so it costs the tenant nothing. Covered is the narrow claim: assigned and reaching somebody by construction — exclusions-only and unassigned cover nothing, and a covering policy with an assignment filter wears 'may' on its verdict. A platform is three answers, not two: gap, covered, or no devices — context, so an unassigned macOS policy in a Windows-only tenant does not read as a hole. And the original script's check-this-setting footnote becomes a read: the tenant's 'Mark devices with no compliance policy assigned as' lean is stated next to the table, because Compliant there means every gap silently passes Conditional Access; unreadable is said with the portal path, never guessed. Either half of the read failing makes coverage unknown, not uncovered. Coverage CSV joins the exports; the Markdown report carries the section. After Ugur Koc's Get Compliance Policy Coverage (MIT)." },
    ],
  },
  {
    build: 10427, date: "2026-08-25", title: "Two more cards: the coverage gap and the LAPS audit",
    items: [
      { kind: "new", tool: "🗺 Roadmap", text: "R28, Compliance coverage: the platforms nothing evaluates — enrolled devices with no assigned compliance policy, which depending on one tenant setting report compliant by default and sail through Conditional Access. Deliberately not a new tile: the script's two reads are exactly the two the Compliance report already does, so it ships as a section inside that tool with zero new Graph traffic — and the original's check-this-setting footnote becomes a read, so the tenant's compliant-by-default lean is stated next to every gap. R29, Windows LAPS audit: which Windows devices have an escrowed local admin password and how old it is — not-escrowed and stale are the two shapes of a LAPS policy not applying. It needs the first new permission since the Device analyzer, DeviceLocalCredential.ReadBasic.All, taken in the open: ReadBasic deliberately, because Graph cannot return a password value through it, and the full Read.All stays with R10's helpdesk viewer where it belongs. Both after IntuneAutomation scripts; the credits entry grows from eight roadmap items to ten." },
    ],
  },
  {
    build: 10426, date: "2026-08-25", title: "The backup widens, proves itself, and assignments come back",
    items: [
      { kind: "new", tool: "📦 Backup configuration", text: "R15 ships, inside the backup tool where it belongs. Nineteen new areas join the original five — security baselines, Autopilot, enrolment configurations, the three update-profile kinds, macOS custom-attribute scripts, assignment filters, scope tags, device categories, terms and conditions, notification templates, both kinds of app configuration, app protection for all three platforms, Windows Information Protection and app categories. The five interop folders keep the PowerShell original's names exactly; the new ones are TUNO's own, because claiming byte-interop with a Go binary's layout would be a claim nothing verifies. Consent is the union of what you tick — the five alone still ask exactly what they always did." },
      { kind: "new", tool: "📦 Backup configuration", text: "Every file now carries a SHA-256 in the manifest, and the manifest goes into the zip last — TenuVault's rule, kept exactly: its presence is what marks a backup complete. A new verify panel checks an archive entirely in the tab, no sign-in: modified, missing and untracked files are named, an archive without a manifest is called what it is — a backup that never finished — and an older archive is called unverifiable rather than quietly passed." },
      { kind: "new", tool: "📦 Backup configuration", text: "Assignments export and import — the amendment Mihai made to the R15 card, which had said the assignment record would be read-only. assignments.json carries every assigned policy's targets, serialised through the assignment editor's own cleaner so the file is exactly what the assign call accepts, with groups.json naming every targeted group as it was named at backup time. The import puts them back under three hard rules: same tenant only, refused outright on a mismatch with no override; all-or-nothing per policy when the archived list names a group that has since been deleted, because writing the list without it changes its meaning and a dropped exclusion widens; and the assignment editor's whole write discipline — same policy ids never a namesake, dry run first, drift skipped, every write verified by read-back. A tampered archive cannot be imported from at all." },
      { kind: "fixed", tool: "📦 Backup configuration", text: "Build 10425 briefly carried the verify panel and the suite's new checks without the engine behind them — two working copies, one tree, the 10355 lesson re-learned politely this time: that build deliberately left backup.js out and reserved the number. This build supplies the engine and completes the pair; push them together." },
    ],
  },
  {
    build: 10425, date: "2026-08-25", title: "The surface pickers fit, and the roadmap tells beta from production",
    items: [
      { kind: "fixed", tool: "All tools", text: "The Graph endpoint under each surface name was being cut off at the edge of its card. It wraps now instead. Shortening it with an ellipsis would have been worse than leaving it cut: these paths all begin with the same prefix and differ only at the end, so trimming the end removes the only part that says which one you are looking at." },
      { kind: "new", tool: "All tools", text: "Every surface picker gained a select-all, which also clears them. Six tools ask you to choose what to read and none of them offered a way to change your mind about all of it at once. One button serves all six \u2014 it is added by the shell rather than copied into each tool, and it works by ticking the boxes the way a person would, so each tool keeps deciding what its own selection means." },
      { kind: "improved", tool: "All tools", text: "The roadmap now separates what is in production from what is only on this channel, the way ENCA's does \u2014 a fourth era reading \u201cIn beta today\u201d. Ten items had been sitting under \u201cNow\u201d while living only here, which is precisely the distinction the promotion queue exists to make, and the cards already carried the build numbers to tell them apart. Nothing was rewritten: the same twenty-seven cards, sorted by what their own badges already said." },
    ],
  },
  {
    build: 10423, date: "2026-08-25", title: "The home centres in its own column",
    items: [
      { kind: "fixed", tool: "All tools", text: "The home screen sat left of centre with a dead band down its right edge. The cause was a leftover from the console shell: the content column widened to 1500 pixels when the sidebar arrived and every tool screen's cards widened with it, but the home grid kept its 1180-pixel cap and stayed pinned to the left — and the home is the one screen that is nothing but that capped block, so all the leftover width piled up on one side. Option A of a two-option mockup round: the grid, its section headings and the title now centre as one unit in the space the sidebar leaves, keeping today's tile size and reading width, with every left edge still aligned — including the Show-more button under a collapsed section. Scoped to the home: the roadmap and help pages, whose own widths already line up, are untouched, and without the sidebar nothing changes at all." },
    ],
  },
  {
    build: 10422, date: "2026-08-25", title: "Multi-admin approval — R27 ships, and the security three are done",
    items: [
      { kind: "new", tool: "🤝 Multi-admin approval", text: "T17, the last of 10419's three security cards, beside Intune RBAC in Access and roles. Every approval policy and what its type gates, the requests of a chosen window — pending, approved, rejected, with average, median, fastest and slowest time to approval — and coverage computed the way MAA actually works: a gate applies to its operation type tenant-wide, a category with no policy has no gate at all, and device actions are listed as action gates rather than inventories. Two findings the original cannot make: a policy whose approver groups hold nobody is flagged as a gate nobody can open, checked transitively; and approved requests without parseable timestamps sit outside the approval-time numbers with their count stated. The admins table carries the Intune-RBAC-only sentence verbatim, on screen and in every export, because Global Administrator is not in that list and a report that hides the fact overstates its own coverage. A tenant that never configured MAA reads as exactly that — an answer, not an error. Fewer permissions than the original asks for, all already consented. Reads only. Exports Markdown, requests CSV and admins CSV. After Ugur Koc's MAA Compliance Dashboard Report (MIT)." },
    ],
  },
  {
    build: 10421, date: "2026-08-25", title: "Firewall & ASR coverage — R26 ships",
    items: [
      { kind: "new", tool: "🧱 Firewall & ASR coverage", text: "T16, the second of 10419's three security cards, in Endpoint security. Whether the Windows fleet actually has firewall and attack-surface-reduction enforcement, or just policy objects: every endpoint security policy — settings catalog and legacy intents — grouped into the portal's own disciplines, each card a verdict: covered, policies exist but none reaches anybody, or no policy at all. 'Covered' is narrower than the original's: assigned AND reaching somebody by construction — no include and no tenant-wide target is nobody, exclusions-only is nobody, a filter caps reach at may. Legacy intents are classified through their template rather than dumped under a pseudo-discipline, so a tenant enforcing firewall the old way no longer reads as a firewall gap — the original's one overstatement in the dangerous direction. The Windows device count is the denominator: a gap is that many machines on local defaults, and a count that could not be read is unknown, not zero. Group names resolved in one call; discipline cards filter; rows fold to their assignments. Reads only, no new permission. Exports Markdown and CSV. After Ugur Koc's Get Firewall and ASR Status (MIT)." },
    ],
  },
  {
    build: 10420, date: "2026-08-25", title: "The Defender status report — R25 ships",
    items: [
      { kind: "new", tool: "🦠 Defender status", text: "T15, first of the three security cards from 10419 to ship, in Endpoint security. Every Windows device's protection state — real-time protection, tamper protection, malware protection, signature currency, overdue quick and full scans, pending reboots, a device state that is not clean — worst first, each device carrying its list of findings rather than one traffic light. The per-device read the original pays one device at a time with a sleep folds into batches of twenty with progress on screen. Three honesty rules: a device that never reported protection state is its own bucket, never healthy by omission, and a state that could not be read lands there too with its error stated; a flag the device did not report reads 'not reported', never 'off' — the original counts them the same, and they are different answers; and the tenant rollup shown beside the per-device counts is Graph's own, refreshed on its own schedule — the two can disagree, and neither is reconciled to the other. Cards filter by bucket, rows fold open to the full state, and a device clicks through to the Device analyzer. Reads only, no new permission. Exports Markdown and CSV. After Ugur Koc's Get Defender Status Report (MIT)." },
    ],
  },
  {
    build: 10419, date: "2026-08-25", title: "Three more of Ugur Koc's questions join the roadmap",
    items: [
      { kind: "new", tool: "🗺 Roadmap", text: "Three cards in Next, all after IntuneAutomation scripts and all needing no new permission — every scope they ride is already consented. R25, the Defender status report: the machines where Defender is silently off or outdated — real-time protection, tamper protection, signature currency, overdue scans, pending reboots — read per Windows device and batched twenty a trip where the original walks the fleet one device at a time with a sleep; a device that never reported protection state is its own finding, never healthy by omission. R26, Firewall and ASR coverage: whether the fleet has firewall and attack-surface-reduction enforcement or just policy objects — every endpoint security policy grouped by discipline with a three-way verdict, and 'assigned' checked by construction so a policy that reaches nobody does not count as coverage. R27, Multi-admin approval: every approval policy, the requests of a chosen window with how long approval takes, and coverage per category computed the way MAA actually gates — per operation type, tenant-wide. Its admins list carries the Intune-RBAC-only sentence from the role assignments tool, because the original counts 'admins without MAA' from a list that does not contain Global Administrator. R25 and R26 will land in Endpoint security; R27 beside Intune RBAC in Access and roles. The credits entry for IntuneAutomation grows from five roadmap items to eight." },
    ],
  },
  {
    build: 10418, date: "2026-08-25", title: "Conflicts fold — and open to the comparison",
    items: [
      { kind: "improved", tool: "⚡ Setting conflict scan", text: "The last of the four 10413 conversions. Stat cards for the verdicts — can collide, may collide, cannot, the number of settings more than one policy configures, and surfaces unread when there are any. Each conflict is now a folded row: closed, it says the setting, the verdict and how many policies are involved; click it and the comparison the finding exists for unfolds — every policy side by side with its value and its reach, filtered and tenant-wide tags included. Open folds survive a re-render. Every caveat is unchanged: a verdict is about group targeting, may is may, redacted values are never compared, and unread surfaces are unknown rather than clean." },
      { kind: "fixed", tool: "⚡ Setting conflict scan", text: "The tool's suite carried the same rot the compliance report's did — four assertions pinned to its birth build, 10382, failing on every build since. De-pinned to derive from live values, and the suite gained the layout checks: cards, folds closed at first, the side-by-side unfold, and folding back." },
    ],
  },
  {
    build: 10417, date: "2026-08-25", title: "Roles become cards you open",
    items: [
      { kind: "improved", tool: "🛡 Role assignments", text: "Third of the four 10413 conversions. Stat cards up top — roles with the built-in and custom split, assignments, members, empty roles, observations, and could-not-be-read when it applies — and each role is now a folded card. Click it and the assignments unfold in place, each with its scope, its tags and its member table; one long page per role was the layout this retires. Open folds survive a re-render because the tool remembers role ids, not rows." },
      { kind: "improved", tool: "🛡 Role assignments", text: "The empty-role toggle inverted, deliberately. Empty roles used to hide behind a show-them checkbox; now they are in the list by default, folded and dimmed — a role with nobody in it is one membership change from being a live grant, and a dimmed fold costs almost nothing to scroll past. The checkbox now hides them, for tenants where fourteen idle built-in roles are noise. Observations keep their no-score rule, and the Intune-RBAC-only warning keeps its place at the top." },
    ],
  },
  {
    build: 10416, date: "2026-08-25", title: "The health findings become one list you can open",
    items: [
      { kind: "improved", tool: "🩺 Assignment health", text: "The second of the four 10413 conversions. Six stat cards, one per finding kind — empty groups, dangling, unassigned, excluded-only, include-and-exclude, failing — and they are buttons: click a kind and the list narrows to it, click again and everything returns. When deployment status was not checked, its card says so instead of showing a zero that reads as health." },
      { kind: "improved", tool: "🩺 Assignment health", text: "The five stacked tables become one flat list of folded findings, the badge saying the kind. A finding unfolds in place to its evidence — which group, which id, and what the finding means alongside what it does not: an empty group is distinct from a deleted one, and whether an empty landing zone is a mistake is still yours to judge. Open folds survive both a re-render and the kind filter, because the tool remembers finding keys rather than rows. Every unknown-not-clean rule is unchanged." },
    ],
  },
  {
    build: 10415, date: "2026-08-25", title: "The compliance report gets the timeline treatment",
    items: [
      { kind: "improved", tool: "📋 Compliance report", text: "The first of four tools taking the change audit's 10413 layout, from the mockup round. The estate strip becomes five stat cards with percentages — compliant, non-compliant, in grace, stale, unknown — and the stale card carries its own tension on its face: the devices that count as compliant AND are stale are named right there, because that number is the reason the column exists." },
      { kind: "improved", tool: "📋 Compliance report", text: "The policy table becomes folded rows. Closed, a row says the policy's name, whether it is failing and how badly, and whether it is unassigned or has a status gap. Click it and the full rollup unfolds in place — compliant, non-compliant, error, conflict, pending, not-applicable — with the failing settings worst-first underneath. Open rows stay open when the list redraws, because the tool remembers which policies you opened rather than which rows were expanded. Everything read is unchanged, and unknown is still not clean." },
      { kind: "fixed", tool: "📋 Compliance report", text: "The tool's own test suite had rotted the way t01-smoke once did: four assertions pinned to build 10383 — the build number itself, the cache-busting, the changelog head, the queue item — failing on every build since. They now derive from the live values and assert the property, and the suite gained the layout checks: five cards, folds closed at first, unfold in place, and the stale-card tension." },
    ],
  },
  {
    build: 10414, date: "2026-08-24", title: "The home screen stops being a wall",
    items: [
      { kind: "improved", tool: "All tools", text: "The tool cards were enormous, and the cause was not the card. Its styling is ENCA's rule byte for byte \u2014 what differed was the writing. ENCA's descriptions run seventeen to sixty words; ours ran a hundred and eleven to a hundred and seventy-five, and because a row of cards takes the height of the tallest one, a single long description inflated everything beside it. The card now shows the first four lines. The full text stays where it was, so Help and the exports still have all of it, and the next long description written will be clipped the same way rather than reopening the question." },
      { kind: "new", tool: "All tools", text: "Sections with more than four tools collapse, with a button that says how many are hidden \u2014 ENCA's, ported. The rule worth knowing: a tool that has just shipped or just changed is never behind the fold. NEW, BETA and UPDATED cards take the visible places first, ranked by which was changed most recently rather than by where they happen to sit on the page, so a tool touched in this build outranks a BETA tag that has been there for weeks. When more are flagged than there is room for, the button counts the ones still hidden instead of burying them quietly. Expanding restores the original order exactly." },
      { kind: "fixed", tool: "\ud83e\ude7a Assignment health", text: "The surface names ran underneath their own tick boxes. The box is sixteen pixels wide and sits twelve from the edge, so it occupies twenty-eight pixels of it \u2014 and the label was reserving twenty-two. Six pixels of overlap, on every name long enough to reach the end. The same six pixels are in the tool this was inherited from; it has simply never had a label long enough to show them." },
    ],
  },
  {
    build: 10413, date: "2026-08-25", title: "The change audit reads like a timeline",
    items: [
      { kind: "improved", tool: "🕓 Change audit", text: "The screen is rebuilt; what is read is untouched. Four cards answer the first questions before any scrolling — how many changes, who made most of them, which area moved most, and the failure rate — computed from the rows currently shown, so narrowing a filter moves the cards with it. Below them the events run down a timeline, each with a coloured dot and a badge for what kind of operation it was: created, deleted, updated, assigned, or an action. The layout went through a two-option mockup round first and this is the option picked." },
      { kind: "improved", tool: "🕓 Change audit", text: "Click an event and the detail unfolds in place: the actor with their IP and kind, the result and severity, the correlation id for a support case, the resource ids, and the full field-level diff of what moved. Several events can be open at once, and the open ones stay open when a filter redraws the list, because the tool remembers which events you opened rather than which rows were expanded." },
      { kind: "improved", tool: "🕓 Change audit", text: "The window control became a proper range picker: the presets from before, plus a custom day range for 'what happened while I was away'. A range reaching past the thirty-day retention is clamped to the floor and says so on the result — a range that quietly shrank would read as a quiet tenant, which is this tool's cardinal sin. And a picked day means the whole day: the range runs to the end of the chosen date, because 'up to Tuesday' that silently excludes Tuesday is how date pickers lie." },
      { kind: "improved", tool: "🕓 Change audit", text: "The filters moved into a dialog and grew two controls: an operation-type filter and an actor dropdown. Category, operation and actor lists are all built from what the tenant actually emitted in the window — the rule this tool has had from the start — and the wildcard search boxes survive unchanged for the cases a dropdown cannot say. The filter button shows how many filters are active, so a narrowed list cannot silently pass for the whole answer. Everything remains in this tab: the dialog and the picker are part of the page, filters run over rows already read, and nothing is stored anywhere." },
    ],
  },
  {
    build: 10412, date: "2026-08-24", title: "The tick finds its corner",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The manual-mark tick on the loop strip hung past the station's right edge; it now sits tucked inside the corner where a corner control belongs." },
    ],
  },
  {
    build: 10411, date: "2026-08-24", title: "The loop strip takes manual marks",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Some stations on the audit loop can never light themselves — the portal edit above all, because a browser tab cannot see what happened in another blade. Those stations now carry a small tick: mark the work done by hand and the loop moves on. Three rules keep the marks honest. Evidence beats the mark, in both directions — a station the session can verify ignores it, and the Gaps station with open gaps stays amber and offers no tick at all, because two open gaps do not close by declaration. A hand-marked station renders with a dashed border and says marked by you, so a claim never dresses up as evidence. And marks persist per browser, because the profile you edited in the portal yesterday is still edited after a refresh — un-tick to take the claim back." },
    ],
  },
  {
    build: 10410, date: "2026-08-24", title: "The audit loop, drawn on the tool",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The page reads 1 to 5, top to bottom — but the work is a circle: scan, build, deploy the audit profile, collect the fleet's events, find the gaps, close them, update the profile, collect again, and only then enforce. That circle was visible only to someone who already knew it. A new strip above step 1 draws it: seven stations, each lit from what is actually loaded or known to be in the tenant this session — the scan bundle lights Scan with the device's name, rules on the table light Build, a created or found audit profile lights Deploy, an uploaded events bundle lights Collect, and the Gaps station carries the live count from the same classification pass as the evidence card, amber while gaps are open and green at zero. The first unlit station wears a ring: that is where you are, and clicking any station scrolls to its part of the page." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Two kinds of honesty are built into the strip. The Update station never claims to be done, because editing the profile in the tenant happens in the portal and a browser tab cannot see it — the strip says so instead of guessing. And the loop-back note states the exit condition in one line: collect, gaps, update repeats until a full window shows zero gaps, which is exactly the evidence the Enforce gate reads. The strip collapses to a one-line summary and remembers the choice per browser; every color is a theme variable, so light, dark and auto all come for free. Layout chosen by mockup round — the live strip won over a Help-only walkthrough." },
    ],
  },
  {
    build: 10409, date: "2026-08-24", title: "The gap report — and the fixes that close it",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The fleet events card now judges, counts, and reports. Every denied file in an uploaded events bundle is classified against the draft on screen: a GAP (would still be blocked, lives in machine space — needs a decision before you enforce), BLOCKED BY DESIGN (ran from a user-writable area — that is the policy working), COVERED (the draft already allows it, so the block belonged to the old policy), or UNDECIDED (the draft has no rules for that type). The counts sit above the table and cover every distinct file, not just the fifty shown — and the new Gap report button downloads the same judgement as a Markdown document for the change ticket: summary, each section in detail, a suggested fix per gap, and the collector's own blind spots listed at the end." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "Gaps close from the card. Each gap row carries a fix button that adds the rule to the draft through the normal undo path — allow by PUBLISHER when the event carries a signer, allow by HASH from the event's own FileHash when it does not (the rule's description says hash rules go stale on the file's next update), and an exact path only as a last resort, with instructions to replace it. Never a directory: a directory allow built from event evidence is how the hole gets rebuilt. Rows blocked by design get the same mechanics behind a deliberately cooler button — Allow anyway — because closing one of those is a business decision, and the button should read like one. The verdict matcher also learned to see hash rules for event rows, so a gap closed by hash actually reads as closed." },
    ],
  },
  {
    build: 10408, date: "2026-08-24", title: "A self-hosted instance can wear its own name",
    items: [
      { kind: "new", tool: "TUNO", text: "Self-host branding, ported whole from the sister tool: a gear button beside Sign out, on any host that is not production. Product and organisation names, logos, login text and light and dark identity colours — chrome only, so every export keeps the neutral TUNO credit whoever's colours the screen wears. Apply keeps the look in this browser; Download produces a selfhost-branding.json that, served next to index.html, gives every visitor the branding and softens the red BETA ribbon to a neutral SELF-HOSTED one — a deliberately configured instance is not a test site, but it must still never be mistakable for the production deployment. Import reads a file back into the form for review before anything applies. The one thing the gear will never configure is the canonical host: that drives the production check and the export credit, and a settings dialog must not be able to make a copy claim to be production." },
      { kind: "new", tool: "TUNO", text: "A branded instance paints its own look first. A small blocking script in the page head reads the brand this browser already knows and injects the palette, logo and favicon before anything renders, so a refresh no longer flashes the default look for a moment. The very first visit in a fresh browser still flashes once — that is the floor for a static site — and the authoritative branding pass takes over the moment the app scripts arrive." },
    ],
  },
  {
    build: 10407, date: "2026-08-24", title: "The header stops repeating itself",
    items: [
      { kind: "fixed", tool: "All tools", text: "The header carried the production address next to the product name, which said the same thing twice on production \u2014 and something worse on the beta channel. The address shown was always the PRODUCTION one, whichever site you were actually looking at, so the beta site displayed a hostname it is not served from. Removed. The name is on the badge, the channel is on the ribbon, and the address is still on the footer stamp and in every export credit, where it belongs." },
    ],
  },
  {
    build: 10406, date: "2026-08-24", title: "Sixth promotion — the console shell and three writers are production",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves to build 8 and takes queue items 44, 58, 59 and 63 to 67 with it — the console shell with its sidebar, the Assignment editor at v0.5 with the Toolkit parity work and today's layout fixes, Assignment filters with the gated delete, restore inside the backup tool, the App Control events loop, tenant autocomplete, the Intune RBAC rename and the progress card. The second partial promotion: items 41 to 43 (what-if, health, settings search) and 60 to 62 (the assignment matrix, the setting conflict scan, the compliance report) stay on this channel until each has run against a real tenant — they are the whole of the next promotion." },
      { kind: "improved", tool: "All tools", text: "On main the held tools' files, tiles, screens and help are absent; the Group Analyzer keeps its pre-matrix form there with only the progress-card delegation carried across, and the documenter goes without the filter fields that belong to the conflict scan. The nine PowerShell scripts carry build 8 in production and name the production site as their home. The queue here drops to six items, productionBuild reads v1.0.8, and the roadmap cards for the editor, the filters and restore now carry both build numbers." },
    ],
  },
  {
    build: 10405, date: "2026-08-24", title: "The editor's screen stops fighting its own layout",
    items: [
      { kind: "fixed", tool: "✏️ Assignment editor", text: "Before a read, the reading progress card rendered 230 pixels wide and hard against the left edge. The surface rail's grid column was the culprit: the rail is hidden until the policies are read, and a hidden grid item leaves its 230-pixel column to whoever comes next — which was the list, progress card and all. The split is single-column until the rail actually exists, so the card now sits centred where every other tool puts it." },
      { kind: "fixed", tool: "✏️ Assignment editor", text: "The floating selection bar is a pill sized to its controls, centred over the content column — not a full-width plank with the close button adrift in the middle and dead green stretching to the right edge. Its left and right bounds keep doing their real job of clearing the sidebar in both states, and below the sidebar breakpoint the bar centres over the whole window." },
    ],
  },
  {
    build: 10404, date: "2026-08-24", title: "The editor learns the Toolkit's other targets",
    items: [
      { kind: "new", tool: "✏️ Assignment editor", text: "The selection bar grows the Intune Toolkit's remaining targeting: a target switch — Group, All devices, All users — and an assignment-filter dropdown fed by the Assignment filters tool's own list. A tenant-wide plan says what it is in capitals: the whole tenant, every device or user it has now and every one it gains later. Adding an exclusion of a tenant-wide target is refused, because Graph has no such thing — an exclusion names a group, and pretending otherwise would plan an operation the tenant cannot hold. Filters ride on adds only, with their include or exclude mode; a removal takes the whole assignment, filter included." },
      { kind: "improved", tool: "✏️ Assignment editor", text: "The group-read permission now rides along with the read-policies click, so the group box suggests from the tenant from the first keystroke rather than after the first dry run. The controls follow the choice: pick a tenant-wide target and the group box steps aside, pick remove and the filter dropdown does." },
    ],
  },
  {
    build: 10403, date: "2026-08-24", title: "The surface rail actually sticks",
    items: [
      { kind: "fixed", tool: "✏️ Assignment editor", text: "The surface rail was designed to stay put while the policy list scrolls, and never did: the card it lives in clips its overflow, and a clipping ancestor becomes the box a sticky element sticks inside — the rail was pinned to something that does not scroll, which is pinned to nothing. The editor's card now lets its overflow show, the rail stays visible for the whole scroll, and a rail taller than the window scrolls within itself rather than losing its bottom entries." },
    ],
  },
  {
    build: 10402, date: "2026-08-24", title: "The bar's group box suggests upwards, and looks like a field",
    items: [
      { kind: "fixed", tool: "✏️ Assignment editor", text: "Autocomplete in the selection bar's group box looked broken and was not: the suggestion menu always opened downwards, and from a bar at the bottom of the viewport that put the answers off-screen — the read had happened, the menu was simply invisible. The menu now flips above its anchor whenever there is no room below, everywhere the shared typeahead is used. And the group box itself takes the app's own field look — the same input every tool has, the what-if's included — because a field should look like every other field wherever it stands; only the operation switch and the buttons wear the bar's warm colours." },
    ],
  },
  {
    build: 10401, date: "2026-08-24", title: "The selection bar dresses for the light",
    items: [
      { kind: "fixed", tool: "✏️ Assignment editor", text: "In light mode the selection bar's controls rendered as bare white slabs on the deep green — holes in the bar rather than parts of it. They now wear the AppLocker tool's warm family: the cream of the Let-TUNO-do-it panel in light mode, the deep amber the bar already had in dark, because those variables flip per theme on their own. The active operation and the dry run button are lemon — the same accent ENCA's selection bar has always used on this green." },
    ],
  },
  {
    build: 10400, date: "2026-08-24", title: "The editor's operation follows the selection",
    items: [
      { kind: "improved", tool: "✏️ Assignment editor", text: "The operation controls left the top of the card for a floating bar at the bottom of the screen — ENCA's selection bar, floated — that exists exactly while something is ticked. It carries the count, the include/exclude/remove switch, the group box and the dry run, because the operation belongs to the selection and an empty selection has nothing to operate on. A cross clears the selection and the bar leaves with it; the policy list keeps its full height; and the filter stays where filters live, at the top. Chosen over a sticky top bar after a mockup round." },
    ],
  },
  {
    build: 10399, date: "2026-08-24", title: "One width per screen, again",
    items: [
      { kind: "fixed", tool: "TUNO", text: "Every tool's intro card stopped at the old 1180-pixel width while the cards under it ran the full 1500-pixel column — two widths on one screen reads as a bug, which is the same lesson the wide-shell experiment taught back in build 10321. The intro card now matches its neighbours in every tool; the paragraphs inside it keep their measure, because a line of text the full column wide is not more readable, it is less." },
    ],
  },
  {
    build: 10398, date: "2026-08-24", title: "A stale device answers to a click",
    items: [
      { kind: "improved", tool: "📈 Compliance report", text: "A device in the stale table is now a link: clicking it opens the Device analyzer on that machine — the question a stale row raises is exactly the one that tool answers, why does this device have what it has and when did it last say so. The click goes through the tool's own front door, so the tab bar and the sidebar follow along." },
    ],
  },
  {
    build: 10397, date: "2026-08-24", title: "Reading looks like reading — ENCA's progress card, everywhere",
    items: [
      { kind: "improved", tool: "TUNO", text: "Every tool's read now shows what ENCA shows: a centred card in the middle of the result area — spinner, the current step, and a progress bar that fills when the tool knows how far it is (a pooled per-policy read, a batched sweep) and sweeps when it only knows the step. One shared implementation behind all fourteen tools, because fourteen hand-rolled progress cards is how fourteen subtly different ones happen. Two rules ride it: results are never covered — a tool re-reading over an existing answer keeps its quiet text line instead of painting a spinner over what somebody is reading — and the restore keeps its line-only progress on purpose, because its plan table must stay visible while an apply walks it." },
    ],
  },
  {
    build: 10396, date: "2026-08-24", title: "Stacked cards get their gap back",
    items: [
      { kind: "fixed", tool: "TUNO", text: "Tools that stack result cards — the conflict scan's findings above all — rendered them edge to edge, and a run of cards with no gap reads as one long wall. Like the padding fix before it, the gap is now a convention over every tool body rather than something each tool remembers: any stacked cards separate, in every tool that exists and every one that comes." },
    ],
  },
  {
    build: 10395, date: "2026-08-24", title: "Card padding is a rule, not a list",
    items: [
      { kind: "fixed", tool: "TUNO", text: "Result cards in several tools rendered their text flush against the card border — the padding rule was a hand-kept list of six tool bodies, and every tool added since fell outside it: Assignment what-if, Assignment health and Settings search shipped that way unnoticed, then the conflict scan, the compliance report and Assignment filters repeated it. The rule now matches every tool body by convention rather than by name, so the next tool cannot repeat it either." },
    ],
  },
  {
    build: 10394, date: "2026-08-24", title: "The references count answers back",
    items: [
      { kind: "improved", tool: "🧩 Assignment filters", text: "After a usage scan, the references chip is a filter: click it and the list narrows to only the filters something actually uses; click again and everything returns. And a row's used-by count expands in place into the references themselves — which policy, on which surface, include or exclude, and the filter mode — instead of hiding them in a hover title. Both exist only once usage has been scanned, because a filter on an absent column would be a filter on nothing; after any write the scan drops back to absent and both reset with it." },
    ],
  },
  {
    build: 10393, date: "2026-08-24", title: "Everything fits the card",
    items: [
      { kind: "fixed", tool: "🧩 Assignment filters", text: "The filter table fits its card instead of scrolling sideways: the columns have real widths now, a rule wraps at any character rather than pushing the Edit and Delete buttons off the edge, and the actions stay pinned right. The same fit pass covers the conflict scan's value tables and the restore picker — a long value wraps inside its column, and nothing forces the page wide." },
    ],
  },
  {
    build: 10392, date: "2026-08-24", title: "Role assignments grows into Intune RBAC",
    items: [
      { kind: "improved", tool: "🛡 Intune RBAC", text: "The tool formerly titled Role assignments is now Intune RBAC, and role assignments is its first section — because the tool is the RBAC surface, not one question about it. The section switch at the top is where scope tags, custom-role review and whatever else belongs to Intune RBAC will land, each as a section here rather than a new tile: one tool per surface, sections per question. Everything the tool did is unchanged, including the line that matters most — this is Intune RBAC only, and the Entra directory roles that also grant full Intune access are not in it." },
    ],
  },
  {
    build: 10391, date: "2026-08-24", title: "The collapsed rail peeks open on hover",
    items: [
      { kind: "improved", tool: "TUNO", text: "Hover the collapsed sidebar and it expands in place — as an overlay, so the page behind it does not move — showing the full names; move away and it folds back. Picking a tool while peeked collapses it again, because the peek is a glance, not a state change: the rail stays collapsed until the chevron says otherwise. A short delay keeps a cursor merely passing on its way to the content from flaring it open." },
    ],
  },
  {
    build: 10390, date: "2026-08-24", title: "The Assignment editor takes the Toolkit's shape",
    items: [
      { kind: "improved", tool: "✏️ Assignment editor", text: "The list works the way the Intune Toolkit's does: surfaces on the left — a sticky rail with All on top, a count per surface and a live tick-count beside it, unreadable surfaces named under the rail — the filter on top, and the policy list beside. Click a surface to see only its policies; the filter narrows within it." },
      { kind: "fixed", tool: "✏️ Assignment editor", text: "The selection now survives everything, which it quietly did not: ticks used to live only in the checkboxes, so every filter keystroke re-rendered the table and silently dropped every one — and a surface rail would have made picking across surfaces impossible. The tick-set lives in the tool now: checkboxes render from it and write into it, switching surfaces and filtering keep every tick, ticking a row no longer re-renders the table (the scroll position survives bulk ticking), and a line under the list counts the selection across all surfaces. A fresh read clears it, because a fresh read is a fresh decision." },
    ],
  },
  {
    build: 10389, date: "2026-08-24", title: "Collapsing the sidebar no longer moves the page",
    items: [
      { kind: "fixed", tool: "TUNO", text: "The content column now centres itself in whatever space the sidebar leaves, in both states. Build 10380 anchored the column against the expanded sidebar, so folding the rail slid the whole page left and left the right side gaping — the collapse read as a relayout rather than a rail folding. Now expanded and collapsed land within a few dozen pixels of each other on any monitor: the same page, with a narrower rail. The column still can never slide under the rail, and below 1240 pixels nothing changes." },
    ],
  },
  {
    build: 10388, date: "2026-08-24", title: "Inputs that name the tenant now ask the tenant",
    items: [
      { kind: "new", tool: "TUNO", text: "Every input that names a directory object now suggests from the tenant as you type: the Group Analyzer's group, the what-if's subject and groups — the subject following the User/Device switch — the compare box completing one line at a time, the Assignment editor's group, and the Device analyzer's device name. One shared implementation, because six copies of a dropdown is how six subtly different dropdowns happen. Picking fills what the tools already resolve: a group or device by its display name, a user by UPN, because two people can share a display name. Suggestions are server-side starts-with — what every directory object supports — so a serial number or GUID still works typed in full, as the placeholders always said." },
      { kind: "new", tool: "TUNO", text: "The consent rule is unchanged: nothing is read on a keystroke unless the permission is already in hand this session. An input whose permission has not been granted shows a single row offering to enable suggestions, and the read happens on that click — never as a side effect of typing. A failed suggestion is silence, not an error card, and ignoring the whole feature costs nothing: typed names resolve exactly as before." },
    ],
  },
  {
    build: 10387, date: "2026-08-24", title: "The sidebar folds to an icon rail",
    items: [
      { kind: "improved", tool: "TUNO", text: "The sidebar gained its collapse: the chevron at the top folds it to a 56-pixel icon rail and the content takes the room. Hover an icon and the tool's full name appears — every button carries its label natively, which is deliberate: a styled tooltip inside a scrolling column would clip at the edge, and a name you cannot read is worse than a plain one you can. Section headings become divider lines, so the grouping survives losing its words; the active highlight stays visible on the rail; clicking an icon opens the tool exactly as the full-width item does. The choice persists across refreshes the same way the theme does, and a browser that cannot remember simply opens expanded. Below 1240 pixels nothing changes — the sidebar already steps aside there in either state." },
    ],
  },
  {
    build: 10386, date: "2026-08-24", title: "The events collector survives its first real machine",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Get-TunoAppControlEvents.ps1 (now 1.0.2) crashed on its first real run with PowerShell 5.1's dictionary-binder defect — the same 'Argument types do not match' that hit the scan script twice, and it took two rounds to kill here because it has more shapes than the house discipline covered. The first crash was in the ordered-dictionary handling the cast rule was written for; the fix for that added a crash reporter that names the failing line, and the very next run pointed it at something the rule did not cover: dot access on a plain hashtable goes through the same broken binder. The discipline is now total and written into the script: no ordered dictionaries anywhere, every object that gets dot-accessed is a pscustomobject — the shape the scan bundle has always used, on the same machine, without incident — and dictionary writes go through the Add method. The crash reporter stays: it turned a useless position-1,1 error into a one-paste diagnosis, and every future house script starts with one." },
      { kind: "fixed", tool: "TUNO", text: "The scripts' build stamps caught up: builds 10379 through 10385 bumped the site build without bumping the TunoBuild constant the scripts carry, so a freshly downloaded script introduced itself as five builds older than the site that served it. All nine now say 10386, and the guard that should have said so runs before every commit again." },
    ],
  },
  {
    build: 10385, date: "2026-08-24", title: "Restore — the backup tool learns the other direction",
    items: [
      { kind: "new", tool: "📦 Backup configuration", text: "R09 ships inside the backup tool, because the file you load is the file the other half wrote — and the original PowerShell script's archives load too, since the folder layout is shared in both directions. Load a zip, pick a single policy, a selection or everything, dry-run, apply. The dry run lists every operation including the administrative-template definition-value child writes the original's WhatIf never shows, and every create is verified by reading the object back: created is the tenant's word, not the request's status code." },
      { kind: "new", tool: "📦 Backup configuration", text: "TenuVault's two rules arrive exactly: create only — nothing of yours is patched or deleted, with the single exception that an administrative template this restore half-created and failed to finish is rolled back as its own mess — and every object gets an editable name prefix, Restored by default. The collision stop sits on top: a name that already exists in the tenant skips that object, checked at dry run and re-checked fresh immediately before each create. Two deliberate departures from the roadmap card, both said on it: overwrite is not offered at a collision, because overwrite is a patch wearing a restore's clothes; and assignments are not restored at all — a source-tenant group GUID either does not exist in the target or names a different group, so everything arrives unassigned and the result says so per row. Scripts without bodies cannot be selected; secrets arrive as references to re-enter, as the manifest always warned." },
    ],
  },
  {
    build: 10384, date: "2026-08-24", title: "Assignment filters — visible at last, and deletable only when safe",
    items: [
      { kind: "new", tool: "🧩 Assignment filters", text: "R21 ships as T14, both halves. The read half shows what the portal does not: every filter with its platform and rule, and — as its own optional read — which assignments on which policies reference it, scanned through the Group Analyzer's own surface table so a surface added there is automatically counted here. Until the scan runs the used-by column is absent, not zero, and a surface that could not be read makes every zero a floor, which the screen says plainly." },
      { kind: "new", tool: "🧩 Assignment filters", text: "The write half is the third thing in TUNO that writes, under the Assignment editor's rules sized for one object: the write permission asked at the click, an edit preceded by a fresh read and refused as drifted if anybody changed the filter since it was loaded, every write verified by reading it back, and delete confirmed by typing the filter's name. The rule the tool exists for: delete is refused while any assignment references the filter — verified against the tenant immediately before the act, not against the session's cached scan, with 'could not check' refusing too — because a filter deleted while in use silently widens every assignment that carried it to the whole target group. After any write the cached usage scan is dropped rather than trusted stale." },
      { kind: "improved", tool: "TUNO", text: "The Graph layer gains PATCH and DELETE, arriving with the tool that needed them and under the same rule writes have always had: no retry, ever — an ambiguous timeout is reported as ambiguous, not resent — and a refusal comes back in the tenant's own words." },
    ],
  },
  {
    build: 10383, date: "2026-08-24", title: "The compliance report — the estate on one page",
    items: [
      { kind: "new", tool: "📈 Compliance report", text: "R22 ships as T13: the tenant's compliance story on one page instead of a morning of portal blades. The estate rolled up — compliant, non-compliant, in grace, unknown — every compliance policy with its verdicts worst first, and which setting is failing where Graph keeps a per-setting summary, read as two cheap rollups per policy folded into batch requests. Stale is its own column, not a flavour of compliant: the threshold is on screen, default 30 days, a device that has never synced counts as stale, and a compliant-but-stale machine sits in both columns with the tension stated, because a compliance verdict is exactly as old as the check-in that produced it." },
      { kind: "new", tool: "📈 Compliance report", text: "The honesty rules ride along: the numbers are Graph's cheap per-policy rollup, refreshed on Intune's schedule rather than live, and the page says so; a policy whose statuses could not be read is listed with a status-gap tag rather than dropped, because unknown is not clean; an unassigned compliance policy is flagged as evaluating nobody; and the per-device answer for one machine is named as the Device analyzer's job rather than half-done here. No new permission — the estate rides the device read that came with the Device analyzer, the policies ride the config read. Exports Markdown, CSV, and a stale-devices CSV with every stale machine, oldest first." },
    ],
  },
  {
    build: 10382, date: "2026-08-24", title: "The setting conflict scan — value-shaped contradictions",
    items: [
      { kind: "new", tool: "⚔️ Setting conflict scan", text: "R20 ships as T12: the same setting configured to different values in two policies, where Assignment health finds the assignment-shaped contradictions. The read is the documenter's own collect — same three configuration surfaces, same redaction, same honesty about what could not be read — because a second copy of that reading is how two tools start disagreeing about one tenant. Identity is exact or it is not claimed: definition id in the settings catalog, definition plus category in administrative templates, and the typed property within one policy type for device configurations. A collision that spans surfaces — a catalog policy and a legacy device configuration driving the same CSP — is stated as not detected rather than guessed at, which is a deliberate departure from the roadmap card's first draft, said on the card." },
      { kind: "new", tool: "⚔️ Setting conflict scan", text: "Every finding carries a verdict computed from the assignments, because a collision is only a conflict if the two policies can meet on a device: can collide when they share an included group or a tenant-wide target meets anything with reach; may collide when the groups differ, because different groups can share members and a browser cannot see membership; cannot collide only where one side reaches nobody by construction — no include, no tenant-wide target. An assignment filter caps any verdict at may, and the filter id now rides along on the documenter's assignment rows for exactly that. Agreement is not a finding, redacted values are never compared, and the worst verdicts sort first. Exports Markdown and CSV." },
    ],
  },
  {
    build: 10381, date: "2026-08-24", title: "The assignment matrix — the sweep's second face",
    items: [
      { kind: "new", tool: "🔗 Group Analyzer", text: "R19 ships, the first of the IntuneShade cards. The tenant sweep now has two faces over its one read: the ranked table it always had, and the assignment matrix — every group down the side, every surface across the top, one grid instead of one portal blade per policy. Switching between them re-renders what was already read and asks the tenant nothing. A cell is a claim and the grid does not overstate it: includes are the count, exclusions are their own red minus-n per surface and are never folded in, because a policy that deliberately does not reach a group drawn as coverage is the exact overstatement a matrix invites. Dangling references keep their deleted flag instead of counting as coverage, and clicking a group opens it in single-group mode through the tile's own pipeline." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "The empty-group check is opt-in and absent-not-empty: a button that names its cost runs one single-member membership probe per group, batched twenty a trip — the same technique Assignment health uses — and until it runs the grid says plainly that empty and populated look the same. After it, a group nothing can reach says empty, and a probe that failed says reach-unknown rather than being passed off as either. The matrix draws from the sweep's own numbers, so the table view and the three exports are unchanged." },
    ],
  },
  {
    build: 10380, date: "2026-08-24", title: "The console shell — a sidebar, and room to breathe",
    items: [
      { kind: "new", tool: "TUNO", text: "Signed in, TUNO now reads like the consoles it sits beside: a fixed sidebar on the left carries every tool, grouped exactly as on the home grid — Overview on top, then Endpoint security, Assignments and scope, Configuration, Monitoring, Access and roles, and the app's own pages — and the working screen sits beside it. The sidebar is built at sign-in by walking the home grid itself, so there is no second copy of the grouping to fall out of step, and the labels come from the tool list rather than being scraped from the tiles, which is the lesson the old tab strip taught. The tab bar stays, because the two do different jobs: the sidebar is where you can go, the tabs are what you have open — and the sidebar's highlight follows the active tab, so the two can never disagree about where you are." },
      { kind: "improved", tool: "TUNO", text: "The content column grows from 1180 to 1500 pixels, because the old width was sized for a shell with nothing next to it — the assignment tables, the audit diffs and the documenter's list all get the room they were asking for. One width for every signed-in screen, still: moving between tools does not change the app's size, which is the rule from build 10321 unchanged. Below 1240 pixels the sidebar steps aside entirely and the shell returns to the centred column — the tab bar and the home grid keep every tool reachable, so the sidebar is a convenience, never the only door. The sign-in screen keeps its centred card; none of this exists before authentication." },
    ],
  },
  {
    build: 10379, date: "2026-08-24", title: "Five roadmap cards from the IntuneShade comparison",
    items: [
      { kind: "new", tool: "🗺 Roadmap", text: "Five cards join the roadmap, after a feature-by-feature comparison with Alper Atar's IntuneShade (MIT) — a React console covering much of the same ground. Where the two overlap, TUNO's tools go deeper; these five are what it genuinely has that TUNO does not. In Next: R19 Assignment matrix, a groups-by-surfaces grid rendered from the tenant sweep R04 already runs, zero new reads; R20 Setting conflict scan, the same setting configured to different values across policies — with one step past the original, because a collision only matters if the two assignment sets can meet on a device; R21 Assignment filters, the per-filter usage view and later the create/edit/delete, with delete refused while anything still references the filter; R22 Compliance report, the estate-wide rollup on a scope R03 already brought in. In Later: R23 AI policy analysis on a bring-your-own key — without the server the original needed, with redaction before anything leaves the browser, and with the new-origin decision named as the R18-shaped question it is. The script generator is deliberately not coming. IntuneShade joins the credits." },
    ],
  },
  {
    build: 10378, date: "2026-08-24", title: "The fleet's events come home — and T01 reads them",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "A third script set joins step 1: App Control events collection. Deployed as an Intune Remediation (the third pair in the deploy panel), it harvests the CodeIntegrity log and all four AppLocker logs from every device in the assigned ring — per-event-ID CSV and XML for digging, an HTML report for reading, and a JSON events bundle for this tool. The report and the bundle are written into the Intune Management Extension logs folder with a .log name on purpose: Collect diagnostics gathers *.log from exactly there, so the harvest rides home on a mechanism the tenant already has. For the impatient there is a Live Response companion that zips the newest report and bundle and prints the path ready for getfile. The detection half always reports non-compliant, deliberately — the remediation IS the collection — and everything that describes the pair says plainly what that does to the console numbers." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "Upload the bundle a device produced and a new evidence card appears: every blocked or audited event in the window, aggregated per file with counts and affected users, judged against the policy draft ON SCREEN — with the same matcher the coverage table uses, publisher rules, exceptions and deny-beats-allow included. Each row gets a recommendation: covered (the draft already allows it, the block belonged to the old policy, nothing to do), stays blocked by design (it ran from a user-writable area, which is the policy working), or likely a missing rule (signed, in machine space — allow it by publisher, never by a path into a profile). The bundle never touches the loaded policy: it is evidence about the draft, and the verdict column recomputes live as the draft is edited. WDAC 3076/3077 counts ride along, so the card also says whether App Control for Business is in the picture." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The set replaces the CloudFlows Remedate_ACB scripts, and the review that produced it found three defects worth naming. The MSI and Script log was queried without event IDs 8005 and 8007 — an ENFORCED policy blocking an installer left no evidence at all, in a collector whose whole job is evidence. A global SilentlyContinue preference made every try/catch downstream dead code, so a broken query reported a clean log. And the CodeIntegrity query passed 48 event IDs in one filter, which Windows compiles to an XPath with a hard limit of about 23 comparisons — the query threw on the machines it mattered on, and the dead catch blocks swallowed the throw. The new collector chunks its queries, runs under Stop, and says in its own header why." },
    ],
  },
  {
    build: 10377, date: "2026-08-24", title: "Fifth promotion — everything but the four new tools is production",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves to build 7 and takes queue items 36 to 40, 45 to 52 and 54 to 57 with it — beta builds 10350 to 10356 and 10361 to 10376, with item 53 having been retired into 57. The first PARTIAL promotion: items 41 to 44, the four new tools — Assignment what-if, Assignment health, Settings search and the Assignment editor — stay on this channel deliberately, each until it has run against a real tenant. Production gains the whole AppLocker migration workflow: the cleanup pair, the IT-TOOLS provisioning pair, both deployable from the browser as Intune Remediations, the carry-over check, the grouping correction, the house convention, the scan's PowerShell 7 and long-path fixes — plus the What's new dialog, the header tenant box with its Sign out button, and the home-tenant badge." },
      { kind: "improved", tool: "All tools", text: "With the second write scope now in production, an existing app registration needs it added and admin-consented before the Remediation buttons work there — the registration script and the security documentation carry it, and the deploy click explains itself if the scope is missing. The production copies of all six PowerShell scripts carry build 7 and name the production site as their home." },
      { kind: "improved", tool: "All tools", text: "The production-side check learned what a partial promotion is: an item left in the queue must name at least one file that genuinely differs from beta — an item whose files all match beta shipped and should have been removed — and a roadmap card may read as live on beta only while the build it names still belongs to a queued item. The queue here now holds exactly the four new tools; they are the whole of the next promotion." },
    ],
  },
  {
    build: 10376, date: "2026-08-24", title: "There is always a sign-in",
    items: [
      { kind: "fixed", tool: "TUNO", text: "Every entry to the app is a real interactive sign-in again — ENCA's model, restored verbatim. Builds 10361 and 10371 had softened this in two steps: first the account chooser was removed so a live browser session could be reused silently, then a refresh started restoring the cached session so the Sign in click dropped you straight into the app with no prompt at all. That made entry a click-through rather than an authentication, and the rule is the opposite: a tool that can write to a tenant authenticates you every time you enter it, with the identity provider re-running its whole policy — multi-factor prompts included, which is the accepted cost and how the sister app has always behaved. Concretely: a refresh or a reopened tab lands on the sign-in screen with no session note, the click opens the account chooser, and only the completion of a redirect sign-in enters directly, because that is a sign-in finishing rather than being skipped. Within a session nothing gets slower — the incremental permissions tools ask for on the click are still acquired silently." },
    ],
  },
  {
    build: 10375, date: "2026-08-24", title: "The Sign out button existed and never rendered",
    items: [
      { kind: "fixed", tool: "TUNO", text: "The tenant identity in the header — your name, avatar, the Sign out button, and since build 10373 the cfdev badge — has never been visible to anyone. The markup was there and the sign-in path switched it on by adding a class named on, but no rule in the stylesheet has ever mentioned that class, so the box stayed at its display none default from the day TUNO was scaffolded. ENCA never had the problem because it shows the box directly rather than through a class, and that is the fix here: the same mechanism, ported. Signing out was always wired and always worked — there was just no button on screen to press." },
      { kind: "fixed", tool: "TUNO", text: "The cfdev tenant detection now matches the tenant's real sign-in domain. Build 10373 listed only cloudfellows.dev, but accounts in that tenant sign in as user at devcf.onmicrosoft.com — the tenant's initial domain — so the detection could never fire from the UPN. Both names are on the list now: devcf.onmicrosoft.com because that is what the UPN carries, cloudfellows.dev kept for a UPN on the verified domain and for the org-name half of the check. Same matching rules, one list, one place." },
    ],
  },
  {
    build: 10374, date: "2026-08-24", title: "The folder provisioning becomes a Remediation pair",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The IT-TOOLS house-folder provisioning now ships the same way the AppLocker cleanup does: as a detect-and-remediate pair. Detect-TunoItToolsFolders.ps1 is new — it reports a device as non-compliant when a house folder is missing, when anyone outside SYSTEM and Administrators can write inside one, or when SYSTEM itself has lost write access, and it changes nothing, ever. It evaluates the DACL with the same write mask and the same trusted principals the provisioning script verifies with, deliberately, because a detection and a remediation that disagree loop forever. The pair is downloadable in step 1 beside the others, and the Remediation deploy panel now creates either pair from the browser — same discipline as before: the exact bytes this site serves, read-before-write with a same-name stop, created unassigned. One difference that matters more than any mechanics, and the screen says it: the cleanup pair is scoped to the migration window and unassigned after, this pair is a standing assignment — a folder that drifts writable after provisioning is exactly what its detection exists to catch." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Initialize-TunoItToolsFolders.ps1 (1.1.0) now proves SYSTEM can write by writing: after setting and verifying the ACL, it appends a provisioning record to IT-TOOLS, in the LOGS folder, and a failure of that write is a failure of the run. The house scripts — the AppLocker cleanup, this pair — log there as SYSTEM, and an ACL that reads back clean but cannot take a log line would have broken them silently. Verified by doing the thing, not by reasoning about it." },
    ],
  },
  {
    build: 10373, date: "2026-08-24", title: "TUNO knows its home tenant",
    items: [
      { kind: "new", tool: "TUNO", text: "Tenant discovery, ported from ENCA with the same list and the same matching rules. When the signed-in account belongs to the cloudfellows.dev tenant — the UPN domain matches exactly or as a subdomain — a badge appears beside the tenant identity in the header saying that the cfdev-only extras are available in this session, because extended behaviour should be said where the tenant identity lives rather than being a hidden mode. Nothing is gated on it yet: this build is the detection and the badge, and the seam tools will gate their cfdev-only features through, so the list exists in exactly one place. On any other tenant TUNO behaves exactly as documented and the badge does not exist. One stated difference from ENCA: ENCA also matches the organization display name it reads at sign-in; TUNO signs in with User.Read only and reads no organization, so until a tool that has read the org fills the name in, the UPN domain is the half of the check that answers — which is the reliable half anyway." },
    ],
  },
  {
    build: 10372, date: "2026-08-21", title: "Remediations have their own write scope",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The Remediation deploy asked for the wrong permission and the tenant refused it — correctly. Build 10369 shipped it under the profile write scope on the claim that DeviceManagementConfiguration.ReadWrite.All covers Remediations; the first real click proved otherwise, with Graph naming the scope it actually wants, and the API reference confirms it: creating a deviceHealthScript accepts only DeviceManagementScripts.ReadWrite.All. TUNO now requests exactly that, for the collision check and the create alike, making it the second write scope TUNO holds — added to the registration script and the security documentation in the open, because a new write scope is a decision, not a side effect. If your registration predates this build, add DeviceManagementScripts.ReadWrite.All as a delegated permission and admin-consent it, or the click keeps failing exactly as it did." },
    ],
  },
  {
    build: 10371, date: "2026-08-21", title: "A refresh restores the session, never the screen",
    items: [
      { kind: "fixed", tool: "TUNO", text: "Refreshing the page no longer drops you straight into the app. Every refresh lands on the sign-in screen, always — entering a tool that can write to a tenant is a deliberate act, not a side effect of pressing F5. Build 10361 fixed the MFA-on-every-refresh problem by restoring the cached session, but over-corrected by also skipping the screen; the two are now split. The session is still restored underneath, so nothing is paid for the gate: the sign-in screen names whose session will be continued, and clicking Sign in enters without a popup, a redirect, or a new authorization — and therefore without MFA. Closing the tab still drops the session entirely, sign out still ends it properly, and coming back from the no-popup redirect flow still enters directly, because that is the completion of a sign-in, not a refresh." },
    ],
  },
  {
    build: 10370, date: "2026-08-21", title: "Cleanup deploy moves up to step 1, and the names take their final form",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The Remediation deploy moved from the bottom of the page into a collapsed panel right under the script downloads in step 1 — because that is where it belongs in time. The cleanup pair runs at the START of a brownfield migration, before there is any policy to upload or export, so the section no longer waits behind step 5 and no longer requires a policy at all: sign in, open the panel, create. It stays collapsed by default because it writes to the tenant, and a write should be opened deliberately, not scrolled past. Everything else about it is unchanged — read first, stop on a name collision, created unassigned." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Both default names now follow the house scheme in full. The Remediation prefills \"[REPAIR_TOOLS]Win - DHS - Device Security - D - Clear Applocker Settings - R27.1 - v3.8\", and the Intune profile name field prefills \"Win - SEC - Device Security - AppLocker (AuditOnly) - R27.1 - V4.0\" — the complete final name, release suffix included, instead of a base the tool silently appended to. The mode token is swapped in place when exporting Enforce, so audit and enforce still get distinct names from the one field, and editing the R/V numbers is editing the field, not the code. The converter script's default and examples follow." },
    ],
  },
  {
    build: 10369, date: "2026-08-21", title: "The cleanup pair deploys as a Remediation, from the browser",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The deploy section gained part E: create the migration cleanup as an Intune Remediation — the detection and clear scripts as one deviceHealthScript, running as SYSTEM, 64-bit, carrying the exact bytes this site serves rather than a copy pasted into the code. The name prefills the house scheme and is editable. Same write discipline as the profiles: the tenant is read first, a Remediation already carrying that name stops the deploy rather than being overwritten, the write scope is asked for at the click, and nothing retries." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "It is created unassigned, on purpose twice over. Assignment carries a schedule and a scope, which are decisions for the portal — and this pair in particular must be scoped to the migration window and unassigned once the new policy is live, because its detection reads any AppLocker state as something to remove, the new policy included. The created-card says exactly that, and suggests putting the unassignment date in the change ticket at creation time, which is when people still remember why." },
    ],
  },
  {
    build: 10368, date: "2026-08-21", title: "The grouping names itself",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The Intune tab no longer suggests \"Pilot\" as the grouping. It prefills the house format instead — AppLocker- followed by a fresh GUID — which is unique the way Microsoft's guidance requires and still recognisable in a cleanup log or a carry-over finding, which a bare GUID is not. A ↻ button mints a new identity for when an export is a genuinely new deployment rather than an edit of an existing profile. The converter does the same when no grouping is supplied, and prints what it generated so it can be recorded." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Why not Pilot and Production as prefilled choices? Because those names encode the exact mistake the grouping rules exist to prevent. The pilot-versus-production distinction lives in which group the one profile is assigned to and whether its mode is audit or enforce — edited in place — not in the grouping name. Two groupings merge on any device that ever received both, and a hand-reusable word is precisely what produces two profiles sharing one, which is the case where removal breaks. Type a name like that anyway and the panel says so: the Intune form now shows every issue with the profile, not only the ones severe enough to block a deploy — a warning nobody can see is a warning that does not exist." },
    ],
  },
  {
    build: 10367, date: "2026-08-21", title: "Help you can navigate, groupings explained, and the folder trap closed",
    items: [
      { kind: "new", tool: "All tools", text: "Help gained section links, the way ENCA has them: one pill per section, in page order, jumping to the section with the sticky header accounted for. And the four newest tools — Assignment what-if, Assignment health, the Assignment editor and Settings search — finally have help sections at all, each written around its honest limits rather than its feature list. The Waiting-for-production pill appears only where the queue itself does." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "A help section on AppLocker groupings: what one is — the policy's address on the device, not a label — and the three consequences that follow. Different groupings merge, so a second deployment stacks on the first rather than replacing it. Identical groupings across two profiles write to the same addresses, which is why Microsoft's guidance says removal breaks unless groupings are unique and recommends a random GUID. And both applying and deleting reboot the device. The how falls out of the why: one profile, one grouping, moved from audit to enforce by editing it in place." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The cleanup script now answers the question it used to dodge: what about the groupings? It clears the local policy and the GPO tattoo as before, and now also names every MDM grouping cached on the device — removal of a live grouping belongs to unassigning its profile, and the script says so rather than deleting something Intune will put back at the next sync. For orphaned groupings, the debris the shared-grouping bug leaves with nothing in the portal pointing at them, there is an opt-in switch that removes the caches. Either way the verification step remains the arbiter: if anything still merges into the effective policy, the run reports failure." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Running icacls on a device without the IT-TOOLS folders exposed the trap in yesterday's convention: ProgramData lets a standard user create missing subfolders, and the creator owns what they create — so a user who created IT-TOOLS\\Apps before IT did would own a folder every policy allows. A new provisioning script closes it: it creates the house folders with inheritance disabled, SYSTEM and Administrators full control, Users read-and-execute, resets anything a user pre-created, verifies by reading the ACL back, and reports failure if a non-admin can still write. Deploy it as SYSTEM before the audit profile — the downloads row in step 1 carries it, and the checklist now leads the house-folder section with it." },
    ],
  },
  {
    build: 10366, date: "2026-08-21", title: "The IT-TOOLS house convention, so nobody has to remember it",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "Everything IT puts on an endpoint lives under ProgramData\\IT-TOOLS, and the policy machinery now knows it. The Apps and Scripts folders get standing allow rules in every generated Exe, MSI and Script collection — from the scanner and from the tool's own add-default-rules — so IT-deployed tooling always runs without anyone remembering to add the rule. AppLocker has no ProgramData variable, so the rules are written against the OS drive, which lands on the same folder on every standard install." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "A standing allow is only as strong as the folder's permissions, and that condition is enforced rather than hoped for. The folders must be writable by SYSTEM and Administrators alone — the Intune Management Extension deploys as SYSTEM, so delivery keeps working — and the device scan now checks exactly that: a user-writable directory inside a house folder raises the loudest warning the scan has, because at that point the standing allow is a live bypass a standard user can walk through. The audit, for its part, reports the house rules at Info instead of flagging the tool's own convention as a risk on every run — with the ACL condition stated, and a pointer at what a clean scan proves." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The cleanup script's logs and backups moved home: ProgramData\\IT-TOOLS\\LOGS, the same convention, instead of a folder in the drive root. LOGS deliberately gets no allow rule — logs are not executables. The convention is written down in the scripts README and the implementation checklist, which is where it will outlive anybody's memory, including this tool's." },
    ],
  },
  {
    build: 10365, date: "2026-08-21", title: "Findings that do not fit become a summary that says so",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Beside the XML panel the findings card is roughly half a screen, and a six-column table in half a screen wraps its reason column into one word per line — rows five hundred pixels tall saying almost nothing. The card now carries two renderings of the same list and shows whichever fits its own width: in the narrow column, a compact summary — severity, collection, rule and the reason clamped to three lines — ending in a button that opens the full screen view; anywhere the card has room, the full table exactly as before. The recommendations and one-click fixes live where there is space for them, and the summary says so instead of squeezing them into a column where the fix button was the first thing pushed off the edge." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The switch is made by the card measuring itself rather than the window, so it comes out right in every layout it appears in: beside the XML panel it summarises, stacked under it at narrow window widths it shows the table, and in the full screen popout it always shows the table. Browsers without support for that measurement keep the table everywhere, which is exactly what they had before." },
    ],
  },
  {
    build: 10364, date: "2026-08-21", title: "The same crash, second shape — and this time the scan survived it",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Rule generation failed again with the same \"argument types do not match\", and this run proves the recovery work: the scan finished, wrote the bundle with all the evidence, and recorded the failing line in its own warnings — which is exactly how the cause was found in minutes instead of another round trip. The defect is the same Windows PowerShell 5.1 compiler problem as before in a second shape: the ordered dictionary's two indexers can make the engine refuse an indexed READ just as they made it refuse an indexed write, and the first fix only covered the write. The discipline is now total instead of case-by-case: every write to those dictionaries goes through the explicit add method, and every read goes through a cast to the one-indexer dictionary interface, which leaves the compiler nothing to choose between. No bare indexer remains anywhere on the rule-generation path — checked mechanically, not by eye." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The version guard caught a drift while this was being fixed: the scan's help header still said 1.4.0 while the script itself had moved to 1.6.0 — two builds had bumped one and not the other. Both now say 1.7.0, and the banner, the help and the bundle agree about which script produced what." },
    ],
  },
  {
    build: 10363, date: "2026-08-21", title: "The cleanup pair is where the work starts",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The cleanup script and its detection half are downloadable from step 1, next to the scan, each with the same copy-able download command — not only from step 5. A device that already carries a policy needs the cleanup before the new policy means anything, so the download belongs where the work starts as well as where it ends. A line under the buttons says which two are for brownfield devices and points at step 5 for the full order: unassign, clean, deploy under a new grouping." },
    ],
  },
  {
    build: 10362, date: "2026-08-21", title: "The cleanup for devices that already have a policy",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The brownfield gap is closed. The carry-over findings could already tell you a device was running an old policy the new one would not remove — now the tool also hands you the script that removes it. Clear-TunoAppLockerPolicy.ps1, downloadable in step 5 beside the checklist, backs up the effective policy, the local policy and the registry tattoo before touching anything, replaces the local policy with the genuinely inert empty one, sweeps the SrpV2 remnants, and then verifies the device is actually clean — exiting with a failure code when it is not, so an Intune Remediation reports the device rather than the wish. Its detection half, Detect-TunoAppLockerPolicy.ps1, makes the pair deployable as a Remediation for the migration window: unassign the old profile, let the cleanup sweep what unassignment leaves behind, deploy the new policy under a new grouping." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The script this replaces had three defects that would have sabotaged the exact migration it exists for. It cleared the AppLocker event logs as a side effect — the 8003/8006 audit evidence the whole rollout runs on, gone in the cleanup step. It stopped the Application Identity service and set it to Manual — the service the new policy needs, without which AppLocker neither blocks nor logs and the pilot reads clean while doing nothing. And it exited 0 even when its own verification failed, which a Remediation reads as remediated. The replacement preserves the logs by default (and exports them first even when told to clear), leaves the service alone unless AppLocker is being retired outright, and tells the truth in its exit code." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The migration order is now written everywhere a person would look for it — the carry-over finding's recommendation, step 5, the checklist and the scripts README all say the same three steps in the same order: unassign the old profile first, because cleanup without unassignment is a loop and everything returns at the next sync; run the cleanup; then deploy under a new grouping, never the old deployment's name. And a warning that belongs with the pair: scope the Remediation to the migration window and unassign it when the new policy lands, or its detection will read the new policy as state to remove." },
    ],
  },
  {
    build: 10361, date: "2026-08-21", title: "A refresh no longer costs you an MFA prompt",
    items: [
      { kind: "fixed", tool: "All tools", text: "Refreshing the page sent you back to the sign-in screen and made you authenticate again, multi-factor and all. That was not your identity provider being strict — it was this app throwing away a session it already had. The account is cached for the life of the browser tab, but the shell only ever looked for one in the moments just after a redirect sign-in; on an ordinary refresh it found nothing, showed the sign-in screen, and the sign-in it then performed asked the identity provider to start over from scratch rather than reuse the session sitting underneath. It now picks the cached account back up and carries straight on. If more than one account is cached and none is marked current, it still asks — choosing for you risks signing you into the wrong customer's tenant, which is worse than a click." },
      { kind: "fixed", tool: "All tools", text: "The sign-in itself stopped forcing the account chooser. Forcing it is what made the identity provider re-run its policy, MFA included, on every single entry. It was buying one thing — letting somebody with several accounts pick — and Sign out already does that, because it signs you out at the provider too. Paying a multi-factor prompt every time to keep a chooser almost nobody reaches for was the wrong trade. Signing out now also clears the remembered current account, so the next sign-in cannot silently reuse the one you just left." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The screen is ordered by what you do rather than by what the file happened to contain. The enforcement modes and the add-a-rule form — the two things you touch while building a policy — are now at the top, above the device evidence, the coverage table, the findings and the rule list, which are the things you read. Adding three rules used to mean scrolling past several screens of analysis to reach the form, three times." },
    ],
  },
  {
    build: 10360, date: "2026-08-21", title: "Assignments change in bulk — behind four gates",
    items: [
      { kind: "new", tool: "✏️ Assignment editor", text: "The eleventh tool, and the second that writes. Add an include, add an exclusion, or remove a group across many policies at once — device configurations, settings catalog, compliance and administrative templates — after Maxime Guillemin's Intune Toolkit, whose question this is. It is the thing every admin wants and the portal makes slow, and it is also the first tool here whose mistake would be a policy reaching the wrong population, which is invisible until somebody's laptop behaves differently. That is why the pipeline is the tool: read, dry run, backup, confirm, apply, verify — in that order, enforced by the screen rather than by anybody's care." },
      { kind: "new", tool: "✏️ Assignment editor", text: "The dry run lists every operation with the group's name and its member count — an empty group is warned as configuring nothing — and it lists what it will NOT do with the reason. Two operations are refused outright, using the health tool's own findings before they happen: adding an include to a policy that excludes the group would create the include-and-exclude contradiction, and adding an exclusion to a policy that includes the group is a contradiction rather than a removal — the right operation is removing the include, and the plan says so." },
      { kind: "new", tool: "✏️ Assignment editor", text: "The assign call replaces a policy's whole assignment list, and three protections follow from taking that seriously. Untouched assignments are re-sent exactly as read, filters included, because dropping a filter on an assignment you did not mean to touch would silently widen it. Every policy is re-read at the moment of writing, and one whose assignments changed since the plan was made is skipped as drifted rather than overwritten — replacing a list somebody else just edited is how two admins erase each other. And every write is followed by a read-back: 'verified' is the tenant's word, never the request's status code." },
      { kind: "new", tool: "✏️ Assignment editor", text: "A backup of every affected policy's current assignments is taken automatically as a restorable file, and the apply button stays locked until it is. Removals are confirmed separately from additions, by typing the group's name. The write permission is asked for at the apply click and never before, so reading and planning cost nothing but reads. Scripts and applications are deliberately absent — each needs a write permission the registration does not hold, and adding one is a decision to take in the open, not a feature's side effect. No delete, no rename, ever: the Toolkit keeps both behind its own Advanced Actions toggle, and neither belongs in a browser tool whose whole argument is that it cannot do much harm." },
    ],
  },
  {
    build: 10359, date: "2026-08-21", title: "Search what can be configured, then who configures it",
    items: [
      { kind: "new", tool: "🔦 Settings search", text: "The tenth tool. 'Which policies configure BitLocker, and to what value?' — answered from the setting definitions rather than the policy names. One read brings the settings-catalog definition catalog, roughly seventeen thousand definitions covering everything that can be configured, searched as you type: every word must match somewhere, and a name that starts with the term outranks a name that contains it, which outranks a keyword, which outranks the description. The catalog is Microsoft's, not the tenant's — it says what exists, not what you use." },
      { kind: "new", tool: "🔦 Settings search", text: "A second, separate read brings the tenant's own settings-catalog policies with their settings, so each search result can say who sets it and to what. Until that read has run, the 'set by' column is absent rather than empty — 'nobody sets this' and 'nobody looked' are different answers and the screen keeps them apart. Child settings are indexed under their own definitions, so a setting configured inside somebody's choice is still found. The documenter finds a setting if you know which policy holds it; this finds the policy when all you know is the setting." },
      { kind: "new", tool: "🔦 Settings search", text: "Values pass the documenter's redaction gate — the same implementation, deliberately not a copy. A settings search is exactly the tool somebody uses to hunt for 'password', and a result table that printed secret-bearing values would be a disclosure engine with a search box. There is no switch to turn the redaction off, for the same reason the documenter has none." },
    ],
  },
  {
    build: 10358, date: "2026-08-21", title: "The assignments that reach nobody",
    items: [
      { kind: "new", tool: "🩺 Assignment health", text: "The ninth tool. Every finding it makes is the same shape of problem — something that looks configured and is not. Policies assigned to empty groups, which have never applied and never will; assignments naming deleted groups, which target nobody silently and forever; policies with no assignment at all; policies whose every assignment is an exclusion, so there is nothing left to exclude from; and one group both included and excluded on the same policy, where the exclusion wins and the include reads as reach it does not deliver. After Ugur Koc's Intune Assignment Checker, whose idea this is: an assignment reaching an empty group is a finding, not a row." },
      { kind: "new", tool: "🩺 Assignment health", text: "Empty and deleted are different faults with the same symptom, and the report keeps them apart. A dangling reference names a group the directory no longer has. An empty group still exists — it simply holds nobody, checked transitively, so a group whose only member is another empty group counts as empty too. The membership check is a one-row peek rather than a count, because 'is there at least one member' is the whole question and it batches twenty to a round trip." },
      { kind: "new", tool: "🩺 Assignment health", text: "Deployment failures are read where Graph keeps a cheap per-policy status — device configurations, compliance policies, scripts and remediations — and the surfaces that keep theirs behind the reports API are named as unchecked rather than passed off as healthy. A health page that silently checks only some surfaces reads as a clean bill for the rest, which would make the tool worse than not running it. The same honesty applies down the stack: a group that could not be peeked into is unknown rather than empty, a name lookup that failed wholesale is unknown rather than a directory full of ghosts, and a surface that could not be read is unknown rather than clean." },
      { kind: "new", tool: "🩺 Assignment health", text: "Enrolment restrictions are deliberately exempt from the unassigned finding: the built-in defaults carry no assignments and apply tenant-wide by priority, so unassigned is their normal state and flagging it would bury the real rows under one per tenant default. Findings, not a score — whether an empty group is a mistake or a landing zone prepared for next quarter is not something a tool can know. Exports Markdown and CSV." },
      { kind: "improved", tool: "🔗 Group Analyzer", text: "The shared assignment reader learned to report objects with no assignments at all, as an opt-in flag the health tool passes and nothing else does. It is keyed on the assignments array being empty rather than on nothing matching, so a policy assigned only to groups outside the question is still correctly counted as assigned. The Group Analyzer, device analyzer and what-if are unchanged — for them an unassigned policy is rightly not a row." },
    ],
  },
  {
    build: 10357, date: "2026-08-21", title: "What would happen — before you change the membership",
    items: [
      { kind: "new", tool: "🔮 Assignment what-if", text: "The eighth tool, and the first that answers a question about the future. Pick a user or a device, name a group, and see what joining or leaving it would change: policies gained, policies lost, applications that would install and applications that would be UNINSTALLED — before anybody touches the membership, while the change is still cheap to reverse. After Ugur Koc's Intune Assignment Checker, whose what-if was the one capability of that suite TUNO had nothing like." },
      { kind: "new", tool: "🔮 Assignment what-if", text: "The mechanics are on the screen rather than hidden, because a simulation whose workings are invisible gets trusted past its competence. Joining a group brings that group's own parents with it — membership of a child is membership of its ancestors, and forgetting that understates every join onto a nested group. Leaving only severs a direct edge: the closure is rebuilt from the memberships that remain, so a parent still reachable through another group is not reported lost. And a membership held only through nesting cannot be resigned from at all — the answer says where the edge actually is instead of simulating an action the portal does not offer." },
      { kind: "new", tool: "🔮 Assignment what-if", text: "The delta has four states rather than two, and the third is the reason to use the tool: joining a group can take policy away. An exclusion targeting the group turns a policy that applies today into one that does not, and that row — lost, by joining — is the most surprising thing an assignment change can do. The fourth state, pre-excluded, is a change that does nothing today but pins the future: the policy cannot reach the subject later while the exclusion stands. An application whose assignment intent is uninstall and starts to apply is labelled 'will be uninstalled', not 'gained'." },
      { kind: "new", tool: "🔮 Assignment what-if", text: "It is a simulation, not a promise, and it says so in its own output. Assignment filters and dynamic-membership rules are evaluated by the service against inventory a browser cannot see: rows they touch say 'may', in both directions. Tenant-wide assignments are not in the delta at all, because they apply whatever the groups say. A dynamic group cannot be joined by hand, so that answer is labelled as what would apply if the rule came to match. And a leave whose remaining memberships could not all be expanded calls its after-set a floor rather than an answer." },
      { kind: "new", tool: "🔮 Assignment what-if", text: "Compare mode puts two to four groups side by side — each column the group plus what it inherits from its own parents — and shows only where they differ, which is the comparison the portal cannot do at all. The endpoint list is the Group Analyzer's own source table, the rule T06 established: the first time a surface joined one list and not the other, this tool would start reporting 'nothing changes' about a workload it never looked at. Reads only; exports Markdown and CSV." },
      { kind: "fixed", tool: "🔮 Assignment what-if", text: "The tool's tile and screen briefly existed on this channel without the code behind them — two parallel working sessions, and the build that landed between them carried the half of the work that had reached the file first. The tile opened nothing. This build supplies the other half, which is a small lesson in why one tree takes one writer at a time." },
    ],
  },
  {
    build: 10356, date: "2026-08-21", title: "What's new actually shows up",
    items: [
      { kind: "new", tool: "All tools", text: "This file has described itself as the source for \"the What's new overlay shown after sign-in\" since TUNO was first scaffolded, and there was no overlay. The text came across with the file; the feature did not. Now there is one. Sign in and anything released since you last looked is in front of you, with a link through to the full list." },
      { kind: "improved", tool: "All tools", text: "It follows four rules so it stays a courtesy rather than an obstacle. It shows only what you have not seen, not the whole history. It shows nothing at all on a first visit — a new user does not need forty builds of other people's work between them and the tools, so the current build is recorded quietly and the notice starts working from the next release. Opening the What's new page counts as having seen it, so it never reappears over something you have just read. And closing it with Escape or a click outside does not mark it seen: only \"Got it\" and reading the full list do, so a stray click cannot cost you the one notice you were given." },
    ],
  },
  {
    build: 10355, date: "2026-08-21", title: "What the device already runs, and one grouping per profile",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "Deploying an AppLocker policy does not clear what came before, and nothing in the tool said so. Every delivery path adds rather than replaces: the Intune CSP keeps one node per grouping and rule type until something explicitly deletes it, Group Policy merges rules from every linked object and will not overwrite ones already present, and a local policy persists until cleared. So a collection your new policy simply leaves out keeps running — and if it was the not-configured-with-rules kind, it keeps blocking, while the policy on screen appears to have nothing to say about that type at all. Upload a scan and the audit now names every collection the device is running that the policy in front of you does not cover, says whether those rules are enforcing today, and explains how to actually remove them. It is the one check that needs the device's own effective policy, which is exactly what the scan brings back." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The advice on the grouping value was wrong, and it was the dangerous kind of wrong. It said to use the same grouping for the audit and enforced profiles so the second replaces the first. Microsoft's guidance on the AppLocker CSP is the opposite: grouping values must be unique, a randomly generated GUID is the stated best practice, and duplicates break removal because two profiles write the same addresses and unassigning one deletes what the other still depends on. One profile, one grouping, and you move from audit to enforce by editing that profile rather than deploying a second beside it. Corrected in the tool, in the converter's help, in the scripts README and in the implementation checklist." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Applying an AppLocker policy through the CSP reboots the device, and so does removing one. That was in none of the guidance, which made the rollback in particular sound cheaper than it is. The checklist now asks for a maintenance window for both directions, and the deployment steps say it where you would be reading them." },
    ],
  },
  {
    build: 10354, date: "2026-08-21", title: "The scan stops losing whole subtrees, and a full review against AaronLocker",
    items: [
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The scan was missing directories, and it was not the permission check \u2014 that part is careful. It was the walk around it. A path longer than 260 characters throws on Windows PowerShell, and the throw happens when listing the PARENT, so the failure did not cost one directory: it cost everything beneath it. Deep folders under ProgramData and AppData are exactly where a droppable directory lives, so this was a hole in the most interesting part of the disk. Paths are now handled in the extended form Windows provides for the purpose, and converted back before they reach a rule, where that form would be wrong." },
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Two silent skips are now counted and named. A directory whose attributes could not be read was passed over without a word \u2014 so something whose nature was UNKNOWN was treated as if it had been examined. Reparse points, skipped on purpose, were never mentioned either. And three different failures shared one counter and one warning telling you to run elevated, which is useless advice for a path that is simply too long. They are separate now, because the remedies are." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "A full written review against Microsoft's AaronLocker ships alongside the scripts. The short version: our permission evaluation is the better of the two \u2014 theirs counts an explicit Deny as a grant, loses permissions held by deleted accounts, and unions rights across different people \u2014 but theirs computes what an account can ACTUALLY do, resolving nested groups, where ours compares identifiers against a list. That difference makes us over-report rather than miss things, which is the safer error and still an error. It also names what neither tool sees: a writable FILE inside a locked directory." },
    ],
  },
  {
    build: 10353, date: "2026-08-21", title: "A bundle with no generated rules says so",
    items: [
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "When a scan bundle carries no generated rule set, the tool falls back to the policy the device was already running \u2014 which is the right thing to do, and it was doing it almost silently. The only sign was a line of small text naming what you were editing, next to buttons offering the choice. So a bundle whose rule generation had failed looked like a scan that had found almost nothing: a sparse policy with a Dll collection and a placeholder rule, exactly what Intune leaves on a device, and no obvious reason why." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "It now says so plainly, at the top of the device card, and explains where the reason is: the scan wraps rule generation so that a failure cannot cost you the evidence it already gathered, and when it fails it writes the reason into the bundle's warnings and prints it in red at the time. All the warnings are shown in that case rather than the first six, because the one that matters is likely to be among the ones that were being hidden." },
    ],
  },
  {
    build: 10352, date: "2026-08-21", title: "The scan reaches AppLocker from PowerShell 7",
    items: [
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Running the scan under PowerShell 7 warned that the AppLocker module was not available, and fell back to reading certificates for every publisher. That was correct as far as it went \u2014 the module is a Windows PowerShell component and 7 cannot load it in-process however it is asked \u2014 but 7 on Windows can still REACH it, through a background Windows PowerShell session that proxies the commands across. The scan does that now, so a 7 session gets the same authoritative answers a 5.1 one does." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "With one deliberate limit, because the proxy is not free. Every call across that boundary is serialised, and the scan asks for file information ONCE PER FILE across thousands of them \u2014 so a fix that looked like a win could have turned a two-minute scan into an afternoon. The script times a single call before committing to it: if it is slow, the effective-policy read still goes through the proxy, which is one call and worth it, while the per-file work stays on certificate derivation. Either way the bundle records which of the three routes was taken, and the device card in the browser now shows it beside whether the scan ran elevated." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "None of this has been run on a real PowerShell 7 machine \u2014 there is no Windows host in the environment it was written in. The timing threshold in particular is a guess that wants one real measurement, and the script says so where the number is set." },
    ],
  },
  {
    build: 10351, date: "2026-08-21", title: "The code panel's header stops fighting itself",
    items: [
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Adding the collection selector gave the panel header four controls and a two-line filename to fit on one row, in a column that is not wide enough for them. Nothing was allowed to wrap, so the layout resolved it by squeezing the filename block toward nothing \u2014 the line underneath broke to one word per line and read as a narrow column of text, while the Download button ran off the right-hand edge. The header wraps now: the actions move to a second row when they have to, and the filename keeps a minimum width so it can never be crushed like that again. Both of its lines clip with an ellipsis rather than wrapping, because a filename is an identifier and not a sentence." },
    ],
  },
  {
    build: 10350, date: "2026-08-21", title: "The full-screen panel scrolls, and you can take one collection at a time",
    items: [
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Opening a card full screen showed the top of it and nothing else \u2014 no scroll, no way to reach the rest. One rule was making every parked card fill the panel exactly, and a card hides whatever does not fit, so everything past the fold was simply unreachable. The two kinds of card need opposite answers and now get them: the code panel takes the height and scrolls inside itself, a table grows to its content and lets the panel scroll it." },
      { kind: "new", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Download and Copy can hand over ONE rule collection instead of the whole policy. A selector beside the button lists every collection that has anything in it, with its rule count. The whole policy stays the default and is still the right answer for a GPO import; a single collection is what Intune's manual route asks for, one value per collection, and cutting it by hand out of a file containing five was the alternative." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "A single collection means different things on the two tabs, so it produces different files. From the Policy XML tab it is a complete policy document containing that one collection \u2014 a bare rule collection is not a policy and neither a GPO import nor PowerShell will accept one. From the Intune tab it is the bare OMA-URI value, because that string is exactly what the portal asks you to paste into a custom setting." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The panel follows the selector: pick a collection and the code on screen narrows to it, the filename changes, and the line underneath says what you are about to get. Showing the whole policy while the button hands over one collection would be the same kind of lie as a preview drifting from its file." },
    ],
  },
  {
    build: 10349, date: "2026-08-21", title: "Fourth promotion — the AppLocker corrections are production",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves to build 6 and takes queue items 30 to 35 with it — beta builds 10342 and 10344 to 10348. Three of the six were high risk and all three were corrections to the one tool whose output is meant to be enforced on endpoints: the scan that crashed at its last step and discarded a whole run, the reference-machine assumption now stated and checked, and the NotConfigured misreading that was both generating an enforcing DLL section and reporting imported policies as unenforced when they were blocking. The checklist, the script version stamps and the full-screen panels went with them. The queue is empty again." },
      { kind: "improved", tool: "All tools", text: "The documentation from builds 10338 to 10343 travelled with it, as documentation always does rather than being queued: eight more roadmap items with their credits, and the security note explaining why TUNO signs in with its own app registration instead of borrowing the identifier of Microsoft's Graph PowerShell tool." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The production copies of both PowerShell scripts carry build 6 rather than 10348. That matters more than a version stamp usually does, because build 10348 taught the scan to work out which channel it belongs to from that number and tell the operator where to take the bundle. A production script still carrying the beta stamp would have sent every operator to the beta site — the bug that build fixed, inverted, and shipped to everyone rather than to the few people on beta." },
      { kind: "improved", tool: "All tools", text: "The check that verifies a production build before it is committed had rotted: it was pinned at build 3 and pointed at a working folder that no longer exists, so it could not run at all. It now works out both build numbers for itself instead of having them written in, and it checks the things a promotion actually gets wrong — a beta number left in a script URL, the roadmap still showing beta builds, the production domain file missing, and a script that arrived without the rest of its changes." },
    ],
  },
  {
    build: 10348, date: "2026-08-20", title: "A beta scan sends you to the beta site",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The scan finished by telling you to take the bundle to the production site, whichever channel the copy had come from. A beta build of the script sending you to production sends you to a different build of the tool — one that may not read the fields that version of the scan writes, and on the day the scripts were new, one that did not have the tool at all. It works out where it came from instead: the build stamp already distinguishes beta from production by the same rule the site itself uses, so the script derives its own channel and names the matching site. A beta copy says outright that it is a beta build and that the bundle belongs on the beta site." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The scan's banner now reads the version, the channel and the build — so the first line of output answers \"is this the copy I think it is\" without reading the file. The bundle records the same three, which means a scan result can be traced to the channel and the build that produced it rather than just to a script name. The version guard covers the channel logic too, so a hard-coded host cannot creep back in." },
    ],
  },
  {
    build: 10347, date: "2026-08-20", title: "NotConfigured does not mean off",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Reading two lines of the new checklist against each other turned up the worst defect in this tool so far, and it was in what the tool generates. The DLL rules were shipped inside a collection marked NotConfigured and described, in the code and in the documentation, as present and inert. They would not have been inert. Microsoft's own guidance is explicit: despite the name, a rule collection that is not configured does not have its rules ignored — if any rules exist in it, they will be enforced, and the value should never be used deliberately. So the tool was about to enforce DLL control on every device the policy reached, against only the DLLs a scan happened to find, while every description of it said the opposite. Nobody had deployed it, so it never bit." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The DLL collection is now left out of the generated policy entirely — absence is the only state that genuinely restricts nothing. The scan still records what it found for DLL in the bundle, so the collection can be taken on later as its own project with the log volume and the start-up cost accepted on purpose. NotConfigured is no longer written into a generated policy for any collection, by the scan, by the browser export, or by the converter." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The audit said the same wrong thing about policies you import, which mattered just as much. A collection marked NotConfigured used to be reported as default-allow for that type — nothing restricted. That is true only when the collection is empty. With rules in it, that collection is blocking today while reading as inactive to whoever opens the policy next, and the tool now says so as a High finding and asks you to make the mode explicit. The evaluation behind the Microsoft coverage table and the writable-path probe was making the same assumption, so a policy in that state was being reported as unenforced when it was not. Every judgement about whether a collection restricts anything now goes through one place rather than testing the word." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The checklist that started it gained the whole trap as its own item, with the three states written out — absent, not configured and empty, not configured with rules — because the first two restrict nothing and the third blocks, and only the presence of rules tells them apart. That is also why the advice is never to write the value at all." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Both downloadable scripts moved a version, which had been forgotten twice in three builds — so it is no longer something to remember. A guard compares the scripts against the last commit and refuses the build if a script's contents changed while its version did not, and separately holds the build stamp to the site's own build number. The rule was written down in the scripts README at the same time, because a check nobody can find the reasoning for eventually gets deleted." },
    ],
  },
  {
    build: 10346, date: "2026-08-20", title: "A checklist to work down, and scripts that admit their version",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "There is a downloadable implementation checklist, offered next to the converter in step 5. It is every check that has to pass before an AppLocker policy is enforced, in the order you need it: reading the XML before anyone else does, the writable-path check that is the whole point, not breaking your own software delivery, whether people will still be able to work, what has to be true before you assign anything, what the audit period is actually for, enforcement in rings, and living with it afterwards. It ends with the things that catch people out — that AppLocker cannot restrict an administrator and never could, that deny beats allow including over the administrator rule, that a collection absent from the file is not off but default-allow, that hash rules break on the next update, every one of them. Print it, work down it, keep the completed copy: when something breaks in three months the useful question is which check was skipped." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The scan script reported itself as version 1.0.0 after two builds that changed it substantially — so a scan bundle could not be traced back to the build that produced it, which is the one thing a bundle's provenance is for. Both scripts now carry two numbers: their own version, for somebody holding a copy that has been sitting on a share for six months, and the site build that served it. The second is checked against the site's own build number by a headless test, so a script edited without bumping it fails before it ships rather than in the field. The same discipline the cache-busting numbers have always had, applied to the two files that leave this site and run on a real machine." },
    ],
  },
  {
    build: 10345, date: "2026-08-20", title: "Scan a reference machine, and say so",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The scan never said out loud what it assumes, and everything it produces depends on it. The policy it writes means: everything on this machine is allowed, and nothing else is. From a freshly built image with your standard applications installed, that is a baseline. From a device somebody has worked in for two years it allows two years of accumulation — installers left in Downloads, a dev toolchain, whatever a colleague once ran out of a zip — which is the exact permission AppLocker is deployed to take away. Step 1 now says clean reference machine, not your own laptop, and says why the machine matters more than the switches do." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The scan checks whether it believes you. It counts the profiles on the device and the executables inside them, and looks for the things that only exist because somebody has been using the machine — programs in Downloads or on the Desktop, a Chrome profile, per-user installed programs, a node_modules tree, VS Code extensions, a NuGet cache, a git working copy. If it finds them it stops and says so in the console, and the finding arrives in the tool as High with the evidence quoted. It will not refuse to run; it will not be quiet either. A machine that does look clean is reported too, at Info, because knowing the check ran is worth as much as the verdict." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The way software is allowed to arrive is now written into the policy instead of implied. Two rules name the Intune Management Extension — the folder it stages Win32 apps into and the folder it runs its own scripts from — and the administrator rules say in their description that they are the second of the two sanctioned routes. Both were already covered by the Windows and Program Files defaults, which is exactly the problem: the day somebody trims those defaults, software delivery dies across the estate with nothing pointing at the cause. A policy whose intent is \"software installs via Intune or an administrator, and no other way\" should contain that sentence." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "And the scan will no longer break software delivery on its own. It carves every user-writable directory out of the default Windows and Program Files rules, and if an Intune Management Extension folder came back writable it would have carved that out too — killing Win32 app installs and remediation scripts silently, days later, on every managed device. Those paths are now protected from being excepted, including when the exception list is compressed into something short enough to swallow one by accident. A writable IME directory is reported as a finding instead, which is the more useful answer: fix the permissions on the folder your software deployment runs from." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "User profiles are recorded once instead of enumerated. A profile is writable by its owner by definition, so listing every subfolder answers a question nobody asked — the last real run returned 530 rows saying so, against 15 under Windows and none under Program Files, and spent the inventory budget on browser caches before reaching anything useful. Nor do those directories need exceptions: no default rule allows execution from a profile in the first place, so there is nothing to carve out of. What the profile is scanned for is what is installed in it, and Windows and Program Files are now inventoried first so a cap can never crowd them out." },
    ],
  },
  {
    build: 10344, date: "2026-08-20", title: "The scan no longer throws itself away",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The device scan crashed at the very last step, after all the expensive work was done. It walked 71,000 directories, inventoried 5,000 executables, read a month of event logs — and then died while counting up the rules it had just built, writing nothing to disk at all. The cause is a genuine trap in Windows PowerShell 5.1: the ordered dictionary used to hold the per-collection counts has two different indexers, one taking a number and one taking a name, so the engine compiles a choice between them at runtime. That choice only works when the thing being stored is an object. The rule count is a plain integer, and the engine refused to compile the assignment at all, reporting \"argument types do not match\" with no line number and no clue. It is stored a different way now. Every other place the script does this stores a list or a string, which is why the fault was in exactly one line and nowhere else." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The deeper mistake was the ordering, and that is fixed too. Rule generation is the last thing the scan does and the only part that can be rebuilt from what is already in hand — the permission walk, the signature inventory and the event logs cannot, and on a real machine they take the best part of an hour. So a failure there no longer takes the scan with it: it is caught, recorded in the bundle's own warnings with the line it came from, and the bundle is written anyway. Upload it and the tool falls back to the policy the device is really running, with every writable directory, every executable and the full event analysis intact. An hour of scanning should never be lost to a fault in the five seconds at the end of it." },
    ],
  },
  {
    build: 10343, date: "2026-08-20", title: "Keeping the minimum OS version honest",
    items: [
      { kind: "new", tool: "Roadmap", text: "R18, after James Robinson's IntuneComplianceMaintainer. Every compliance and app-protection policy carries a minimum OS version, and that number is right on the day it is typed and slowly wrong afterwards — which is how a tenant ends up letting in devices three releases behind, on a policy nobody has opened since they wrote it. The tool would read what is currently supported, from endoflife.date for iOS, iPadOS, macOS and Android and from the Windows Update catalog for Windows builds, work out what each selected policy should say, and update the ones that are behind." },
      { kind: "new", tool: "Roadmap", text: "It runs on demand, and the card says plainly that this is the honest half rather than a smaller version of the original. His script is a runbook: an automation account, a managed identity, a schedule, and the whole point is that it runs when nobody is watching. A page in a browser has no server and nothing to schedule with. What it can be is the thing you open after a Patch Tuesday, or when somebody asks whether the compliance floor is still current — press it, read what would change, decide, apply. Anyone wanting monthly enforcement unattended should run his script, and the card points at it." },
      { kind: "new", tool: "Roadmap", text: "The sharp edge is stated in capitals, because it is easy to read this as a tidying-up tool. Raising a minimum OS version is not a configuration change, it is an access change: where Conditional Access requires a compliant device, every machine below the new floor stops being compliant and then stops getting in, and the ones that cannot reach the new version are the ones that never will without new hardware. So the dry run would say, per policy, how many devices in scope sit below the proposed floor — which TUNO can genuinely work out, because it already reads device OS versions and already works out who a policy reaches — and show the grace period from the policy's own scheduled actions beside every change, since that is the difference between a lockout this afternoon and a week's warning." },
      { kind: "new", tool: "Roadmap", text: "Two things it would cost that are worth deciding rather than discovering. Updating app-protection policies needs a second write permission, which would be only the second in the whole application. And endoflife.date would be the first host that is not Microsoft's this page has ever contacted — the connection policy currently allows two addresses and nothing else, which is a large part of why the security documentation can say what it says. Either widen it by exactly one host and write down why, or take the target version as something typed in. Both are on the card; neither should happen quietly. The Windows half needs no new address at all, and asks for read where the original asks for write." },
      { kind: "improved", tool: "Roadmap", text: "The credits list gains the project and its author, who also maintains OpenIntuneBaseline. The Android handling is the part worth copying exactly rather than improving on: set the floor to the oldest release still maintained rather than the newest, so any supported device passes, and treat the monthly security patch level as a second floor of its own." },
    ],
  },
  {
    build: 10342, date: "2026-08-20", title: "The code panel joins the product, and three things open full screen",
    items: [
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The code panel was a navy slab \u2014 the one thing on the screen wearing a colour this product does not own. It is themed now, and it changes family rather than shade: the warm cream of the deploy card in light mode, the greens in dark. Both come from variables the app already carries, so there is no fixed colour left in it and nothing to keep in step by hand. The command snippets in step 5 follow, because fixing the large blue block and leaving the small ones blue would only have moved the odd one out." },
      { kind: "new", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The code panel, the findings table and the Microsoft coverage table each open full screen. The panel MOVES the card into it rather than copying it, so every button, filter and fix inside keeps working \u2014 a copy would look right and do nothing, which is the worst kind of broken. Apply a fix while it is open and the table simply redraws in place; close it and the card goes back exactly where it was. Escape or a click outside closes it. Ported from ENCA, whose matrix has worked this way for a long time." },
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Both of those tables were losing their right-hand side. The coverage table kept its Add-allow-rule button in a column of its own at the far edge, so on any window that needed a sideways scroll the only control on the row was the only thing you could not reach \u2014 the same mistake the findings table had, fixed there in an earlier build and missed here. It now sits under the detail it acts on. Both tables also share their width out by proportion instead of letting the longest sentence decide, so the columns stop being pushed off the end." },
    ],
  },
  {
    build: 10341, date: "2026-08-20", title: "Why TUNO does not borrow Microsoft's client id",
    items: [
      { kind: "improved", tool: "All tools", text: "A question worth writing down rather than answering twice. Several community Intune tools sign in with no app registration at all, by using the client identifier of Microsoft's own Graph PowerShell application — it exists in every tenant, it already carries broad consent, and it costs nobody a setup step. TenuVault does this, and for a program you install on your own machine it is a sound choice. The security documentation now says why TUNO cannot follow, and would not want to." },
      { kind: "improved", tool: "All tools", text: "It is not a preference, it is a boundary. A web page signs in with authorization code and PKCE, and redeems the code with a request from its own origin. Entra permits that only where the redirect address is registered as belonging to a single-page application, and refuses everything else outright with a specific error. Microsoft's Graph PowerShell application is registered as a locally installed program: its redirect addresses point at the machine the program runs on, which a binary can listen to and a web page cannot. No single-page address could be added to it either, because the application is Microsoft's rather than ours." },
      { kind: "improved", tool: "All tools", text: "And three reasons it would still be the wrong answer if it worked. The consent record in a customer's directory would name Microsoft's tool rather than this one, so nothing in the tenant would record that TUNO had been used or what it had been allowed to do. It would inherit consent nobody granted it — that application is already widely consented, usually far more broadly than TUNO asks — which is the exact opposite of asking per tool at the moment of use. And every read would appear in the sign-in log as Microsoft's tool, so during an incident review the question of who read the configuration would have no answer." },
      { kind: "improved", tool: "Roadmap", text: "The tenant-sync card names the same shortcut and refuses it there too, because that is where it will be most tempting: syncing between two tenants means persuading two directories to trust you, and doing it on somebody else's registration would mean neither of them recorded that they had. There is also a practical objection worth knowing — tenants increasingly restrict that application through Conditional Access precisely because it is broad and pre-consented, so a tool built on it stops working in exactly the security-conscious tenants it most wants to serve." },
    ],
  },
  {
    build: 10340, date: "2026-08-20", title: "Copying configuration between tenants, and why it is the sharpest thing here",
    items: [
      { kind: "improved", tool: "Roadmap", text: "R17, tenant sync, as a tool of its own rather than a corner of the restore card. Take configuration from one tenant and create it in another, whole types or named policies, for the jobs nobody has a good answer to: standing up a new customer from a known-good baseline, keeping a pilot tenant and production in step, lifting a policy set out of a tenant being decommissioned. A managed-service provider does this by hand, in the portal, once per tenant, and it is the most repetitive thing in the job." },
      { kind: "improved", tool: "Roadmap", text: "Two routes, and the difference is not a detail. Backup-mediated: export one tenant, sign into the other, restore the archive — one sign-in at a time, and it leaves a file on disk saying exactly what was copied. Live: both tenants signed in at once, read from one and write to the other in a single act. The card says the backup-mediated route should exist FIRST, because it is the one that cannot surprise you." },
      { kind: "improved", tool: "Roadmap", text: "The live route breaks an assumption the whole application rests on, and the card says so rather than leaving it to be discovered. TUNO holds one signed-in account, and every safety rule it has assumes you are looking at the tenant you are changing — the collision stop, the member count before an assignment, the tenant recorded in a backup manifest. With two tenants held at once, a mistake is no longer a duplicate policy in front of you; it is configuration created in the WRONG TENANT, quite possibly a customer's. It is buildable, and it needs the source and target named in words at every step and a confirmation that types the target's domain rather than clicking a button. It should be built last." },
      { kind: "improved", tool: "Roadmap", text: "What cannot travel is written on the card rather than found at the far end. Assignments do not copy: a group identifier means nothing in another tenant, and one that happens to resolve there is worse than one that fails. Secrets do not copy, for the same reason they are absent from a backup — Graph never returned them — so anything referencing a certificate, a wireless key or an encrypted setting arrives configured but incomplete, and every one gets named. Objects that cannot be recreated at all are reported as skipped rather than attempted. And a sync with no name prefix is refused when the source and target are the same tenant, because at that point it is not a sync, it is silently duplicating your own configuration." },
    ],
  },
  {
    build: 10339, date: "2026-08-20", title: "What the backup tool should grow into",
    items: [
      { kind: "improved", tool: "Roadmap", text: "Ugur Koc's TenuVault read and mapped against the backup tool, which came off worse. It backs up twenty-seven policy types where TUNO covers five, and the twenty-two missing are not obscure ones: security baselines, Autopilot, enrolment, the three kinds of update profile, macOS custom attributes, assignment filters, scope tags, device categories, terms and conditions, notification templates, app configuration, app protection for three platforms, information protection, app categories. Most of them TUNO already READS somewhere — the documenter covers thirteen surfaces — so the gap is the archive rather than the plumbing. R15." },
      { kind: "improved", tool: "Roadmap", text: "It also writes a checksum per file and can verify an archive offline, reporting what was modified, what is missing and what turned up that should not be there. An archive nobody can check is a file you hope is a backup. And it has one trick worth copying exactly, which is the smallest thing in the whole project: the manifest is written LAST, so its presence is what proves the run finished, and a folder without one is hidden rather than offered. TUNO builds its zip in one act so a half-written archive is less likely — but less likely is not cannot, and a manifest that only exists on success is a better guarantee than a promise." },
      { kind: "improved", tool: "Roadmap", text: "R16, and it is the one that does not need a tenant at all: drop two archives onto the page and read what changed between them, without signing in. Almost nothing here works that way — only the AppLocker analysis. It answers the questions a backup is actually kept for. What changed between Friday and Monday. What differs between the pilot tenant and production. The change audit answers the first already, but only for thirty days and only if the change went through Intune; two archives answer it for as long as you kept them, and the field-level diff that tool already carries would do the comparing." },
      { kind: "improved", tool: "Roadmap", text: "The restore card took two rules from the same project. CREATE ONLY: never patch, never delete, the one exception being rolling back an administrative template it failed halfway through creating itself — cleaning up its own mess rather than touching yours. And a name prefix on everything restored. Together those turn the silent-duplicate problem into a visible one: you still get a second copy, but it is labelled and nothing you had was altered to produce it. TUNO keeps its collision stop on top, because a labelled duplicate is better than an overwrite and worse than being asked. One more detail worth stealing outright: it creates restored Conditional Access policies disabled, and that principle generalises to anything whose restoration could immediately change who gets access." },
    ],
  },
  {
    build: 10338, date: "2026-08-20", title: "Four more on the roadmap, and what is already covered",
    items: [
      { kind: "improved", tool: "Roadmap", text: "Two community projects read and mapped against what TUNO already does, which turned out to be most of one of them. Ugur Koc's Intune Assignment Checker is a suite of fifteen commands, and its assignment reading — by group, by device, All Users and All Devices, filters shown inline, unassigned policies, scope tags — is already shipped here across the Group Analyzer, the Device analyzer and the Documenter. Adding a tool that repeated it would have been the easy thing and the wrong one. What it has that nothing here does went onto the roadmap as three separate items rather than one blanket port." },
      { kind: "improved", tool: "Roadmap", text: "R11, assignment what-if: before you add somebody to a group, what will they receive, and if you remove them, what will they lose — as a delta against what they have today, including applications that would be uninstalled. Plus comparing two or more groups side by side. This is the standout: every tool here answers what IS, and none answers what WOULD BE, which is the question asked while a change is still cheap to reverse. It also brings the third subject, since TUNO can start from a group or a device but not yet from a person." },
      { kind: "improved", tool: "Roadmap", text: "R12, assignment health: the assignments that exist and reach nobody. A policy assigned to an EMPTY group has never applied and never will, and the portal does not surface it — which is a different fault from the dangling references the Group Analyzer already finds, where the group is gone rather than merely empty. With failed deployments and unassigned policies alongside it, because all three are the same shape of problem: something that looks configured and is not. And R14, searching setting DEFINITIONS rather than policy names — which policies configure BitLocker and to what value — kept separate because it needs the definition catalog itself rather than anything from your tenant." },
      { kind: "improved", tool: "Roadmap", text: "R13, an assignment editor, after Maxime Guillemin's Intune Toolkit: add and remove assignments in bulk across every policy, app and script type, with backup and restore of the assignments alone rather than the whole configuration. It is flagged as writing to the tenant, and the card is explicit that it would be the first thing here to write AT SCALE — the mistake it enables is not a duplicate policy but a policy reaching the wrong population, which nobody notices until a laptop behaves differently. Two of that project's features are deliberately left off the card: it can hard-delete and rename policies, and neither belongs in a browser tool whose argument is that it cannot do much harm." },
    ],
  },
  {
    build: 10337, date: "2026-08-20", title: "Third promotion — the queue is empty",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves to build 5 and takes queue items 20 to 29 with it: Backup, the Configuration documenter, the Device analyzer, Role assignments, the fix for consent triggering multi-factor authentication, the spacing pass, and today's two changes. Every tool on this channel is now also in production." },
      { kind: "improved", tool: "All tools", text: "The queue is empty for the first time, and that is a state worth keeping rather than a gap to fill. What remains between the channels is only the two permanent entries: this queue itself, which has no meaning on a production site, and the absence of a CNAME here, which must never be promoted. The next item added will be the whole of the next promotion." },
      { kind: "improved", tool: "All tools", text: "Every roadmap card now carries both numbers \u2014 the beta build it shipped in and the production build it reached. On production the same cards name the production build alone, because a beta number means nothing on a channel that has no beta." },
    ],
  },
  {
    build: 10336, date: "2026-08-20", title: "Open the policy where you are standing",
    items: [
      { kind: "improved", tool: "\ud83d\udda5 Device analyzer", text: "The table said which policies reach a device. The next question is always what they set, and answering it meant leaving for the Documenter and finding the policy again by name. Policy names are now buttons: press one and the settings open underneath it, read on demand, one policy at a time. Press it again and it closes. Settings catalog, administrative templates, device configuration and compliance policies all open; an object that keeps no readable settings list says so rather than opening onto nothing." },
      { kind: "improved", tool: "\ud83d\udda5 Device analyzer", text: "It renders through the Documenter's own readers rather than its own, and that is a safety decision rather than a tidiness one: redaction lives in those readers. A second implementation here would be a second place for a script body, a certificate or a pre-shared key to escape. The standalone HTML export stays static \u2014 a downloaded report has no tenant to call, so a button in it would be a button that cannot work." },
      { kind: "improved", tool: "\ud83d\udcc4 Configuration documenter", text: "The platform filter moved into the selection bar, which follows you down the page. Flipping platform is what you do repeatedly while scanning a tenant's worth of policies, and the setup form it used to live in scrolls away after the first screenful. Its value is now held by the tool rather than read off the control, because the bar is rebuilt on every change and something that gets destroyed and remade cannot also be where the answer is kept." },
      { kind: "improved", tool: "\ud83d\udcc4 Configuration documenter", text: "The line explaining why the filters are switched off before a read now says where the platform filter went, instead of describing a control that is no longer on that part of the screen." },
    ],
  },
  {
    build: 10335, date: "2026-08-20", title: "Second promotion — Group Analyzer and Change audit are production",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves to build 4 and takes queue items 14 to 19 with it: the two tools that read a tenant, the setup-script fix, the single column width with Help reordered, and the consent fix that makes a missing permission prompt rather than report a surface as unreadable. Items 20 to 28 remain the gap \u2014 Backup, the Documenter, the device analyzer, role assignments, and the MFA fix." },
      { kind: "fixed", tool: "All tools", text: "The request was for items 16 to 19, and that range does not exist as a thing that can be promoted: item 18 changes the Group Analyzer's own file, which is item 14, and items 17 and 19 both edit a page that by then already carries its tile, screen and tab. Only item 16 stood alone. The queue is a list of decisions, not a list of independent patches, and this is the first time that distinction has cost anything \u2014 worth remembering when picking a range." },
      { kind: "improved", tool: "All tools", text: "The roadmap cards for both tools now carry their production build alongside the beta one they were promoted from, which is the step of a promotion that gets missed." },
    ],
  },
  {
    build: 10334, date: "2026-08-20", title: "Granting a permission stops asking you to prove who you are",
    items: [
      { kind: "fixed", tool: "All tools", text: "Consenting to a scope walked you through multi-factor authentication every time. The consent popup was being opened with an instruction to re-prompt, and that instruction re-authenticates as well as re-consents \u2014 so a permission grant, which should be one click on a screen listing what is being asked for, became a full sign-in with a second factor. ENCA has never sent that instruction and has never had the problem; TUNO now matches it." },
      { kind: "fixed", tool: "All tools", text: "It was added in build 10310 to make the consent prompt appear at all, which was the wrong fix for a real symptom: the prompt was missing because the code that decides whether a failure needs a human did not recognise the specific error the tenant returns when consent is what is missing. Build 10322 fixed that properly. From then on the instruction bought nothing and cost an MFA challenge every time, which is the shape of a workaround outliving the bug it was working around." },
    ],
  },
  {
    build: 10333, date: "2026-08-20", title: "This channel stops claiming the production domain",
    items: [
      { kind: "fixed", tool: "All tools", text: "The CNAME file naming tuno.limon-it.nl was on this branch as well as on production. It had been there since the scaffold \u2014 the beta branch was cut from build 1 and inherited it, and nobody took it off \u2014 so the beta site, which is served from its github.io address and wants no custom domain at all, was telling GitHub Pages it owned the production one. Two sites naming one domain fight over it, and the losing side is whichever GitHub decides; the README has said so in as many words since the beginning. It is removed here and only here. Production keeps its CNAME, and the promotion queue now lists this absence as something that must never be promoted \u2014 a merge carrying the deletion across would take production off its own domain." },
    ],
  },
  {
    build: 10332, date: "2026-08-20", title: "The first promotion — items 1 to 13 are production",
    items: [
      { kind: "improved", tool: "All tools", text: "Production moves from build 2 to build 3, and takes queue items 1 through 13 with it: the AppLocker tool as a whole workflow rather than a policy reader, TUNO's own mark, the tab bar, the Graph layer, and a registration script that declares what the tools ask for. That is beta builds 10301 to 10317, which is exactly where item 13 ends and item 14 begins, so the cut needed no picking apart. The five tools that came after stay here \u2014 items 14 to 27 are still the gap." },
      { kind: "fixed", tool: "All tools", text: "Two files the promotion needed were missing from the item that described it. The TUNO mark was recorded as three SVGs and one HTML file, but the dark-mode logo is swapped by a stylesheet rule and the logo path is also set at runtime from branding.js \u2014 and that copy was unversioned, so it would have overwritten the cache-busting in the HTML and served the old mark from cache regardless. Both were caught by reading the tree instead of trusting the list, and the rule about what belongs in an item's file list now says so in the promotion queue's own header." },
      { kind: "fixed", tool: "All tools", text: "The roadmap card for the AppLocker tool now reads what is actually true on each channel: production 3 here, alongside the beta build it was promoted from. Getting that wrong on one channel and right on the other is the step of a promotion that gets missed, which is why the file says so in capitals." },
    ],
  },
  {
    build: 10331, date: "2026-08-20", title: "Who can change Intune",
    items: [
      { kind: "new", tool: "🛡 Role assignments", text: "The seventh tool. Every Intune role definition — built-in and custom — with each assignment under it, the users and groups in it resolved to names, the scope tags it is limited to and what it is scoped over, on one page. After Ugur Koc's Get Intune Role Assignments, whose question this is. The portal makes you click into each role one at a time, which is the practical reason nobody audits this." },
      { kind: "new", tool: "🛡 Role assignments", text: "Member names are resolved in ONE call rather than two per member, and that is the improvement worth naming. The original asks Graph for /users/{id} and, when that comes back empty, /groups/{id} — up to two round trips for every principal, on a surface where a hundred members across a dozen assignments is ordinary. Every principal in this report goes through directoryObjects/getByIds instead: a thousand at a time, in one request, and the response says what TYPE each object is, so there is nothing left to probe." },
      { kind: "new", tool: "🛡 Role assignments", text: "Two things the original does not read, both of which change the answer. Scope tags, which are how a tenant gives a regional helpdesk a role over only its own devices — a report that omits them overstates every scoped assignment it lists. And the scope itself, read from scopeType and resourceScopes together, so that \"all devices\" and \"Graph did not say\" come out as different answers. The original falls back to the literal word \"All\" when the scope list is empty, which is right often enough to be dangerous." },
      { kind: "new", tool: "🛡 Role assignments", text: "The most important line on the screen is the one about what is NOT in the report. This is Intune RBAC only. Global Administrator and Intune Administrator are Entra directory roles; they grant full access to Intune, and Graph does not return them from deviceManagement/roleAssignments at all. An access review that reads this as the complete list of who can change Intune is missing the accounts with the most access — so the sentence is on the screen and in all three exports rather than in a footnote." },
      { kind: "new", tool: "🛡 Role assignments", text: "Roles with nobody in them are one toggle away rather than hidden. An empty role is one membership change from being a live grant, which makes it a fact worth seeing; it is off by default because most tenants use a handful of the built-in roles and the rest is noise on a first read. Switching it on re-renders what has already been read and asks the tenant nothing." },
      { kind: "new", tool: "🛡 Role assignments", text: "Concentrations worth a second look are flagged as observations, and there is deliberately no score. An assignment scoped to all devices; a broad built-in role with a lot of members, at a threshold the screen prints because it is arbitrary; a role whose members are all one group, where whoever can change that group's membership can grant the role without ever holding it. Whether any of those is wrong depends on things this tool cannot see, and a number would invite somebody to chase it rather than read it." },
      { kind: "new", tool: "🛡 Role assignments", text: "An assignment whose role definition could not be resolved is kept, under a heading that says exactly that, rather than dropped. It still grants access — dropping it would take a live grant off an access review, and the version of the original before its own 1.2 fix filed all of them under one hardcoded \"Unknown\"." },
      { kind: "improved", tool: "🛡 Role assignments", text: "The per-assignment read asks for the role definition and the scope tags in one expand, and falls back to the role definition alone where a tenant refuses the combined form — the scope tags are then named from the tenant's own scope-tag list instead. The report says which assignments took the fallback, so a tag shown as a GUID is a lookup that failed rather than a tag with no name." },
    ],
  },
  {
    build: 10330, date: "2026-08-20", title: "Why did THIS device get that policy",
    items: [
      { kind: "new", tool: "🖥 Device analyzer", text: "The sixth tool, and the Group Analyzer read from the other end. Name one machine — by device name, serial number, Intune device id or Entra device id, because every device has two GUIDs and which one ends up on a support ticket is a coin toss — and the same assignment graph is walked backwards. The groups the device itself is in, the groups its primary user is in, and the tenant-wide targets that name no group at all. Every row says which of those put the policy there: \"via group X\", \"via the primary user's group Y\", \"All Devices\"." },
      { kind: "new", tool: "🖥 Device analyzer", text: "The nine assignment surfaces are NOT a second copy of the Group Analyzer's list. This tool builds a match set out of the device's memberships and hands it to that tool's own source table. A second copy would have drifted the first time somebody added a surface to one and not the other, and the failure would have been silent: a device reported as unaffected by a workload nothing ever looked at." },
      { kind: "new", tool: "🖥 Device analyzer", text: "Tenant-wide assignments are ON by default here, which is the opposite of the Group Analyzer and is deliberate. For a group, All Devices is the same answer every time and says nothing about the group you asked about. For a device it IS the answer — All Devices reaches this machine whatever else is true — so leaving those rows out would produce a report missing policy the device is definitely getting. All Users lands on the person rather than the machine, so on a device with no primary user it reaches nothing, and the row says that rather than quietly disappearing." },
      { kind: "new", tool: "🖥 Device analyzer", text: "The last check-in is the first thing on the page, not a field in a details table. Everything an assignment tells you is what Intune INTENDS; what the machine actually has is whatever it collected the last time it talked to the service. A policy assigned ten minutes ago has not reached a device that last synced yesterday, and the report says so in those words rather than leaving a correct-looking list to be misread as a confirmation." },
      { kind: "new", tool: "🖥 Device analyzer", text: "Intended and actual sit in adjacent columns, and the gap between them is the whole reason to use this. Where the device has reported back on a configuration profile or a compliance policy, its own verdict is shown next to the assignment. Where those endpoints refuse, come back empty, or cannot be tied to a policy with certainty — two policies sharing one name is enough to make a match ambiguous — the state is UNKNOWN. It is never \"fine\" by omission, and the report names which of those three happened." },
      { kind: "new", tool: "🖥 Device analyzer", text: "A filtered assignment is an open question and is shown as one. Whether a device filter includes THIS device is decided by the service against inventory a browser cannot see, and Graph offers no call that answers it. Those rows say \"may reach it\" and name the filter. They will never say yes, and a tool that pretended otherwise would be wrong in exactly the cases somebody is looking this up to resolve." },
      { kind: "new", tool: "🖥 Device analyzer", text: "An exclusion beats an inclusion, which is Intune's own rule — except where the two arrive through different kinds of group. A policy included through a device group and excluded through the primary user's group depends on whether the policy is targeted at users or at devices, and the assignment does not say which. That case is called a conflict and handed to the portal, rather than answered with a coin toss." },
      { kind: "improved", tool: "All tools", text: "One new permission: Device.Read.All, read-only and admin-consent. The Entra device object is not the Intune enrolment record — Intune's own scope reads the enrolment, and only the directory can say which groups a machine is IN. It costs a tenant one more consent round trip, which is why it goes on with the tool that needs it rather than ahead of it. Without it the device analyzer still runs, and reports the device's own group memberships as unknown rather than as none." },
      { kind: "improved", tool: "🖥 Device analyzer", text: "Filtering the device inventory server-side is not supported for every property on every tenant, and a refusal on one field must not lose a good match on another. Each filter is tried on its own; when they are all refused the inventory is listed and matched in the browser instead, stopping after ten pages. The report says which route answered and how many devices were read, so a slow run is explained rather than mysterious — and a scan that stopped early says it stopped early." },
    ],
  },
  {
    build: 10329, date: "2026-08-20", title: "All five platforms, every time",
    items: [
      { kind: "improved", tool: "📄 Configuration documenter", text: "The platform list is fixed now: Windows, macOS, iOS and iPadOS, Android, Linux, plus the bucket for things that target no platform at all. Every time, whatever the tenant holds. The previous build assembled the list from what the read actually returned, which sounded tidier and was worse in practice — the options changed shape from one tenant to the next, so you could never learn where anything was, and an estate with a single platform got a control holding one entry and nothing to do with it." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "Each option carries its count, and that is what makes a fixed list honest rather than noise. \"Linux (0)\" tells an administrator something an absent Linux entry never could: not that the tool has nothing to say, but that the tenant has no Linux policies. Confirming an absence is a real question and it now has a real answer." },
      { kind: "fixed", tool: "📄 Configuration documenter", text: "A one-platform tenant used to have the control disabled entirely, on the reasoning that a choice which cannot change the result is worse than no choice. That was wrong for the same reason: it removed the ability to confirm what the tenant does not have. The control stays live." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "The per-platform counts deliberately do not add up to the total, and the label says \"All platforms\" rather than a sum because of it. A policy that targets two platforms is counted under both, which is the honest way to count something that genuinely lands on two estates." },
    ],
  },
  {
    build: 10328, date: "2026-08-20", title: "The platform filter had nothing usable in it",
    items: [
      { kind: "fixed", tool: "📄 Configuration documenter", text: "Reported: the platform filter had no platforms in it. Two separate things were wrong. Before a read it was empty and switched on, which is simply what an empty list looks like when nothing has told you why — it is disabled now until there is something to filter, with a line saying the list is built from what your tenant returns and cannot exist before then. And after a read it was worse than empty: it was inconsistent." },
      { kind: "fixed", tool: "📄 Configuration documenter", text: "Graph names the same platform three different ways depending on which surface you ask. A settings-catalog policy reports the string \"windows10\". A device configuration says nothing but implies Windows through its type. The first version passed whatever it found straight into the list, so a tenant holding both offered TWO SEPARATE ENTRIES FOR WINDOWS, each matching about half the estate and neither obviously wrong. Everything normalises to one vocabulary now — Windows, macOS, iOS and iPadOS together, Android, Linux — so one platform is one entry however Graph chose to spell it on the way in." },
      { kind: "fixed", tool: "📄 Configuration documenter", text: "A policy targeting two platforms was arriving as a single run-together string and matching neither of them. A policy now carries a LIST of platforms rather than one, because targeting two platforms is a thing a policy genuinely does, and it is found under either. Graph's own placeholder values for \"no platform\" and \"a platform we have not told you about yet\" no longer appear in the list as though they were platforms." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "A policy with no platform at all — a script, an assignment filter, a scope tag — is filterable as \"Not platform-specific\" instead of quietly vanishing from every filtered view. It sits last in the list and only appears when something actually is. And where the whole read came back as a single platform, the control stays disabled and says so in the option text: offering a choice that cannot change the result is worse than offering none." },
    ],
  },
  {
    build: 10327, date: "2026-08-20", title: "Pick what goes in the document",
    items: [
      { kind: "improved", tool: "📄 Configuration documenter", text: "Clicking a policy opens a popout instead of unfolding it where it sits. Expanding in place was wrong for a list this long: a settings-catalog policy carries twenty-odd rows, so opening one pushed everything else off the screen, and closing it again lost your place in a list of four hundred. The popout is the same one the sister tool uses, with the same behaviour — Escape closes it, so does the backdrop, and it carries the policy's assignments, its description and the endpoint the data came from alongside the settings table." },
      { kind: "new", tool: "📄 Configuration documenter", text: "Policies are selectable, and THE EXPORTS FOLLOW THE SELECTION RATHER THAN THE FILTER. Those are two different jobs: the filter narrows what you are looking at, the tick boxes decide what you are sending. Keeping them apart is the whole point — you can filter to find three policies, tick them, clear the filter, and still export exactly three. A bar above the list keeps the count and offers select all, select everything currently shown, invert, and select none; a whole section can be ticked from its heading; and the popout can tick the policy you are reading without closing it." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "Everything is selected after a read, because the common case is documenting the tenant and starting at nothing would leave the export buttons dead with no explanation. With nothing selected they disappear instead: a button that would produce an empty document is worse than no button at all." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "The document header now distinguishes a selection from a filtered view. \"I picked these twelve\" and \"a platform filter left twelve\" are different claims about the same short document, and neither is \"this is the tenant\" — a reader who has to act on the thing deserves to know which one they are holding." },
      { kind: "fixed", tool: "All tools", text: "The build stamp on the sign-in screen was showing a time from an earlier build. The released timestamp had been left behind by the previous release while the build number and date moved on, so the footer and the sign-in screen were quietly claiming this build was cut the previous evening. It is set from the clock now, as the file's own instructions have always said to do." },
    ],
  },
  {
    build: 10326, date: "2026-08-19", title: "The summary strips stop wearing a box",
    items: [
      { kind: "fixed", tool: "All tools", text: "The sticky summary strip at the top of Group use, Change audit, Backup and Documenter was marked as a card as well as a strip. A card draws a border; the strip sets its own padding to nothing horizontally, because it is meant to sit on the page background rather than inside a frame. Together that produced a box with its heading and its numbers pressed against the frame. It is a bare strip now, which is what ENCA has always had and what the styling was written for. It also follows the measured height of the header and tab bar instead of assuming they come to 106 pixels." },
      { kind: "fixed", tool: "All tools", text: "The result cards in those same four tools had no padding at all, so their prose ran to the border on both sides. Cards in this app carry no padding of their own \u2014 every one sets its own \u2014 and these four never did. One rule now covers them, so the text is inset and the tables inside inset with it." },
      { kind: "improved", tool: "All tools", text: "The summary pills are more compact: tighter padding, smaller type, less space between them. Four numbers do not need the height of a button. The figure is still larger than its label, so the number still leads." },
    ],
  },
  {
    build: 10325, date: "2026-08-20", title: "Read it, browse it, write it down",
    items: [
      { kind: "new", tool: "📄 Configuration documenter", text: "The fifth tool, and it does two jobs from one read. The list is the one you use on a Tuesday: every policy in the tenant on one page, filterable by name, by any setting inside it, by platform and by whether it is assigned to anything at all, with the settings expandable underneath and assignments resolved to group names rather than left as identifiers. The document is the thing an auditor asks for and nobody has. They are one tool because reading thirteen surfaces twice would be absurd — you browse what you are about to document, and document what you have just browsed." },
      { kind: "new", tool: "📄 Configuration documenter", text: "Three formats. Markdown for a wiki. A standalone HTML page that opens anywhere, needs no access to the tenant and prints cleanly to paper. And Word, for the auditor who is going to edit it — with real Word tables, so a settings list arrives as a table rather than a run of paragraphs somebody has to reformat before circulating it." },
      { kind: "new", tool: "📄 Configuration documenter", text: "Secret-bearing values are redacted, and there is deliberately no way to turn that off. This is the most widely circulated thing the tool can produce: it goes to auditors, into wikis, onto shared drives. A script body, a certificate, a pre-shared key or an encrypted setting inside one of those is a disclosure nobody can recall afterwards. Every such field is replaced with a marker, so the document still records that a value is set — which is what a reviewer actually needs to know — without carrying the value itself." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "The filters apply to the exports, and the exports say so. Narrow the list to unassigned Windows policies and the document holds exactly those; every export header states how many of how many objects it contains and which filters produced it. A document of forty policies that reads as a document of the whole tenant is worse than no document, and this is the line that prevents it." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "Unknown is stated rather than hidden, and it is handled differently here than in the backup tool — on purpose. A whole section that could not be read is named as unreadable and left out. But a single policy whose settings could not be retrieved is still LISTED, with the gap stated on the row. A backup excludes such an object because restoring it would recreate an empty policy; a document is for reading, and knowing that a policy exists and that its configuration could not be retrieved is worth more than the policy quietly disappearing." },
      { kind: "improved", tool: "📄 Configuration documenter", text: "It is a reimplementation rather than a fork, and the reason is worth stating: the project whose coverage plan decided which surfaces belong in a tenant document is licensed under terms that permit self-hosting but not offering the work as a service. This is built from its published list of surfaces and shares none of its code. That project also keeps a server tier which its browser hands a token to; TUNO has nowhere to put one, so the reading happens in the tab — which is, as it happens, the more literal version of running in your browser." },
    ],
  },
  {
    build: 10324, date: "2026-08-20", title: "Take a copy, and say what the copy cannot do",
    items: [
      { kind: "new", tool: "📦 Backup configuration", text: "The fourth tool. Everything Intune holds as configuration — device profiles, settings catalog policies with their settings, compliance policies with their scheduled actions, administrative templates with their definition values, and platform scripts for Windows and macOS with their bodies — read out and written to one zip file in this tab. Nothing is uploaded and nothing is stored: the archive is built in memory and handed to your browser's download. One permission, read-only, asked on the click." },
      { kind: "new", tool: "📦 Backup configuration", text: "The folder names and file names match the PowerShell original exactly, and that is a deliberate constraint rather than laziness. Restore is driven by folder names — in his script and in the one TUNO has planned — so an archive written here can be restored by his, and one written by his can be read back here. A tidier naming scheme would have cost that, and interoperability is worth more than tidiness." },
      { kind: "new", tool: "📦 Backup configuration", text: "Three of the five areas cost an extra request per object, because the list endpoint returns a count or an empty field where the content should be: a settings-catalog policy's settings, an administrative template's definition values, a script's body. The original asks for these one after another with a pause between, which on a tenant with four hundred policies is more than a minute of waiting. They run several at a time here, with failures caught per object rather than per run." },
      { kind: "improved", tool: "📦 Backup configuration", text: "An object whose detail could not be read is left OUT of the archive rather than written without it. A settings-catalog policy restored with the right name and no settings looks exactly like a restore that worked, which makes it worse than a policy that is visibly missing. The same principle one level up: an area that could not be read at all marks the whole archive as partial, on the screen and in the manifest, so nobody restores from it believing it complete." },
      { kind: "improved", tool: "📦 Backup configuration", text: "The manifest says what the archive cannot do. It keeps the original's four fields so the shape stays familiar, and adds an index of every file with its type, which Graph surface it came from and whether it can actually be restored — plus the tenant it was taken from, because six months later the question is whether this is the right tenant. Two limits are written down at backup time rather than discovered at restore time: Graph never returns secret values, so encrypted settings, passwords and certificates are references that have to be entered again by hand; and a script saved without its body can be read but not put back, which the original's own switch produces without saying so." },
    ],
  },
  {
    build: 10323, date: "2026-08-20", title: "Who changed it, and what actually moved",
    items: [
      { kind: "new", tool: "🕓 Change audit", text: "The third tool. Somebody changed something and a fleet of devices behaved differently the next morning — this reads the tenant's own audit log and says who, what, when and from where. Two views over one read, because both are the same Graph call and two tiles would be two copies of the same paging, throttling and export. Policy changes carries a severity on each event: a delete or a failure outranks a create, a create outranks an assignment change. All events shows every category with filters on actor, activity, category and result. Filters apply to what has already been read, so changing one costs nothing and asks the tenant nothing. Reads only." },
      { kind: "new", tool: "🕓 Change audit", text: "The field-level diff is the reason to use this rather than the portal, and it is ENCA's, ported. The values the audit log carries arrive as text that is sometimes JSON, sometimes JSON inside JSON, and sometimes a single-element list wrapped around the thing you wanted — so a naive reader prints a wall of escaped braces. This unwraps them and reports what moved, field by field. Lists are compared as sets, which is what an assignment list actually is, so you see what was added and what was removed rather than two versions of the whole thing side by side. Both scripts this is built on print one before-and-after pair and stop, which for a settings-catalog policy means several kilobytes of JSON in a single spreadsheet cell." },
      { kind: "fixed", tool: "🕓 Change audit", text: "Three bugs inherited from the originals, fixed on the way in. The two scripts ask the same endpoint for a time window in two different ways, and one of them uses a bare date — which means midnight, so \"the last day\" was anything between a minute and twenty-four hours depending on when you ran it. Both use a full timestamp here. The category filter shipped with a fixed list containing values Graph never returns, so picking one gave you nothing and read as a quiet tenant; the list is built from what your tenant actually emitted. And the policy view pages the entire result set and then keeps the first five rows — not a page size, a cap applied after all the work was done. There is no cap." },
      { kind: "fixed", tool: "🕓 Change audit", text: "Every resource on an audit record is read, not just the first one. An assignment change routinely records the policy in the first slot and the group in the second, so a tool that reads only the first was dropping the answer to \"who got this\" — from the tool whose entire job is to say what changed." },
      { kind: "improved", tool: "🕓 Change audit", text: "The time windows stop at thirty days, and that is a fact about Intune rather than a limitation of this tool: after thirty days the audit data is gone from the service. ENCA offers a ninety-day review window because the directory audit log keeps longer, and carrying that over would have produced a confident, empty answer for days thirty-one to ninety that looks exactly like a tenant where nothing happened. The screen says why the option is missing instead." },
      { kind: "improved", tool: "🕓 Change audit", text: "An event with no detail says so. Some records carry no field-level information at all — a create, usually, or something the service did on its own — and rendering those as an empty list invites the reading that nothing changed. \"Nothing changed\" and \"the log does not say\" are different answers and the tool gives whichever one is true. Wildcards in the actor and activity filters are honoured as wildcards too, rather than being quietly widened into substring matches the way a careless port would." },
      { kind: "fixed", tool: "All tools", text: "A comment left behind by the previous build. The screen-switching code still explained that the AppLocker tool needs more room than the standard column, which stopped being true the moment every screen was given the same width. It now describes what the line actually does, which is take that class off." },
    ],
  },
  {
    build: 10322, date: "2026-08-20", title: "It asked for permission by not asking",
    items: [
      { kind: "fixed", tool: "All tools", text: "Every Intune read failed with a permission error, and no permission prompt was ever shown. Graph refuses a token you have not consented to with a specific code — the one that literally says the user or administrator has not consented — and the check deciding whether an error meant \"ask the person\" did not recognise it. It looked for three older codes, found none of them, and treated the single most common consent error as a hard failure. So the tool reported nine surfaces as unreadable while the thing that would have fixed it, a consent prompt, was one branch away and never taken. It is recognised now, along with the multi-factor step-up codes that have the same character." },
      { kind: "fixed", tool: "🔗 Group Analyzer", text: "A sweep wanted nine consent prompts and would only ever have got one. Each surface asked for its own permission at the moment it ran, which is right for a single read and wrong for a run of nine: browsers allow a pop-up on the click that opened it, so the first might have appeared and the rest would have been blocked — leaving a run that reported eight permission failures for permissions nobody was asked about. A run now declares everything it needs and asks once, before it reads anything. The principle is unchanged and the granularity is better: permission is still requested on the click and never at sign-in, and a run limited to configuration profiles still asks only for configuration. It asks per RUN rather than per request, which is the unit a person can actually consent to." },
      { kind: "fixed", tool: "All tools", text: "An administrator-only permission was being reported as a declined prompt. Entra has two different refusals here — you said no, and you are not allowed to say yes — and the second one contains the word consent, so testing for consent first caught both. The effect was that somebody without the rights would be told to try again and accept the prompt, which for them can never work. Administrator is checked first now, and says so, with the tenant-wide consent link." },
      { kind: "improved", tool: "🔗 Group Analyzer", text: "A refused permission stops the run instead of half-running it, and the message says what to do next: retry and accept, or hand it to an administrator. Nothing is read, so there is no page of surfaces marked unreadable to wade through, and no export buttons for a run that produced nothing. A pop-up blocked by the browser now says so as well, rather than reading as a refusal from the tenant." },
    ],
  },
  {
    build: 10321, date: "2026-08-20", title: "One column width for the whole app",
    items: [
      { kind: "fixed", tool: "All tools", text: "The two tools opened wider than everything else. Each had been given a 1680-pixel shell for a good local reason — the AppLocker screen shows an audit table and the code it produces side by side, the Group Analyzer shows a five-column assignment table — while the tools home, Help, What's new and the Roadmap all stayed at 1180. Moving between them made the application appear to change size, and the header and tab bar changed with it, so it read as a rendering fault rather than as a choice. Everything is one width now, centred, the same as the home screen. The mechanism for widening a screen is still there and still documented, in case a future tool genuinely earns it; it just has nothing in it." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The two-column split was drawn for the wider shell and had to give ground. Its code panel could previously demand 400 to 520 pixels, which was comfortable at 1680 and left the audit table under 700 at 1180 — narrow enough that finding text wrapped every few words. The panel floors lower now and the table keeps the remainder — at a typical desktop width the table gains about eighty pixels. No new breakpoint: the split already stacked below 1240, and the second one this build briefly added moved where that happened, which the layout tests caught." },
      { kind: "improved", tool: "Help", text: "Help is in the order somebody actually reads it, and matches ENCA's. What TUNO is, then — on a beta channel only — what this build has that production does not, because on a preview site that is the first thing worth knowing. Then how each tool works and the security model. Credits and thanks last, where a list of acknowledgements belongs rather than wedged between the tools and the security section. Each part is its own card instead of one long one, so the sections are separable at a glance." },
    ],
  },
  {
    build: 10320, date: "2026-08-20", title: "The setup script stopped rewriting its own instructions",
    items: [
      { kind: "fixed", tool: "All tools", text: "Running New-TunoAppRegistration.ps1 quietly damaged the file it was patching. It writes the new client ID into js/authConfig.js, and that file mentions the setting twice: once as the real value, and once inside a comment showing somebody running their own fork how to point at THEIR registration instead. The replace was not anchored, so it rewrote both — turning the instructions for using your own tenant into a hardcoded copy of ours. Nothing failed, nothing was logged, and the only way to notice was to read a diff. It is anchored to an actual assignment now: a line that begins with the setting rather than with two slashes." },
      { kind: "improved", tool: "All tools", text: "The same script now counts before it writes. It expects exactly one assignable line and refuses to touch the file otherwise, saying so in red. Zero matches would mean the tool keeps whatever registration it had and signs in to the wrong one; more than one would mean the anchor has stopped discriminating and the comment is about to be overwritten again. Either way the honest move is to stop and say which, rather than to write something and report success." },
    ],
  },
  {
    build: 10319, date: "2026-08-20", title: "Ask the whole tenant at once",
    items: [
      { kind: "new", tool: "🔗 Group Analyzer", text: "The tenant sweep, ported from ENCA. Instead of naming one group, ask the question of every group at once and get a table: how much each one receives, split into direct, inherited and excluded, with a column per surface. The trick that makes it affordable is that a sweep reads each surface ONCE and matches every group against it — three hundred groups is still twenty reads, and the cost barely grows with the group count. That falls straight out of how the single-group mode was already written, which is why it fits in one build rather than being a second tool." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "The default scope reads no groups at all. \"Only groups Intune assigns to\" takes the group ids off the assignments themselves as they are read, so there is no directory enumeration anywhere in that run. It is the fastest scope and the one that matches what this tool is for. It also CANNOT find groups nothing assigns to — every group in it is used by definition, so an empty unused list there would be a tautology dressed up as a clean bill of health. The screen says so where you make the choice, the export repeats it, and the count shows a dash rather than a zero. Finding unused groups means enumerating groups, which is what the counted scopes are for." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "An assignment can name a group that no longer exists. Nothing in the portal will tell you, and the assignment goes on targeting nobody indefinitely. The sweep keeps those ids, marks them as dangling, and leaves them in the table rather than quietly dropping them — an id that resolves to nothing is a finding, not a gap in the data." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "A name filter narrows which groups are scanned, so a prefix keeps a sweep of a twenty-thousand-group tenant to the set you care about. It runs server-side where Graph supports the shape and falls back to listing and filtering here when the tenant refuses. The local check is always the final word, and that is deliberate rather than belt-and-braces: the search Graph offers matches whole tokens rather than substrings, so trusting it would put groups in the report that do not match what you typed." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "Group nesting is off by default in a sweep, because it is the one part whose cost does grow with the number of groups — it is a lookup per group, batched twenty at a time. With it off, a group that only receives policy through a parent reads as zero and would land in the unused list, so the report says nesting was not walked in both places it matters. With it on, a parent's assignment counts for every child and is tallied as inherited rather than direct." },
      { kind: "fixed", tool: "🔗 Group Analyzer", text: "A guard that looked like protection was doing nothing. The sweep tally had an explicit check to stop tenant-wide assignments being counted against a group — reasonable-looking, and dead: the tally is built from the group list, so an id that is not a group in scope already had nowhere to land. A mutation test proved the line could be deleted without changing any outcome. It is gone, and the real guard is commented as the one that carries the weight. Dead code that reads as a safeguard is worse than no code, because the next person to read it will trust it." },
    ],
  },
  {
    build: 10318, date: "2026-08-20", title: "The second tool, and the first one that reads your tenant",
    items: [
      { kind: "new", tool: "🔗 Group Analyzer", text: "Name an Entra group and see everything Intune sends to its members: configuration profiles across device, settings-catalog and ADMX shapes, compliance policies, PowerShell and macOS shell scripts and remediations, application assignments with their install intents, app protection, app configuration, enrolment restrictions, Autopilot deployment profiles and Windows update profiles. Nine surfaces, twenty endpoints, one read. Every surface asks for its own permission at the moment it runs, so a run limited to configuration profiles never prompts for the applications permission. Reads only. Exports as Markdown, as CSV, or as a standalone HTML report that opens for somebody with no access to the tenant at all — which is the artefact you attach to a change request when you are arguing that a group must not be touched." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "Exclusions are answers, not omissions. A policy that names your group in an exclusion is listed beside the ones that include it, marked as an exclusion. Leaving them out would be tidier and would make the report wrong about half the time: \"this policy does not reach you\" is the same question as \"this policy reaches you\", and an admin reading a list of inclusions has no way of knowing which one they are looking at." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "Assignments your group inherits are included, and say where they came from. If the group is a member of another group, anything assigned to the parent lands on your members too — those rows appear with the parent named, rather than being left out because no assignment mentions your group directly. The PowerShell script this borrows its Intune surfaces from says in its own notes that it does not evaluate this. If membership cannot be read at all, the report says so and states that it is showing direct assignments only, instead of narrowing the answer without mentioning it." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "Tenant-wide assignments are one toggle away, and off until you ask. All Users and All Devices are not about your group — they are the same answer for every group, and putting them in by default would bury the rows that are actually about the group you named. But they do land on its members, so the surface that reaches those devices is larger than the group's own assignments, and nothing else on that page would have told you." },
      { kind: "new", tool: "🔗 Group Analyzer", text: "A surface that could not be read is reported as UNKNOWN, never as empty. If the applications permission is missing or the signed-in account has no Intune role that can read a workload, that surface is listed as unreadable with both the permission and the role it would need — those are two different things and having the scope is often not enough. Showing \"no assignments\" for a surface nobody managed to look at is the most dangerous thing this tool could do, so it does not." },
      { kind: "improved", tool: "🔗 Group Analyzer", text: "It stops at Intune, deliberately, and the tool says why on its own screen. Where a group is used in Conditional Access, group-based licensing, directory roles or access packages is the same question asked of Entra, and answering it needs permissions TUNO holds none of and has no reason to. Half-reading those surfaces with what is to hand would be worse than not reading them, so the answer is a link to ENCA's Group Analyzer, which is signed in for exactly that. The roadmap card used to promise the Entra areas as context; it now records that it changed its mind, and why." },
    ],
  },
  {
    build: 10317, date: "2026-08-20", title: "The registration script knows what the tools will ask for",
    items: [
      { kind: "improved", tool: "All tools", text: "New-TunoAppRegistration.ps1 gained the eight delegated read scopes the Intune tools need: configuration, apps, scripts, managed devices, service configuration, RBAC, group membership and directory users. All eight are read-only and all eight need an administrator to consent — an ordinary user cannot grant any of them, so a tenant that has not consented sees the ask refused rather than prompted, which is the correct behaviour but only if somebody knew to expect it. TUNO still holds exactly one write scope, and it is still only step 5 of the AppLocker tool that uses it." },
      { kind: "improved", tool: "All tools", text: "The eight went on together rather than one per tool, and that bends the rule written in the script itself, which says to add a scope when the tool that needs it lands and not in advance. The reason is the cost falls on someone else: every addition means another admin-consent round trip in every customer tenant, and eight of those spread across eight builds is a worse deal for them than one. What was traded away is the tight coupling between what is consented and what exists, so the script now names, per scope, which roadmap item reads it — the check is still possible, it just has to be read rather than inferred." },
      { kind: "fixed", tool: "All tools", text: "One scope on that list looks redundant and is not. The configuration read scope is there even though the read-write scope TUNO already holds would functionally cover every read of the same data. Entra consents by name: a token asked for the read scope is refused unless the read scope itself was consented, whatever else is on the registration. The alternative was to point the read-only tools at the write scope, which would mean a tool that only reports could, on any future bug, write. The extra consent line is the cheaper of the two, and the script says so where the list is." },
      { kind: "fixed", tool: "Help", text: "The security documentation's example of how to register TUNO without any write capability was left behind by this change — it named two scopes, which would now also strip out every read the Intune tools depend on. It lists the full read-only set instead, and gains a table of what each of the eight is actually read for." },
    ],
  },
  {
    build: 10316, date: "2026-08-20", title: "Six more tools named, and the plumbing they all need",
    items: [
      { kind: "improved", tool: "All tools", text: "TUNO learned to read a tenant. Everything the Graph layer could do until now was written for one act — create an AppLocker profile, assign it, stop — and every tool on the roadmap asks the opposite question: read the whole tenant and tell me what is in it. That needs four things a write path does not have, and all four are ENCA's rather than newly invented. Reads now retry: Graph answers a tenant-wide sweep with 429 and a header saying how long to wait, and honouring it is the difference between a sweep that finishes and one that dies at the first quota. Writes still do not retry, for the same reason as before — a POST that timed out may already have created something. Paging follows the server's own next-page link, which means following a URL the server chose, so the host is now checked before the token is attached to anything. The beta endpoint is named at the call site rather than swapped into a string, because nearly every Intune assignment surface is missing or incomplete on v1.0 and that caveat should be visible where it applies. And the one-extra-request-per-object shape — settings for a policy, a body for a script — runs several at a time with per-item errors instead of one at a time in a line, so one policy you cannot read does not cost you the other 399." },
      { kind: "fixed", tool: "All tools", text: "Two ways to build a Graph query wrongly, closed together. Encode too much and a correct query comes back rejected: run an expand that contains brackets through the browser's own query builder and the brackets, commas and dollar signs all come back escaped, and the tenant answers 400 to something that was right. Encode too little and a group name with an apostrophe in it ends the filter early — and a name with an ampersand starts a whole new query parameter, which is an injection into your own request. The structure is written literally now and the values are escaped, on either side of the same interpolation." },
      { kind: "improved", tool: "Roadmap", text: "Six items, one honest refusal. R07 Intune role assignments, R08 Backup and R09 Restore of the configuration surface, R06 a tenant document that exports as Markdown, Word and PDF, and R03 the device analyzer moved up from Later — it is R04 read from the other end, so the two share an engine. R04 and R05 both grew: R04 gains exclusion assignments and a tenant-wide toggle, because \"this policy does not reach you\" is the same question as \"this policy reaches you\" and All Users lands on your group's members too; R05 becomes one tool with two views over the same audit read rather than two tiles duplicating the paging. Then R10, which is the refusal: a LAPS self-service portal cannot be a browser-only tool. Reading a password with delegated permission needs a role no ordinary employee holds, and the permission is tenant-wide with no per-device scoping, so \"only your own device\" is only true if a server the user cannot reach enforces it. The card says so, and scopes TUNO's version to the helpdesk side that does fit a static site." },
      { kind: "new", tool: "Help", text: "A Credits and thanks section, the same one ENCA carries. Spencer Alessi's AppLockerInspector and Microsoft's AaronLocker were already named inside the AppLocker tool but had nowhere to be listed; Ugur Koc's IntuneAutomation and IntuneDocumentation and Daniel Fraubaum's LAPS portal are the reading behind six roadmap items. Every entry links its source, names its author, says what it actually contributed and states its licence — one of them is Elastic 2.0 rather than an open-source licence, which is worth knowing before anyone assumes otherwise. Each roadmap card carries its own source link too, so a reference does not have to be traced through Help." },
    ],
  },
  {
    build: 10315, date: "2026-08-19", title: "Enforcing is a decision, not a button",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "A collection sitting in AuditOnly offered a one-click \"Set Enabled\". That is the single most expensive click in this tool, and it was the easiest one to make. Audit exists to be watched — across a month-end, a patch cycle, a new starter — and nothing in a policy file can tell the tool whether that has happened, so it had no business deciding. The button now opens a panel that lists what should be true first: time in audit that covers a real month, an event log with nothing left in it you do not recognise, a coverage table that says allowed for everything your users need, and a tested way to put a device back. You pick the mode. It is still one undo from wherever you were." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Two other routes to accidental enforcement closed with it. Adding a missing collection used to land it in Enabled the moment a default rule appeared — one click from \"this collection is absent\" to enforcing something nobody had ever audited. It lands in AuditOnly now. And a NotConfigured collection that already has rules steps to AuditOnly rather than straight to Enabled. The only enforcement change the tool will still make on its own is the one that blocks nothing." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The DLL collection says what it costs. AppLocker evaluates every DLL load: enforcing it measurably slows application start, and even auditing it buries the log under Microsoft-signed system libraries, security-product components and .NET native images. The scanner and the Intune export have always shipped DLL as NotConfigured for that reason — now the audit says the same thing in the same place you would make the decision, instead of quietly offering to enable it." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "The tool stopped arguing with itself. Click the coverage fix for per-user OneDrive and it adds a publisher rule for that vendor and that product — then flagged its own rule Medium for having no upper version bound and a broad principal, and marked it risky in the coverage table two panels up. Both complaints are wrong for that shape of rule: an upper bound on a named vendor product breaks the rule at the vendor's next release, and narrowing the principal on something every user needs is a helpdesk ticket, not a hardening. A rule naming both a publisher and a product is now read as the construction it is. The facts are still reported — an unbounded allow does mean a compromised update would run — at Info, with a recommendation that says why pinning a version would be worse. Rules that name no publisher, or a publisher and nothing else, are flagged exactly as hard as before." },
    ],
  },
  {
    build: 10314, date: "2026-08-19", title: "Help, What's new and the Roadmap sit in the middle",
    items: [
      { kind: "fixed", tool: "All tools", text: "The three prose screens hugged the left edge. They cap their column at 920 pixels so a line of text stays readable, which is right, but the cap had no auto margins \u2014 so the column sat hard against the left of an 1180-pixel shell whose header and tab bar used the full width, and the whole page read as knocked sideways rather than as a column. The measure stays; the column is centred now. It shows most on Help, which is the longest of the three, and it is the same one-line omission on all of them." },
    ],
  },
  {
    build: 10313, date: "2026-08-19", title: "Step 1 asks for one thing, and says what it does not need",
    items: [
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Step 1 offered two downloads, and the second one has nothing to do with scanning a device. Convert-TunoAppLockerToIntune.ps1 turns a finished policy into an Intune profile, which is step 5's job; sitting in step 1 it made the first thing you are asked to do look twice as heavy as it is. It has moved to step 5, next to the instructions that use it. Step 1 now asks for one script." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Step 1 also answers a question the walkthroughs raise: no, you do not need Sysinternals. AaronLocker and the write-ups based on it shell out to AccessChk.exe and parse its output; this scan works write access out natively from each directory's security descriptor, with Deny entries honoured \u2014 which is not just fewer downloads but the more correct answer, because AaronLocker's own alternate-data-stream check ORs the rights across every matching entry without checking whether one of them is a Deny, so a Deny there reads as a grant. Nothing is downloaded or run from anywhere but this site." },
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The tool's own intro card stopped short of the cards beneath it. It carries a 1180-pixel cap of its own, which was invisible while everything sat in an 1180-pixel shell and became a ragged edge the moment this screen went wide. The card now matches its neighbours; the paragraph inside it keeps a measure, because a line of text 1600 pixels long is unreadable however tidy the edge looks." },
    ],
  },
  {
    build: 10312, date: "2026-08-19", title: "Five things that were in the way",
    items: [
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Scanning a device looked compulsory. Two cards stacked one above the other read as a sequence, so the obvious conclusion was that nothing could happen until PowerShell had been run somewhere \u2014 when a policy XML you already have goes straight to step 2 and always could. Step 1 now says it is optional, and there is a divider between the two that says out loud that this is a fork rather than an order." },
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The fix button was the first thing to disappear. It had a column of its own at the far right of the findings table, which meant that on any window narrow enough to need a horizontal scroll, the one control on the row that actually does something was the one you had to scroll sideways to reach. It now sits underneath the recommendation it carries out \u2014 next to the words it applies, in the same cell, and no longer at the mercy of six columns to its left." },
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Copy and Download scrolled out of sight. The code panel is sticky, but it was pinned 14 pixels from the top of the window \u2014 which is behind the header and the tab bar, both of which are sticky too. So scrolling down slid the panel's own head, with the filename and both buttons, underneath them, and getting to the buttons meant scrolling back up. It now pins below that chrome, using the same measurement the rest of the app already keeps up to date, and its height accounts for it too." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The Microsoft default rules are per collection \u2014 Exe, Msi, Script, Dll and Appx each get their own set \u2014 and there was no way to see that. The button appeared only on a collection that was missing them and was invisible otherwise, so the state was legible only by its absence. Enforcement per collection now carries a column of its own: every row says either \u201cpresent\u201d or offers an Add, for all five types, including one that is not in the policy yet. Where more than one is missing there is a single press that does the lot, and a single undo that takes it back." },
      { kind: "improved", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Coverage of the enforcement table itself: it scrolls sideways now rather than being clipped, like the other three." },
    ],
  },
  {
    build: 10311, date: "2026-08-19", title: "The registration script knows what the app now asks for",
    items: [
      { kind: "fixed", tool: "All tools", text: "The previous build gave TUNO a permission the thing that registers TUNO had never heard of. New-TunoAppRegistration.ps1 still declared User.Read and SecurityEvents.Read.All, with a comment stating that the AppLocker tool reads nothing from the tenant \u2014 true when it was written, false the moment step 5 could create a profile. It now declares DeviceManagementConfiguration.ReadWrite.All and Group.Read.All as well, each with a note saying which tool needs it and why, ENCA-style. The script has always been safe to re-run: it updates an existing registration rather than making a second one." },
      { kind: "fixed", tool: "All tools", text: "A trap in that same script, found while fixing the first thing. It sets the SPA redirect URIs from one list and Update-MgApplication REPLACES that array rather than merging into it \u2014 so the two beta-channel URIs, which had been added by hand in the portal, would have been silently deleted by the next run, breaking sign-in on the beta site with nothing on screen to explain why. All four hosts are in the default list now, and the comment says why they have to stay there." },
      { kind: "improved", tool: "All tools", text: "The security documentation stops claiming TUNO only reads. It names the single write scope, says Graph offers no narrower split \u2014 the check for an existing profile and the creation of one are the same permission \u2014 and lists the constraints the tool imposes on itself beyond what the permission allows: nothing overwritten, creating and assigning kept apart, enforcement gated, writes never retried, no delete path at all. It also spells out how to register TUNO WITHOUT the write scope, and what still works if you do." },
    ],
  },
  {
    build: 10310, date: "2026-08-19", title: "Deploying it is a button now, and it argues with you first",
    items: [
      { kind: "new", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Step 5 used to be three sets of instructions for doing it yourself. It now has a fourth option that does it: TUNO creates the Intune profile in the tenant you are signed in to. This is the first thing TUNO writes to a customer tenant, and it works under rules deliberately narrower than Graph would allow. The permission is asked for on the click, not at sign-in \u2014 reading an XML in your own browser should not buy the right to create configuration profiles. Nothing is overwritten: a read runs first, and a profile carrying the same name or the same AppLocker grouping stops the deploy and is reported with the date it was last changed, because TUNO did not create it and does not get to change it." },
      { kind: "new", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Assignment is a second act, never part of the first. Creating a profile changes nothing on any device; assigning it is the moment that stops being true. So the pilot group is searched by name, and the confirmation states how many members are about to receive an application-control policy before you press anything. A dynamic group is labelled as one, because its membership can change afterwards without anyone touching the assignment." },
      { kind: "new", tool: "\ud83d\udd10 AppLocker builder & validator", text: "The Enforce profile cannot be created on enthusiasm. It stays locked until two things are true: the audit profile exists in this tenant, and an uploaded scan shows nothing was blocked and nothing would have been. Until then the button says which of the two is missing \u2014 no scan at all, a scan that could not read the event logs, so many still blocked, so many that would have been. That order is the entire discipline of the tool, and a screen that let you skip it would be disagreeing with its own instructions." },
      { kind: "new", tool: "All tools", text: "A Graph layer, which TUNO did not have. It asks for scopes per capability rather than up front, and it repeats a refusal in the tenant's own words with the error code and request-id attached, plus an admin-consent link when that is what is actually missing. Paraphrasing Graph into \u201csomething went wrong\u201d is how an administrator ends up guessing at their own tenant. Writes are never retried: a POST that timed out may have created something, and the honest answer is to say so and let you look." },
      { kind: "fixed", tool: "\ud83d\udd10 AppLocker builder & validator", text: "Found while testing the above: uploading a scan bundle wiped the record that the audit profile had been created \u2014 which is precisely the step the walkthrough tells you to take between auditing and enforcing, so the upload erased the evidence that step one had happened and re-locked the gate asking for a profile that was already there. Created profiles now survive a re-import, tagged with the name and grouping they were made under so they stop counting the moment those change." },
      { kind: "improved", tool: "All tools", text: "The wide layout moved from the tool to the shell. Build 10309 widened only the split, which left the page ragged \u2014 full-width cards above a narrower column, and a code panel running off the right edge, because a negative margin against a percentage does not centre against the viewport. The header, the tab bar, the intro, the numbered steps, the toolbar and both columns now share one width and start and end on the same two lines. Leaving the tool narrows everything back." },
    ],
  },
  {
    build: 10309, date: "2026-08-19", title: "The two columns had nowhere to go",
    items: [
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Splitting the screen in two gave neither half enough room, and a wider monitor did not help, because the limit was never the monitor. The whole app sits in an 1180-pixel shell. Two columns inside that left the findings table about 700 pixels for seven columns — the condition ended up wrapping one character per line — and the code panel about 400, narrow enough that every path broke mid-token. T01 is now the one screen that escapes the shell: it widens with the window up to 1680 pixels and re-centres itself, and the code panel gets 520 of that. Below 1240 pixels there is no room for two columns and it stacks, which is what the old breakpoint was trying to say and set too low to mean." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Two tables were being cut off rather than scrolled. Coverage and Rules had no horizontal scroller of their own, and the cards they sit in hide their overflow, so anything past the card edge was simply not there — no scrollbar, no ellipsis, just missing text. Both scroll now, and the flex children carry a zero minimum width so a wide table can no longer push its card past the column it was given." },
      { kind: "fixed", tool: "🔐 AppLocker builder & validator", text: "Paths stop being broken mid-token where the line had a space to break at instead. The rule condition, the finding condition, the two scan tables from the previous build and the code panel all asked for a break anywhere; they now ask for a normal break first and only fall back to breaking inside a token when the token genuinely does not fit." },
    ],
  },
  {
    build: 10308, date: "2026-08-19", title: "The tool can finally see the machine",
    items: [
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "A browser cannot read a directory permission, verify a signature or open an event log — and those three facts are most of what decides whether an AppLocker policy is sound. A rule allowing the Program Files folder is only as strong as the permissions underneath it, and the XML does not say what those are. So the tool now hands you the PowerShell that can: Invoke-TunoAppLockerScan.ps1, downloaded from the tool itself, run once on a representative device. It finds every directory a standard user can write to, inventories the executables sitting in them with their signer and version, reads what AppLocker has already blocked, and builds a rule set with every one of those directories excepted out of the default rules. It changes nothing on the device." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "Upload what the scan wrote and the audit gains a second half. Findings marked with a satellite came from the device rather than the file: a writable directory an allow rule still reaches, named together with the rule that reaches it and the group that can write there; unsigned programs that can only ever be allowed by a hash rule, which stops working the day they are updated; and the executions the endpoint has already refused, per user. Those verdicts are reached with the same evaluation the Microsoft coverage table uses, by asking what would happen to an executable dropped into the directory — there is no second opinion to disagree with the first." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "The panel on the right has a second tab: the Intune custom profile. AppLocker has no settings-catalog surface, so the supported route is a custom profile carrying one OMA-URI string per rule collection, and that is now built for you from the policy on screen — name it, give it a grouping, pick Audit or Enforce, and copy or download it. The DLL collection always ships as NotConfigured whatever you pick: AppLocker evaluates every DLL load, so enforcing it cripples the device and even auditing it buries the event log. The rules are still in the profile, documented and inert." },
      { kind: "new", tool: "🔐 AppLocker builder & validator", text: "A second script, Convert-TunoAppLockerToIntune.ps1, does the same conversion on the command line — offline it writes the profile JSON, online it creates the profile in the tenant. The screen now reads as five numbered steps, scan through deploy, with the portal, Graph and script routes written out including the ones nothing here does for you: assign the audit profile first, to a pilot group, and make sure the Application Identity service is actually running or AppLocker does nothing and logs nothing." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The scan bundle carries three policies — the generated audit set, the generated enforced set, and the policy the device was really running — and you can put any of them on the table and switch between them. It opens on the audit set, because that is the only one it is ever safe to start with. The Markdown report gained the device facts, what the scan could not see, and the full list of writable directories still reachable through a rule." },
    ],
  },
  {
    build: 10307, date: "2026-08-19", title: "You can see the XML you are about to ship",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The tool produced its XML only as a download, so the artefact that actually goes into the GPO was the one thing you never looked at. It now has a panel of its own down the right-hand side, syntax-coloured and redrawn on every change — apply a fix, add a rule, change an enforcement mode, undo, and the XML moves with it. Copy and Download XML sit on that panel rather than in the toolbar at the top of the page, because they belong to the thing they act on. Export MD stays in the toolbar." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "The screen is two columns: the audit, coverage and rules on the left, the XML on the right, sticky, scrolling inside itself. Below 1100px the panel drops underneath instead of shrinking — a column too narrow to show a file path wraps it into something you cannot read. The panel renders from the same export function the download uses, so a preview cannot drift from the file: there is no second serialiser to keep in step." },
    ],
  },
  {
    build: 10306, date: "2026-08-19", title: "The promotion queue says how to check it, not just what it is",
    items: [
      { kind: "improved", tool: "All tools", text: "The beta-only promotion queue in Help was a four-column table that told you a change existed and how risky someone thought it was. It is now ENCA's table, ported: each row still carries its stable number, but the risk cell explains what that word means, the beta builds have a column of their own, the files the change touched are listed, and the reasoning is split in two. Why says what the risk is and what would have to be true for the item to graduate. How to test it — folded away, because you read the table to decide what to promote and the steps only once that decision is made — names the tenant or policy state each check needs and the outcome you should see, so a step can fail rather than be nodded through. Where a check cannot be run without a tenant nobody has to hand, the step says so: knowing which check was skipped is worth more than a list that pretends all of them were run." },
      { kind: "improved", tool: "All tools", text: "All three queued items were written up against that standard — twenty-three steps in total, including the ones that matter most: that undo restores an AppLocker policy byte-for-byte, that a ruleless collection is never offered a straight Enable, that the new logo arrives on a browser that saw the old one WITHOUT clearing its cache, and that the tab labels do not drag the tiles' BETA tag along with them. An item with no checklist now renders as \"not written\" instead of rendering as nothing, because an item nobody has said how to check is not finished." },
    ],
  },
  {
    build: 10305, date: "2026-08-19", title: "Findings you can act on",
    items: [
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Every finding in the audit already ended in a recommendation, and every recommendation ended in you scrolling down to the builder to carry it out by hand. Findings now have a fix button. Where the recommendation is mechanical the button just does it: set an AuditOnly collection to Enabled, add the Microsoft default rules to a collection that has none, add a collection the XML never mentioned. Where the recommendation needs a fact the tool does not have — the exact publisher behind a path allow, an upper version bound, the purpose-built group that should replace Everyone — the button opens that rule prefilled and you supply the missing piece, with tighten-the-path, replace-with-a-publisher-rule and delete all one dropdown apart. It refuses to invent a publisher or a version for you, the same way the report refuses to pretend the NTFS checks ran." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "One deliberate disagreement with the tool's own advice: a collection with no rules is never offered a straight Enable, because enabling it would block every file of that type — exactly what the next finding down warns about. A ruleless collection is offered AuditOnly, or the default rules first, and only then the enable." },
      { kind: "improved", tool: "🔐 AppLocker builder & validator", text: "Undo. Every change to the policy — fix buttons, the coverage table's add-allow-rule, the enforcement dropdowns, the delete crosses — leaves one step back, named for what it did, next to the severity filters. A misclick no longer costs you the import." },
    ],
  },
  {
    build: 10304, date: "2026-08-19", title: "The old logo can no longer haunt from the cache",
    items: [
      { kind: "fixed", tool: "All tools", text: "Dark mode kept showing the pre-10303 logo with its baked-in box: the dark variant is loaded by a stylesheet content-url swap, and neither that URL nor the img and favicon references carried a version — so a browser that had seen the old art kept serving it (GitHub Pages caches assets for ten minutes, browsers at their own discretion). All five logo and favicon references now carry an asset version (v=2, bumped whenever the artwork itself changes), so new art arrives the moment the page does. The SVGs themselves were already clean." },
    ],
  },
  {
    build: 10303, date: "2026-08-19", title: "The logo loses its box",
    items: [
      { kind: "fixed", tool: "All tools", text: "The office logo shipped as a full 1024-pixel canvas: a square background baked into the SVG and the emblem floating in a wide margin, which is why the sign-in screen showed a small mark inside a visible box. The background rect is gone (transparent), the viewBox is cropped to the emblem and its orbit ring, and the favicon is cut tighter still — the medallion only, so it survives 16 pixels. The sign-in screen draws the mark at 132 pixels instead of 76. TUNO's copies only; the Website-Limon-IT originals are untouched." },
    ],
  },
  {
    build: 10302, date: "2026-08-19", title: "TUNO wears its own face",
    items: [
      { kind: "new", tool: "All tools", text: "The TUNO office logo replaces the Limon-IT mark in the header, on the sign-in screen and as the favicon — light and dark variants, with the existing theme swap picking the right one per mode. The source lives with the other office logos in the Website-Limon-IT icon set; the app carries its own copies in assets, on the same filenames the stylesheet already knew." },
      { kind: "new", tool: "Roadmap", text: "Two ports from ENCA join the roadmap as Next: R04 Group Analyzer (ENCA's T19, turned Intune-side-out — enrolment, compliance, configuration, scripts, apps, Autopilot and update rings lead the answer) and R05 Change audit (ENCA's T16 pointed at deviceManagement auditEvents — who changed which Intune policy, when, with the field-level diff where the record carries it). References follow the house rules: permanent, never reused, not a priority order." },
    ],
  },
  {
    build: 10301, date: "2026-08-19", title: "The tab bar is ENCA's tab bar",
    items: [
      { kind: "fixed", tool: "All tools", text: "The tool bar under the header is now ENCA's browser-style tab bar, ported verbatim, instead of the unstyled always-on button strip build 1 shipped: a home button that reads as a button, pill-shaped tabs that exist only for the tools you have OPENED (with a close cross each), a plus menu to open another tool in a new tab, a close-all button once more than one tab is open, and Help pinned at the right. The bar stays hidden on the tools home and appears with the first tool you open. The old strip also dragged the tile's NEW and BETA tag text into its labels; tab labels now come from the tool list, with the emoji and nothing else." },
    ],
  },
  {
    build: 2, date: "2026-08-19", title: "Sign-in works — the app registration exists",
    items: [
      { kind: "new", tool: "All tools", text: "The TUNO (Limon-IT) app registration is live and js/authConfig.js carries its client ID, so the sign-in screen signs in instead of explaining why it cannot. Multi-tenant SPA, authorization code + PKCE, no client secret; delegated User.Read plus SecurityEvents.Read.All consented up front so the Secure Score visualizer (roadmap R02) can ask for it on the click when it lands. Created by New-TunoAppRegistration.ps1, which also patched this config." },
    ],
  },
  {
    build: 1, date: "2026-08-19", title: "TUNO exists — and it opens with AppLocker",
    items: [
      { kind: "new", tool: "AppLocker builder & validator", text: "TUNO's first tool, in BETA. Import an AppLocker policy XML — a GPO export or the output of Get-AppLockerPolicy -Effective -Xml — and it is audited in the browser with the AppLockerInspector check set: collections left NotConfigured or AuditOnly, allow rules granted to Everyone / Authenticated Users / BUILTIN-Users, paths under user-writable trees (profiles, AppData, Temp, Downloads, ProgramData), drive-root and wildcard patterns, UNC allows, publisher rules with any-product / any-binary / no upper version bound, and hash rules assigned to broad principals — each finding with a severity, the reason and a recommendation, and an allow that a deny rule provably shadows is skipped rather than reported. After Spencer Alessi's AppLockerInspector; the NTFS and share ACL reality checks need a filesystem and stay in his PowerShell — the report says which checks did not run here rather than pretending they did." },
      { kind: "new", tool: "AppLocker builder & validator", text: "The Microsoft coverage check — the question every locked-down estate discovers too late: are the Microsoft apps people actually use still allowed to run? A catalog of the ones that bite (OneDrive's per-user install under AppData first among them, plus Teams, Edge WebView2, Office click-to-run and the servicing executables) is evaluated against the imported policy the way AppLocker itself would decide — deny beats allow, exceptions respected, path macros expanded, publisher fields matched — and every app comes back allowed, blocked, or allowed only through a rule the audit flags as risky. A blocked app gets a one-click fix: the publisher rule (path fallback where signing is inconsistent) added to the policy." },
      { kind: "new", tool: "AppLocker builder & validator", text: "The builder half: start from the imported policy or from empty, add the Microsoft-recommended default rules per collection, set enforcement modes, add publisher / path / hash rules by hand, delete what should not be there — and export the result as AppLocker policy XML that imports straight back into a GPO, plus a Markdown findings report for the change ticket. Everything runs in the browser; the XML never leaves the machine." },
      { kind: "new", tool: "All tools", text: "The TUNO shell itself, built as ENCA's sister: same layout, same branding, same sign-in model (a SPA using authorization code + PKCE, no client secret, single-tenant registration supported via New-TunoAppRegistration.ps1), same two-series build numbering, same changelog / roadmap / promotion-queue discipline, and the same BETA ribbon on any host that is not tuno.limon-it.nl." },
    ],
  },
];
